<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>MySQL事务</title>
    <link href="/2022/08/08/%E4%BA%8B%E5%8A%A1/"/>
    <url>/2022/08/08/%E4%BA%8B%E5%8A%A1/</url>
    
    <content type="html"><![CDATA[<h1 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h1><p>四大特性：ACID（原子性、一致性、隔离性、持久性）</p><p>用于保证一组SQL语句执行的完整性并且执行期间不会受到其他不相关的SQL语句的影响</p><h2 id="事务隔离"><a href="#事务隔离" class="headerlink" title="事务隔离"></a>事务隔离</h2><p>事务的隔离级别：</p><ul><li>读未提交：事务未提交时所做的变更就已经能被别的事务看到</li><li>读提交：事务提交后做的变更才能被别的事务看到，视图在每个SQL语句执行前创建</li><li>可重复读：事务在开始时会创建一个视图，在事务执行的整个过程中，事务看到的数据都以这个视图为准，整个过程中数据一致，只有自己的事务所进行的修改才为可见</li><li>串行化：对于同一行一句，读加读锁，写加写锁，读写冲突时后面的事务需要等待前面的事务提交</li></ul><p>四种隔离级别自上而下，随着隔离级别的提高，并发性能依次下降，InnoDB默认的隔离级别为可重复读。</p><p><strong>事务启动方式</strong></p><p><code>set autocommit = 1</code>，再通过显示的方法启动事务，手动begin commit</p><p>可以使用<code>commit work and chain</code>,可以提交当前事务并开启下一个事务，减少一次交互，在频繁使用事务的业务中使用</p><p><strong>事务的启动时机</strong></p><ul><li>对于bein&#x2F;start transsaction，在执行到第一个操作InnoDB表的语句时，才最终启动事务</li><li>start transaction with consistent snapshot 立即启动事务，创建一致性视图</li></ul><p><strong>视图</strong></p><p>分为两种：</p><ul><li>一种为view，通过查询结果而创建，为一个虚拟表，可以当作一个表来进行查询，为真实的物理结构</li><li>另一种为consistent read view,为一致性读视图，用于实现MVCC,在读提交和一致性读的隔离级别下存在，不存在真实的物理结构，只是定义的概念</li></ul><h2 id="回滚日志"><a href="#回滚日志" class="headerlink" title="回滚日志"></a>回滚日志</h2><p>可重复读需要保证在整个事务的执行过程当中的数据的一致性，在此期间，如果有别的事务对数据进行的修改，则需要通过回滚日志 undolog找到视图创建时的版本</p><p>值得注意的是，对于回滚日志，回滚日志中记录的是每一步的操作，想要得到前几步的结果需要通过当前数据加计算得到，因此长事务的回滚，性能开销也较大，会导致慢sql的问题</p><p><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E4%BA%8B%E5%8A%A1%E4%B8%93%E9%A2%98.assets/d9c313809e5ac148fc39feff532f0fee.png" alt="img"></p><p><strong>回滚日志的删除</strong></p><p>当没有事务需要这个回滚日志时，即与该回滚日志相关的事务均已提交，该日志则会被删除</p><h2 id="版本快照"><a href="#版本快照" class="headerlink" title="版本快照"></a>版本快照</h2><p><strong>transaction id</strong></p><p>在事务开始时，InnoDB会向事务分配一个transaction id，按照严格递增的顺序</p><p><strong>trx_id</strong></p><p>数据库中的每一行都有自己的版本，版本号即为trx_id</p><p>数据由事务产生，在事务更新数据时,将transaction id赋值给该行的trx_id,借此，便将事务和数据关联起来。</p><p><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E4%BA%8B%E5%8A%A1%E4%B8%93%E9%A2%98.assets/68d08d277a6f7926a41cc5541d3dfced.png" alt="img"></p><h3 id="活跃数组"><a href="#活跃数组" class="headerlink" title="活跃数组"></a>活跃数组</h3><p>由于trx_id是由transaction id生成的，因此二者之间便存在统一性，对于事务A，如果事务B是在事务A之后创建的，那么B的transaction id即大于A，同样trx_id也大于A，便可以借此将B操作产生的数据给”屏蔽掉”</p><p>实现上，InnoDB为<strong>每个事务</strong>，用于保存这个事务启动瞬间时所有事务的状态，为一个静态数组，可认为拍了一张快照保存状态</p><ul><li>活跃指的是启动了但还未提交的事务,以最小的transaction id作为低水位，以最大的作为高水位</li><li>低水位之下的为已提交的事务</li><li>高水位之上的为目前还未开始的事务</li></ul><p><strong>数据的可见性</strong></p><ul><li>如果trx_id小于低水位，那么数据为当前事务开始前产生的，可见</li><li>trx_id大于高水位,数据为当前事务开始之后其他事务做出的更新，不可见</li><li>trx_id位于高低水位之间，则：<ul><li>如果trx_id存在于活跃数组当中，则表明该数据由未提交的事务产生，不可见</li><li>不存在于活跃数组当中，则表明该事务已经提交，数据可见</li></ul></li></ul><blockquote><p>对于高低水位，只能表明出一个范围，但是在此范围之间的可见性还要根据活跃数组来判断。</p><p>例如，低水位对应的事务为一个长事务，事务B为一个短事务，事务B在低水位事务开始后开始，transaction id大于低水位事务，但是B为短事务，在事务A开始时事务B已经提交，那么事务B则不存在于事务A的活跃数组当中，因此事务B更新的行对于A为可见的</p></blockquote><p>在可重复读的隔离级别下，通过回滚日志undolog，找到对自己可见的trx_id的数据即可</p><h3 id="当前读"><a href="#当前读" class="headerlink" title="当前读"></a>当前读</h3><p>在进行更新操作时，可重复的理念与并发更新存在冲突，并发更新在当前的最新值基础上进行更新，而可重复读又恰好看不到当前的最新值，有点类似于操作系统中其他线程产生的最新值还在寄存器当中，而该线程的更新无法读取到寄存器当中的值。</p><p>因此，为了解决这种情况，存在这样一条规则</p><p>更新数据都是先读后写的，而这个读，只能读当前的值，称为“当前读”（current read）。</p><p>先读出最新的版本号的数据，在此之上进行更新</p><p>当前读的语句：</p><ul><li>update</li><li>select 加锁时的情况：<code>for update / lock in share mode</code></li></ul><p>由于读写锁之间的冲突，只有等待事务提交之后，select 语句才能读到update所作出的变更</p>]]></content>
    
    
    
    <tags>
      
      <tag>数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MySQL日志系统</title>
    <link href="/2022/08/08/%E6%97%A5%E5%BF%97%E7%B3%BB%E7%BB%9F/"/>
    <url>/2022/08/08/%E6%97%A5%E5%BF%97%E7%B3%BB%E7%BB%9F/</url>
    
    <content type="html"><![CDATA[<h1 id="日志系统"><a href="#日志系统" class="headerlink" title="日志系统"></a>日志系统</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>在目前的MySQL当中，日志系统主要通过redo log和binlog相配合，redo log 由InnoDB引擎提供，而binlog为MySQL Server层中实现，redo log负责在崩溃时进行恢复工作，binlog负责进行归档，用于备份和主从数据一致</p><h2 id="redo-log"><a href="#redo-log" class="headerlink" title="redo log"></a>redo log</h2><h3 id="主要功能"><a href="#主要功能" class="headerlink" title="主要功能"></a>主要功能</h3><p>redo log主要用于负责进行crash-safe功能，当MySQL崩溃时，便可以通过redo log将数据进行恢复</p><p>简单来说，在MySQL向硬盘中写数据进行持久化之前，先写入日志系统，并且更新内存当中的数据。</p><p>此时，由于之后对于当前数据页的读取都是直接从内存中读取，再进行CRUD时，因此可以认为以及完成了此次的数据更新过程，MySQL只需要再之后寻找机会再将内存中的数据<strong>flush</strong>到硬盘当中去即可。之后便涉及到另外一个相关的知识点flush脏页回写的过程</p><h3 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h3><p><strong>循环写入：</strong></p><ul><li><p>相比于binlog的追加式写入方式，redo log为循环写入，空间会用完，即存在一个checkpoint和write pos，当write pos追上 checkpoint时，即需要清除一些记录，推进一下checkpoint</p></li><li><p>如果一个脏页已经被flush到内存当中，那么即便断电也不会收到威胁，因此可以对应的日志也无用，在推进checkpoint清除记录时，即可以将其删掉</p></li><li><p>循环写入指的是四个文件循环写入，而并非整个redo log为一个大型循环链表类似的结构</p></li></ul><p><strong>记录形式：</strong> redo log为物理日志，记录的是进行的是在哪个数据页上进行了什么修改</p><p><strong>写入时机：</strong> redo log的写入无需等待事务的提交，事务中进行了相应的操作边写入到redo log buffer当中(为进行持久化)</p><p><strong>来源：</strong> 由InnoDB引擎所支持，MySQL本身并不存在redo log</p><p><strong>缓存：</strong> 相比于binlog的 binlog cache，redo log buffer的为全局维护一个</p><h3 id="写入机制"><a href="#写入机制" class="headerlink" title="写入机制"></a>写入机制</h3><p>整个redo log的写入过程可以认为有三部分：</p><ul><li>写入redo log buffer</li><li>写入page cache</li><li>fsync刷到硬盘中进行持久化</li></ul><p>在这个三个步骤其中，page cache(页高速缓冲存储器)为操作系统的相关机制，本质上为向文件系统申请的一块内存，用于处理内外存直接的速度同步问题，可以移步操作系统进行进一步学习</p><p>只有真正的写入到了硬盘中的redo file当中去，才可认为是进行了持久化，之后才可以根据其进行cash safe</p><p><strong>持久化策略</strong></p><blockquote><p>InnoDB 提供了 innodb_flush_log_at_trx_commit 参数，它有三种可能取值：</p><ul><li>设置为 0 的时候，表示每次事务提交时都只是把 redo log 留在 redo log buffer 中 ;</li><li>设置为 1 的时候，表示每次事务提交时都将 redo log 直接持久化到磁盘；</li><li>设置为 2 的时候，表示每次事务提交时都只是把 redo log 写到 page cache。</li></ul></blockquote><p>而后台有一个进程专门进行持久化操作，每隔一秒进行一次轮询，将redo log buffer当中的数据直接持久化磁盘当中(先page cache再write)</p><p>进行持久化的方式：</p><ul><li>等待后台线程的轮询</li><li>被其他并行的进程顺带着持久化：由于无需等待事务提交即可将redo log写入redo buffer当中，因此redo log位于同一个数据页的事务进行了提交，将整个数据页持久化到硬盘当中，还未提交的也没等来轮询的事务的redo log 就被顺带着持久到硬盘当中去</li><li>innodb_flush_log_at_trx_commit设置为1时主动提交</li></ul><p>而在innodb_log_buffer_size到达一半时会将其写入到page cache中，还需fsync然后进行持久化</p><h4 id="组提交"><a href="#组提交" class="headerlink" title="组提交"></a>组提交</h4><p><strong>LSN：</strong> 日志逻辑序列号，单调递增，用来对应redo log的一个个写入点，每次写入长度为 length 的 redo log， LSN 的值就会加上 length。</p><p>每一轮中最先抵达的事务选为leader，后续每来一个事务，则将leader的LSN递增至当前的LSN，在需要进行持久化时，在将所有小于等于leader LSN的事务的日志作为一个组全部进行持久化</p><p>LSN有点类似于事务的多版本数据，在时间轴上进行一个标记</p><p><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E6%97%A5%E5%BF%97%E7%B3%BB%E7%BB%9F%E4%B8%93%E9%A2%98.assets/933fdc052c6339de2aa3bf3f65b188cc.png" alt="img"></p><h2 id="binlog"><a href="#binlog" class="headerlink" title="binlog"></a>binlog</h2><h3 id="主要功能-1"><a href="#主要功能-1" class="headerlink" title="主要功能"></a>主要功能</h3><p>归档备份，用于恢复数据，或者通过binlog传递数据，保证主从库的数据一致性</p><h3 id="特点-1"><a href="#特点-1" class="headerlink" title="特点"></a>特点</h3><p><strong>追加写入：</strong> 写完当前的文件后会切换到下一个文件，并不会对之前的进行覆盖</p><p><strong>记录形式:</strong> binlog为逻辑日志，记录的时进行了什么操作：</p><ul><li>在statement形式时，记录的为原始sql语句，</li><li>在row形式时，记录的为发生了什么事件，如用于定义删除的行为</li></ul><p><strong>缓存:</strong> 每个线程单独一个binlog cache</p><p><strong>写入时机：</strong> 按照两阶段提交，在redolog写完并且事务处于prepare的状态时，开始进行binlog的写入</p><h3 id="写入机制-1"><a href="#写入机制-1" class="headerlink" title="写入机制"></a>写入机制</h3><p><strong>binlog格式</strong></p><ul><li>statement<ul><li>记录的为原始sql语句</li><li>在进行主备统一时会存在数据不一致的问题(如使用了limit并且索引选择不一致)</li><li>通过COMMIT标识来确定日志的完整性</li></ul></li><li>row<ul><li>记录的为事件，如Delete_rows event，用于定义删除的行为</li><li>在不同的情况下可以完全复现，直指原本操作的数据行，而非再对其重新执行得到结果</li><li>相比于statement形式，占用的空间较大</li><li>可以用于进行数据恢复，由于记录的为事件，可以对事件中涉及的数据直接进行逆向操作,如delete-&gt;insert，update前后写过互调</li><li>通过XID event确定日志的完整性</li></ul></li><li>mixed<ul><li>statement和row相结合，利用各自的优点</li><li>由MySQL自己进行判断选择，如有limit则使用row,无则使用statement</li></ul></li></ul><p><strong>写入过程：</strong></p><p>事务执行时，先将其写入到binlog cache当中,由于binlog的写入需要保证完整性不被拆开，因此每个线程独享一个binlog cache</p><p>事务提交时，再将binlog cache中的数据写入到page cache后再fsync进行持久化到磁盘当中去,持久化完成后再将cache中的数据进行清空</p><p><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E6%97%A5%E5%BF%97%E7%B3%BB%E7%BB%9F%E4%B8%93%E9%A2%98.assets/9ed86644d5f39efb0efec595abb92e3e.png" alt="img"></p><p><strong>控制策略：</strong></p><blockquote><p>write 和 fsync 的时机，是由参数 sync_binlog 控制的：</p><ol><li>sync_binlog&#x3D;0 的时候，表示每次提交事务都只 write，不 fsync；</li><li>sync_binlog&#x3D;1 的时候，表示每次提交事务都会执行 fsync；</li><li>sync_binlog&#x3D;N(N&gt;1) 的时候，表示每次提交事务都 write，但累积 N 个事务后才 fsync。</li></ol></blockquote><p>sync_binlog 设置为 N，对应的风险是：如果主机发生异常重启，会丢失最近 N 个事务的 binlog 日志。</p><h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><p><strong>两阶段提交</strong>:prepare和commit状态的设计意义</p><p>简要来说即为redo log 和binlog穿插写入，二者全部写好之后再一同提交</p><p>反证：</p><ul><li><p>前提：InnoDB中redo log提交了则事务无法进行回滚</p></li><li><p>先redo log再binlog，可crash-safe但归档中事务丢失，影响备份和从库</p></li><li><p>先binlog再 redo log，备份中存在事务，但是数据库中无该事务进行的操作，同样出现数据不一致的问题</p></li></ul><p>两阶段提交的意义为保证redo log和binlog写入的原子性，redo log对应着当前数据库中的数据,binlog对应着备份中的数据，只有保证二者的原子性才能保证备份和当前数据库的数据一致</p><p><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E6%97%A5%E5%BF%97%E7%B3%BB%E7%BB%9F%E4%B8%93%E9%A2%98.assets/ee9af616e05e4b853eba27048351f62a.jpg" alt="img"></p><p><strong>关联：</strong></p><p>redo log和binlog有一个共同的数据字段，叫 XID。崩溃恢复的时候，会按顺序扫描 redo log：</p><ul><li>如果碰到既有 prepare、又有 commit 的 redo log，就直接提交；</li><li>如果碰到只有 parepare、而没有 commit 的 redo log，就拿着 XID 去 binlog 找对应的事务。</li></ul><p><strong>与内存的关系</strong></p><p>在执行sql语句时，向redo log buffer之中写入redo log，并且此时已经对内存进行了修改(否则无法进行当前读和当前事务在此基础上的操作)，二者之间不存在先后以及等待关系</p><p><strong>change buffer&#x2F;redo log</strong></p><ul><li><p>对于一条更新数据，数据页存在于内存当中直接写入，若不在则写入change buffer</p></li><li><p>而redo log 对于上述两种动作进行记录，再根据这些记录，对磁盘进行更新，即redolog有两种，一种记录普通数据页的改动，一种记录changebuffer的改动</p></li><li><p>真正对磁盘数据页的修改是通过将内存里脏页的数据刷回磁盘来完成的，而不是根据redolog， 真正刷新磁盘的工作为innodb中的存储引擎中的线程去做</p></li><li><p>change buffer 和redo log 二者的作用目标不同，关注的为两个事情</p><ul><li>redo log作为一个日志系统，最大的作用为再数据库宕机后的恢复工作</li></ul></li></ul><p><strong>双1配置：</strong></p><p>通常我们说 MySQL 的“双 1”配置，指的就是 sync_binlog 和 innodb_flush_log_at_trx_commit 都设置成 1。也就是说，一个事务完整提交前，需要等待两次刷盘，一次是 redo log（prepare 阶段），一次是 binlog。</p><h4 id="突发断电"><a href="#突发断电" class="headerlink" title="突发断电"></a>突发断电</h4><ol><li><p>而对于还未进行持久化的redo log buffer，由于此时的redo log还存储于内存当中，binlog还未开始写，事务也处于还未提交的状态，现实，备份，逻辑上数据均未被记录(发生),因此直接认为事务回滚</p></li><li><p>redo log已经写完，内部事务完整，并且已经有了commit标识(表示binlog也已经写完)，供电后直接进行提交(逻辑，现实，备份均已经有了数据记录)</p></li><li><p>redo log写完，但是只处于prepare状态</p><ul><li>binlog完整，提交事务</li><li>binlog不完整，回滚事务</li></ul></li></ol>]]></content>
    
    
    
    <tags>
      
      <tag>数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>并发与锁</title>
    <link href="/2022/08/08/%E5%B9%B6%E5%8F%91%E4%B8%8E%E9%94%81/"/>
    <url>/2022/08/08/%E5%B9%B6%E5%8F%91%E4%B8%8E%E9%94%81/</url>
    
    <content type="html"><![CDATA[<h1 id="并发与锁"><a href="#并发与锁" class="headerlink" title="并发与锁"></a>并发与锁</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>设计用于控制并发问题，保证并发状态下的合理的控制资源的访问规则<br>在粒度上，可以分为全局锁，表锁，行锁<br>相关名词：</p><ul><li>全局锁：锁住整个数据库</li><li>表锁：锁住表</li><li>行锁：锁索引</li><li>共享锁</li><li>排他锁</li><li>悲观锁（抽象）</li><li>乐观锁（抽象）</li></ul><h2 id="全局锁"><a href="#全局锁" class="headerlink" title="全局锁"></a>全局锁</h2><p><strong>效果</strong>：锁住整个数据库，整个数据库都处于只读的状态，DDL和DML和更新类的事务提交语句都被阻塞<br><strong>命令</strong>：</p><ul><li>推荐使用 Flush tables with read lock</li><li>set global readonly&#x3D;true的影响范围较大，风险较高<br><strong>使用场景</strong>：做全库备份，锁住保证主库备库的逻辑一致</li></ul><h2 id="表级锁"><a href="#表级锁" class="headerlink" title="表级锁"></a>表级锁</h2><p>共有两种表级锁：元数据锁(MDL)和表锁</p><p>在限制上可以分为读锁和写锁，写锁和写锁，写锁和读锁冲突，读锁和读锁之间不产生冲突</p><p><strong>表锁</strong>：</p><ul><li>显式使用，表锁语法为：<code>lock tables...read/wrtie</code> ，可以通过unlock tables主动释放锁，也可在客户端断开连接时自动释放</li><li>lock tables除了会限制别的线程的读写，也会限制本线程接下来的操作对象以及读写</li></ul><p>线程A执行<code>lock tables t1 read ,t2 wrtie</code>，此后其他线程对t1写，t2的读写均会冲突，此外，线程A也只能对t1进行读，t2进行读写，无法对t1,t2进行其他操作，更无法对其他表进行操作</p><p><strong>MDL</strong>：</p><ul><li><p>隐式使用，在访问表时会被自动加上</p></li><li><p>作用：防止DDL和DML之间并发发生冲突</p></li><li><ul><li>进行DDL(CRUD）时，加读锁</li><li>进程DML(变更表结构)时，加写锁</li></ul></li></ul><p>基于以上，对于一张表的CRUD同加上读锁，之间不会互相冲突，而在有表结构改变时，不允许其他线程同样对结构进行改变，也不允许CRUD，</p><p>CRUD之间的逻辑正确性由事务隔离+行锁进行负责，无需MDL</p><p><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E9%94%81%E4%B8%93%E9%A2%98.assets/1657040787931-db461d6f-0420-4348-bf33-a6317ce34db2.webp" alt="img">以上这种情况，A与C之间读写锁冲突，导致C被阻塞，C又将后续的读锁给阻塞</p><p>解决方案</p><ul><li>解决长事务，让事务A尽快结束，释放MDL读锁</li><li>改变等待时间，默认的50s超时时间过长</li></ul><h2 id="行锁"><a href="#行锁" class="headerlink" title="行锁"></a>行锁</h2><p>在引擎层由各个引擎负责实现，如MyISAM即不支持行锁，被InnoDB取代</p><p>innodb加行锁的前提是：必须是通过索引条件来检索数据，否则会切换为表锁。行锁是加在索引上的，比如某个表中id字段是主键，如果给id&#x3D;2这条记录加锁，那这把锁是加在主键索引(聚簇索引)上的。如果为某个没有索引的字段加锁，最终会在主键索引上锁住所有的记录。</p><p>InnoDB中实现的算法</p><ul><li>记录锁(Record Lock) 对单个行记录进行上锁</li><li>间隙锁(Gap Lock) 对两行之间的间隙进行上锁，可以视为开区间，主要用于解决幻读的问题，即单靠Record Lock无法锁住即将新加进来的数据</li><li>Next-Key Lock 等同于记录锁+间隙锁，为左开右闭的区间。</li></ul><p><strong>两段式锁协议</strong></p><p>行锁在需要时加上，只有在事务结束后才释放，因此，最可能造成锁冲突，最影响并发度的语句应当处于一个事务中较后的位置，减少占用锁的时间，获取锁后尽快结束事务</p><h3 id="记录锁"><a href="#记录锁" class="headerlink" title="记录锁"></a>记录锁</h3><p>记录锁如同字面意思，锁住对应的记录行，为标准意义上的行锁</p><p>但是就像上面所说的那样，真正锁住的时这一行的索引，只有涉及到了被锁住的索引的操作才会被阻塞</p><h3 id="间隙锁"><a href="#间隙锁" class="headerlink" title="间隙锁"></a>间隙锁</h3><p>锁住两条记录之间的间隙，如5到10之间的区间(5,10),在此区间内进行插入操作会被阻塞</p><p>同样也是针对某一个索引上锁，较为好理解，由于使用不同索引得到的排序结果也不尽相同，因此该区间也只是针对该索引才适用</p><p>间隙锁是在可重复读隔离级别下才会生效的</p><p><strong>作用</strong>：</p><p>主要用于解决幻读问题，记录锁只能对当前已用的行进行上锁，即便是锁住了所有行，也无法阻止当前不存在而未来要插入的数据，因此提出了间隙锁的概念</p><p>在记录锁的情况下，与锁冲突的为另一把锁，而对于间隙锁，与间隙锁冲突的为向间隙中加锁的这个操作，不同的线程之间可以对同一个间隙进行加锁，目的均为保护这个间隙不允许其插入值，合乎逻辑，但是埋下了死锁的风险</p><p><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E9%94%81%E4%B8%93%E9%A2%98.assets/1657047333289-c47cefbd-66df-44e3-9147-b254faf75da6.webp" alt="img"></p><p>事务A和事务B均为向(5,10)这个间隙上加间隙锁，二者之间不存在冲突问题，但是B的插入被A阻塞，A的插入又被B的间隙锁所阻塞，死锁形成。</p><h3 id="邻键锁"><a href="#邻键锁" class="headerlink" title="邻键锁"></a>邻键锁</h3><p>记录锁和间隙锁合并在一起成为邻键锁，在整个数轴上构成了连续的区间，对于单独的区间，为左开右闭的形式，如(5,10]</p><p>将邻键锁next-key lock作为加锁的基本单位，再某些特定场合下进行优化，令其退化成记录锁和间隙锁</p><h3 id="上锁机制"><a href="#上锁机制" class="headerlink" title="上锁机制"></a>上锁机制</h3><p><strong>上锁规则</strong>：</p><ol><li><p>原则 1：加锁的基本单位是 next-key lock。next-key lock 是前开后闭区间。</p></li><li><p>原则 2：查找过程中访问到的对象才会加锁。访问到的对象指的是在索引树上访问到的记录，而非最终查询的结果，如x&lt;10对于非唯一索引，需要比较到10才会停止搜索，查询得到的结果中并没有10，但是10确是被访问到的，10这一行会被加上锁，之后再可根据优化二进行优化，得到的为一个间隙锁</p></li><li><p>优化 1：索引上的等值查询，给唯一索引加锁的时候，next-key lock 退化为行锁。</p></li><li><p>优化 2：索引上的等值查询，向右遍历时且最后一个值不满足等值条件的时候，next-key lock 退化为间隙锁。</p></li><li><p>一个 bug：唯一索引上的范围查询会访问到不满足条件的第一个值为止。</p></li><li><ol><li>再正常的唯一索引访问过程中，只需要访问到最后一个满足条件的记录即可，</li><li>不满足条件的第一个值，对于此次范围查询并无任何意义</li><li>并且会对要访问不满足条件的第一个值的其他事务造成阻塞</li></ol></li></ol><p>因此可以称为一个bug</p><p><strong>加锁位置</strong></p><p><strong>主要和使用到的索引和锁的种类有关</strong></p><ul><li><strong>使用到的索引指的是在查询过程中真正访问，搜索了的索引，而并非只是作为查询条件的索引，例如在回表过程中锁访问了的主键索引，便也会被一同锁上</strong></li><li><strong>锁的种类指的是共享锁&#x2F;排他锁，即在语句中是指定为for update 还是lock in share mode</strong></li></ul><p>只讨论最基础的等值条件存在的情况，在均基于select *  for update的情况：</p><ul><li>查询条件为主键索引时，在主键索引上进行锁<br><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E9%94%81%E4%B8%93%E9%A2%98.assets/1657045764255-1cad9341-902d-47c2-b5c9-df624162706e.webp" alt="img"></li><li>查询条件为唯一索引时，由于使用的为排他锁for update,故系统认为要对其进行数据的更新，因此防止其他事务通过其他索引找到这行进行数据修改，或者进行读取，在唯一索引和主键上进行加锁，如<code>delete from t where name = &#39;john&#39;</code> <code>select * from t where name =jhon lock in share mode</code>均会产生冲突<br><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E9%94%81%E4%B8%93%E9%A2%98.assets/1657045771946-dc2b03ec-d37d-4798-ac9a-1f3ed1df0d38.webp" alt="img"></li><li>查询条件为非唯一索引时，对非唯一索引加上next-key lock，在主键索引上加上记录锁 </li><li>当查询条件为非索引时，无论是等值查询还是范围查询，无论是否存在查询结果，都会对表中所有的记录加邻键锁，也就是我们常说的“锁表”</li></ul><p>而在使用lock in share mode时<img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E9%94%81%E4%B8%93%E9%A2%98.assets/1657045893582-80d702ef-1c5c-41f6-9a26-f30861810857.png" alt="img"></p><ol><li>事务A查询id，直接使用C覆盖索引，因此并未对主键进行加锁的操作</li><li>事务B通过主键索引进行更新，发现没有锁，更新成功</li></ol><p>若A的查询语句为<code>select * from t where c=5 lock in share mode</code>：</p><ol><li>因为涉及回表操作，故同样对主键索引加上了锁</li><li>此时事务B再进行根据主键索引进行更新时，则会被阻塞</li></ol><p>若A的查询语句为<code>select * from t where c=5 for update</code>：</p><p>​系统会认为你接下来要更新数据，因此会顺便给主键索引上满足条件的行加上行锁，此时事务B也同样会被阻塞，与上述for update的情况分析一致</p><h3 id="共享锁-x2F-排他锁"><a href="#共享锁-x2F-排他锁" class="headerlink" title="共享锁&#x2F;排他锁"></a>共享锁&#x2F;排他锁</h3><p>逻辑关系为读写锁，读写，写写之间存在冲突，多个共享锁之间不存在冲突</p><p>本质上为广义上的行锁，即只在sql中通过for update 和lock in share mode声明共享锁和排他锁，而具体是记录锁，间隙锁，还是邻键锁要按照加锁原则来上具体的锁</p><p>MySQL InnoDB引擎默认的update，delete，insert语句会自动给涉及到的数据加上排他锁，select语句默认不会加任何锁。加排他锁可以使用select …for update语句，加共享锁可以使用select … lock in share mode语句。</p><ul><li><p>共享锁： </p></li><li><ul><li>又称为读锁，多个事务可以对于同一事务共享一把锁，进行数据访问，只能读不能修改</li><li>使用方式 <code>slect * from t lock in share mode</code></li></ul></li><li><p>排他锁： </p></li><li><ul><li>称为写锁，保证对数据的独占性，加上排他锁之后其他事务无法进行读和写的操作</li><li>使用方式 <code>select * from for update</code></li></ul></li></ul><h3 id="理解等值查询"><a href="#理解等值查询" class="headerlink" title="理解等值查询"></a>理解等值查询</h3><p>例：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">select * from t where id&gt;9 and id&lt;12 order by id desc for update;<br></code></pre></td></tr></table></figure><p><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E9%94%81%E4%B8%93%E9%A2%98.assets/ac1aa07860c565b907b32c5f75c4f2bb.png" alt="img"></p><p>由于按照降序排序，因此首先找到12，由于InnoDB索引底层为B+树，因此首先使用<strong>等值查询</strong>找到对应的边界值(非叶子节点），由于表中无12，因此只找到了（10，15）这个间隙，在找到对应的叶子节点，开始遍历搜索(遍历的过程即为区间查询，而非等值查询)</p><p>因此，最终的加锁区间为(0,5],(5,10],(10,15)</p><ul><li>(0,5]对应1个bug：唯一索引上的范围查询会访问到第一个不满足条件的值，即为5，因此加上了(0,5]的锁，但是5实际上由于不满足条件，无需加锁，故称为1个bug，并且已经在MySQL8.0中进行了修复</li><li>(10,15)使用了优化2:(10,15]退化为间隙锁</li></ul><p>因此，<strong>等值查询</strong>与是等号还是不等号并无关系，真正决定等值查询的是InnoDB底层B+树的搜索方式，先等值查询确定位置，在进行遍历筛选或确定范围</p><h2 id="悲观锁-x2F-乐观锁"><a href="#悲观锁-x2F-乐观锁" class="headerlink" title="悲观锁&#x2F;乐观锁"></a>悲观锁&#x2F;乐观锁</h2><p>乐观锁与悲观锁是一种广义上的概念，非真正存在的锁类型其实是对线程同步的不同角度看法。在Java和数据库中都有此概念对应的实际应用。</p><p>悲观锁：对于同一个数据的并发操作，悲观锁认为自己在使用数据的时候一定有别的线程来修改数据，因此在获取数据的时候会先加锁，确保数据不会被别的线程修改。Java中，synchronized关键字和Lock的实现类都是悲观锁。而在MySQL当中，排他锁即为悲观锁</p><p>乐观锁：乐观锁认为自己在使用数据时不会有别的线程修改数据，所以不会添加锁，只是在更新数据的时候去判断之前有没有别的线程更新了这个数据（具体方法可以使用版本号机制和CAS算法）。如果这个数据没有被更新，当前线程将自己修改的数据成功写入。如果数据已经被其他线程更新，则根据不同的实现方式执行不同的操作：重试或者报异常。</p><p>不同线程出现依赖循环，相互等待</p><p>如：事务A B相互等待对方的排他锁<img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E9%94%81%E4%B8%93%E9%A2%98.assets/1657048735918-f0da6176-3d3f-404f-997b-c2d95be6a034.webp" alt="img"></p><p>再如：事务AB被对方的间隙锁所阻塞</p><p><img src="https://github.com/Fischer0522/note/raw/master/mysql45%E8%AE%B2/%E9%94%81%E4%B8%93%E9%A2%98.assets/1657047333289-c47cefbd-66df-44e3-9147-b254faf75da6.webp" alt="img"></p><p><strong>策略</strong></p><ul><li>设置超时时间，等待过长则超时</li></ul><p>问题：过短容易误伤一般事务，过长又等待不起</p><ul><li>主动检测死锁</li></ul><p>innodb_deadlock_detect 设置为on（默认为on），为了减少死锁检测所产生的性能开销：</p><ul><li><ul><li>控制并发度</li><li>确定事务无死锁对其进行关闭</li></ul></li></ul>]]></content>
    
    
    
    <tags>
      
      <tag>数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MySQL45讲</title>
    <link href="/2022/08/08/MySQL45%E8%AE%B2/"/>
    <url>/2022/08/08/MySQL45%E8%AE%B2/</url>
    
    <content type="html"><![CDATA[<h1 id="mysql45讲"><a href="#mysql45讲" class="headerlink" title="mysql45讲"></a>mysql45讲</h1><h2 id="基础篇"><a href="#基础篇" class="headerlink" title="基础篇"></a>基础篇</h2><h3 id="基础架构"><a href="#基础架构" class="headerlink" title="基础架构"></a>基础架构</h3><p><strong>mysql基本架构示意图</strong>：</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/0d2070e8f84c4801adbfa03bda1f98d9.png?raw=true" alt="img"></p><p><strong>连接器</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mys">mysql -h$ip -P$port -u$user -p<br></code></pre></td></tr></table></figure><ul><li>若不指定ip与端口，则默认为localhost的默认端口(3306)</li><li>tcp握手后便进行身份验证，校验用户与密码</li><li>用户密码认证通过后，会从权限表中查询该连接对应的权限，并且，此次连接后，即使管理员修改了该用户的权限，此次连接的权限也不变，建立新的连接后才会使用新的权限设置</li></ul><p><strong>长连接&#x2F;短链接</strong></p><p>长连接为连接成功，客户端持续请求都使用这一个连接</p><p>短链接为执行很少几次的查询断开连接，下次查询重新建立连接</p><p>建议使用长连接，但是长链接过多时会导致资源占用过多而不被释放，最后导致OOM重启</p><p>解决方法：</p><ul><li>定期断开长连接</li><li>mysql_reset_connection重新初始化连接资源(mysql 5.7及以上)</li></ul><p><strong>查询缓存</strong></p><p>mysql将执行过的语句和查询的结果以key-value的形式存储</p><ul><li>查询后若缓存命中则直接返回结果</li><li>每次数据更新导致缓存被清空，因此命中率较低</li><li>适用于静态查询表</li><li>mysql8.0缓存被删除</li></ul><p><strong>分析器</strong></p><p>词法分析-&gt;语法分析：</p><ul><li>词法分析识别出其中的字符串是什么</li><li>语法分析检验语法</li></ul><p><strong>优化器</strong></p><ul><li>多个索引的情况下选择索引的使用</li><li>join的先后顺序</li></ul><p>经过优化器之后，便可以确定执行方案，交给执行器进行执行</p><p><strong>执行器</strong></p><p>首先判断权限，是否有查询该表的权限</p><p>之后根据表的引擎定义，使用这个引擎对应的接口</p><h3 id="日志系统"><a href="#日志系统" class="headerlink" title="日志系统"></a>日志系统</h3><p>与查询不同的是，更新语句还涉及日志模块，分别为redo log 和 bin log</p><p><strong>redo log</strong></p><p>MYSQL中的Write-Ahead Logging(WAL)技术：先写日志，再写磁盘</p><p>更新是InnoDB先将记录写道rodo log中，再空闲时再从redo log中写入到硬盘之中</p><p>与此类似，InnoDB 的 redo log 是固定大小的，比如可以配置为一组 4 个文件，每个文件的大小是 1GB，总共就可以记录 4GB 的操作。从头开始写，写到末尾就又回到开头循环写<strong>四个文件之间循环写入，但是单个文件为随机写入</strong></p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/16a7950217b3f0f4ed02db5db59562a7.png?raw=true" alt="img"></p><p>write pos处写入，check point处擦除，当write pos追上check point时，即为已经写满，需要将一部分写入磁盘， 推进check point</p><p>redo log 分为两部分，一部分为存储在内存中的redo buffer ，另一部分真正写入磁盘之中，如果发生断电，则redo buffer中的丢失，事务回滚</p><p>有了 redo log，InnoDB 就可以保证即使数据库发生异常重启，之前提交的记录都不会丢失，这个能力称为 crash-safe。</p><p><strong>bin log</strong></p><p>最开始 MySQL 里并没有 InnoDB 引擎。MySQL 自带的引擎是 MyISAM，但是 MyISAM 没有 crash-safe 的能力，binlog 日志只能用于归档。</p><p>binlog与redolog的不同：</p><ul><li>redolog InnoDB特有，binlog为mysql server层实现的 任何引擎都可以使用</li><li>redolog为物理日志，记录在某个数据页上做了什么修改，binlog为逻辑日志，记录语句的原始逻辑 （statement格式记录sql语句，row格式记录行的内容）</li><li>redo log循环写，空间大小固定会用完，binlog可切换新的文件进行追加</li><li>备份时即为备份 binlog 用于归档</li></ul><p><strong>执行过程：两阶段提交</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">mysql&gt; update T set c=c+1 where ID=2;<br></code></pre></td></tr></table></figure><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/2e5bff4910ec189fe1ee6e2ecc7b4bbe.png?raw=true" alt="img"></p><p>通过两阶段提交，全部写入成功后再提交事务，保证两份日志的逻辑一致</p><p>若不使用两阶段提交：</p><ul><li><p>先redo log后bin log：crash后redo log进行数据恢复，值被修改，但是备份中的值未被修改</p></li><li><p>先bin log后 redo log：crash后成功备份但是当前值为改变</p></li></ul><h3 id="事务隔离"><a href="#事务隔离" class="headerlink" title="事务隔离"></a>事务隔离</h3><p>事务：ACID 原子性、一致性、隔离性、持久性</p><p>隔离级别：</p><ul><li>读未提交：事务未提交时别的事务就可以看到</li><li>读提交：该事务提交后所作的变更才能被别的事务看到</li><li>可重复读：该事务执行过程中看到的数据一致（同一个视图内的数据一致）在事务启动后，对于其他事务进行的改动不承认，只会去找在事务启动时那一时刻的数据版本，但是，对于本事务的改动，依旧会承认(对于本事务进行的更新，存在当前读的情况，详情见后)</li><li>串行化：对同一条记录进行上锁</li></ul><p>隔离性自上至下依次增强，并行性能依次减弱</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/7dea45932a6b722eb069d2264d0066f8-16546557330432.png?raw=true" alt="img"></p><p>查询结果：</p><ul><li>读未提交：V1:2 V2: 2 V3 2</li><li>读提交：V1:1 V2:2 V3:2</li><li>可重复读：V1:1 V2:1 V3:2(事务A执行过程中读取的数据相同)</li><li>串行化：V1:1 V2: 1 V3: 2(B修改时会被锁到A事务提交完)</li></ul><p><strong>视图</strong></p><p>实现上数据库会创建一个视图，查询结果以视图为准，</p><ul><li><p>可重复读的隔离级别下，视图在事务启动时创建(事务在第一个select时启动，注意区分创建和启动的概念)，整个事务时期都使用这个视图。</p></li><li><p>读提交下，为在SQL语句开始执行时创建，每一条语句执行前都会重新计算出一个新的视图</p></li><li><p>读未提交下，在直接返回记录上的最新值，没有视图概念</p></li><li><p>串行化直接通过加锁的方式</p></li></ul><p><strong>事务隔离的实现</strong></p><p>MYSQL每次更新时同时记录一条回滚操作</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/d9c313809e5ac148fc39feff532f0fee.png?raw=true" alt="img"></p><p>一个值顺序从 1被改成2、3、4，回滚段则为记录逆向操作</p><blockquote><p>在查询这条记录的时候，不同时刻启动的事务会有不同的 read-view。如图中看到的，在视图 A、B、C 里面，这一个记录的值分别是 1、2、4，同一条记录在系统中可以存在多个版本，就是数据库的多版本并发控制（MVCC）。对于 read-view A，要得到 1，就必须将当前值依次执行图中所有的回滚操作得到。</p></blockquote><p><strong>数据库中并不会记录1、2、3这几个值，只会保存回滚日志undo log，想要得到1，只能通过undo log进行一步步的计算得来</strong></p><p>回滚日志的删除：</p><p>​    当系统内没有比当前回滚日志更早的视图时，即为没有事务会再用到该回滚日志，就会删除该回滚日志</p><p>​    如当事务A执行完，视图消失后，对应的回滚日志将2改成1也一并被删除</p><p>尽量避免使用长事务：</p><p>​    该事务被提交前，所有的回滚日志都会被保存，占用大量空间，拖垮数据库</p><p><strong>事务启动方式</strong></p><p>建议使用<code>set autocommit = 1</code>再通过显示语句<code>begin</code>或<code>start transaction</code>进行启动，避免<code>set autocommit = 0</code>长事务</p><h3 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h3><p>作用：提高查询效率</p><p>本质：B+树，每多添加一个索引就多一棵B+树（InnoDB中）</p><p><strong>常见模型</strong></p><ul><li><p>哈希表：适合等值查询，由于其无序性，区间查询则需要整个进行遍历</p></li><li><p>有序数组：等值查询和范围查询都性能优秀，但是插入时移动数组性能损耗大，只适用于静态存储引擎</p></li><li><p>二叉搜索树：查询的时间复杂度为O(log(N))，为了保持树为平衡二叉树，更新复杂度也为O(log(N))</p><ul><li>存在的问题：树过高，在磁盘中一次读取一个子节点，内存与硬盘的交互过程中开销大</li><li>解决方式：查询过程中尽可能少的访问数据块，使用N叉树：一次访问获取多个数据或多个后续节点</li></ul></li></ul><p><strong>InnoDB的索引模型</strong></p><p>B+树，一个索引对应一个B+树</p><p>​    B+树中只有叶子节点存放数据，并且不同叶子节点之间使用链表进行连    接，其他只存放索引</p><p><a href="https://juejin.cn/post/6929833495082565646">MySQL索引底层：B+树详解 - 掘金 (juejin.cn)</a></p><p>设置主键为id，索引为k: </p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs mysql">mysql&gt; create table T(<br>id int primary key, <br>k int not null, <br>name varchar(16),<br>index (k))engine=InnoDB;<br></code></pre></td></tr></table></figure><p>对应的索引树如下</p><ul><li><p>主键索引中存放整行数据</p></li><li><p>非主键索引(二级索引)存放主键的值：查询时需要进行回表（先查询到主键，在通过主键索引进行查询）</p></li></ul><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/dcda101051f28502bd5c4402b292e38d.png?raw=true" alt="img"></p><p><strong>索引维护</strong></p><ul><li>追加插入：直接在叶子节点后进行追加</li><li>中间插入：涉及到数组的移动</li><li>数据页满：叶子的分裂(具体过程看B+树算法)，空间利用率降低50%</li></ul><p>自增主键在插入时直接追加，不涉及数组的移动，并且本身占空间小(int 4字节,bigint8字节), 在性能和存储空间上考虑，自增主键较为合理</p><p>&#x3D;&#x3D;因此大多数情况推荐使用自增主键&#x3D;&#x3D;</p><ol><li><p><strong>覆盖索引</strong>：如果查询条件使用的是普通索引（或是联合索引的最左原则字段），查询结果是联合索引的字段或是主键，不用回表操作，直接返回结果，减少IO磁盘读写读取正行数据 </p></li><li><p><strong>最左前缀</strong>：联合索引的最左 N 个字段，也可以是字符串索引的最左 M 个字符 (通过like进行模糊匹配时) 当存在(a,b)这个索引时，一般不需要再建立单独a的索引         </p></li><li><p><strong>联合索引</strong>：根据创建联合索引的顺序，以最左原则进行where检索，比如（age，name）以age&#x3D;1 或 age&#x3D; 1 and name&#x3D;‘张三’可以使用索引，单以name&#x3D;‘张三’ 不会使用索引，考虑到存储空间的问题，还请根据业务需求，将查找频繁的数据进行靠左创建索引。 </p></li><li><p><strong>索引下推</strong>：like ‘hello%’and age &gt;10 检索，MySQL5.6版本之前，会对匹配的数据进行回表查询。5.6版本后，会先过滤掉age&lt;10的数据，再进行回表查询，减少回表率，提升检索速度</p></li></ol><h3 id="全局锁和表锁"><a href="#全局锁和表锁" class="headerlink" title="全局锁和表锁"></a>全局锁和表锁</h3><p><strong>全局锁</strong></p><p>MYSQL 使用 Flush tables with read lock(FTWRL) 让整个库处于只读的状态，通常用于做全库逻辑备份</p><blockquote><p>官方自带的逻辑备份工具是 mysqldump。当 mysqldump 使用参数–single-transaction 的时候，导数据之前就会启动一个事务，来确保拿到一致性视图。而由于 MVCC 的支持，这个过程中数据是可以正常更新的。对于 MyISAM 这种不支持事务的引擎，如果备份过程中有更新，总是只能取到最新的数据，那么就破坏了备份的一致性。这时，我们就需要使用 FTWRL 命令了。</p></blockquote><p>不建议使用<code>set global readonly = true</code>的方式令全库只读：</p><p><strong>表级锁</strong>：表锁&#x2F;元数据锁</p><p><strong>表锁</strong></p><ul><li>语法<code>lock tables ... read/write</code></li><li>可以用unlock tables主动断开，也可在客户端断开时主动释放</li><li>不仅会限制别的进程的读写，同时本进程的接下来的操作也受限定</li></ul><blockquote><p>举个例子, 如果在某个线程 A 中执行 lock tables t1 read, t2 write; 这个语句，则其他线程写 t1、读写 t2 的语句都会被阻塞。同时，线程 A 在执行 unlock tables 之前，也只能执行读 t1、读写 t2 的操作。连写 t1 都不允许，自然也不能访问其他表。</p></blockquote><p><strong>元数据锁</strong></p><ul><li><p>不显式使用，访问表时会自动加上去</p></li><li><p>防止DDL和DML并发的冲突</p><ul><li>对一个表进行CRUD时，加读锁</li><li>对表结构进行改变时，加写锁</li></ul></li><li><p>事务提交时才会释放</p></li><li><p>分为读锁、写锁   读锁之间不互斥，读锁与写锁，写锁与写锁之间互斥</p><p>故可以有多个线程对同一个表进行CRUD，但是在对表结构进行改变时，不允许别的线程对表结构进行改变以及CRUD</p></li></ul><p>由于在事务提交时才会释放锁，因此要安全的插入字段则需要解决长时间不释放锁的问题：</p><ul><li>解决长事务，防止一直占用MDL锁</li><li>在alter table语句中设置等待时间，过期超时，防止在请求量大的时候阻塞后面的业务语句</li></ul><h3 id="行锁"><a href="#行锁" class="headerlink" title="行锁"></a>行锁</h3><p>&#x3D;&#x3D;存疑&#x3D;&#x3D;</p><ul><li>MySQL 5.7当中如何查看上的锁而非等待锁的事务</li><li>查询字段对上锁是否有影响</li></ul><p>在引擎层由各个引擎负责实现，如MyISAM即不支持行锁，被InnoDB取代</p><p>innodb加行锁的前提是：必须是通过索引条件来检索数据，否则会切换为表锁。行锁是加在索引上的，比如某个表中id字段是主键，如果给id&#x3D;2这条记录加锁，那这把锁是加在主键索引(聚簇索引)上的。如果为某个没有索引的字段加锁，最终会在主键索引上锁住所有的记录。</p><p>两阶段锁协议：行锁在需要时加上，在事务提交时才释放，故在整个事务当中，应当把最可能引起冲突的锁尽可能的往后放，确保其尽快释放，占用时间较短，影响较小</p><p><strong>加锁位置</strong></p><p>&#x3D;&#x3D;有待深入学习&#x3D;&#x3D;</p><p>只讨论最基础的等值条件存在的情况，并且均基于select *  for update的情况</p><ul><li><p>查询条件为主键索引时，在主键索引上进行锁</p><p><img src="/../../../../%E5%BC%80%E5%8F%91/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/44dab2bb11654529b81e1e81d7f2f540tplv-k3u1fbpfcp-zoom-in-crop-mark3024000.webp" alt="img"></p></li><li><p>查询条件为唯一索引时，在唯一索引和主键上进行加锁，防止其他事务通过其他索引找到这行进行数据修改，如<code>delete from t where name = &#39;john&#39;</code></p><p><img src="/../../../../%E5%BC%80%E5%8F%91/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/791b82772bac4081ba1466cafe726efdtplv-k3u1fbpfcp-zoom-in-crop-mark3024000.webp" alt="img"></p></li><li><p>查询条件为非唯一索引时，对非唯一索引加上next-key lock，在主键索引上加上记录锁</p></li><li><p>当查询条件为非索引时，无论是等值查询还是范围查询，无论是否存在查询结果，都会对表中所有的记录加邻键锁，也就是我们常说的“锁表”</p></li></ul><p><strong>死锁和死锁检测</strong></p><p>例：<img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/4d0eeec7b136371b79248a0aed005a52.jpg?raw=true" alt="img"></p><p>其中事务A在等待事务B释放掉id&#x3D;2的行锁，事务B在等待事务A释放掉id&#x3D;1的行锁，故产生了死锁</p><p>应对策略：</p><ul><li>设置超时时间innodb_lock_wait_timeout，默认时间为50s，手动设置时：<ul><li>过短：误伤一般的锁</li><li>过长：影响了等待时间过长无法接受，如默认的50s在生产环境中过于长</li></ul></li><li>死锁检测：innode_deadlock_detece设置为on，默认情况死锁检测即为开启状态,通常也采用这种方式处理死锁<ul><li>问题：检测本身消耗过大：每次新来的被堵住的线程都要检测会不会因为自己而产生了死锁，复杂度为O(n)</li><li>优化：<ul><li>当确定业务一定不会出现死锁时，手动关闭</li><li>控制并发度，令新来的线程不需要进行大量检测排查</li><li>将一行改成逻辑上的多行来减少冲突的情况（每行分别计算再进行汇总）</li></ul></li></ul></li></ul><h3 id="视图、锁与多版本数据"><a href="#视图、锁与多版本数据" class="headerlink" title="视图、锁与多版本数据"></a>视图、锁与多版本数据</h3><p>事务的启动时机：</p><ul><li>使用begin&#x2F;start transaction时，在执行第一个操作innoDB的表时，事务才真正启动</li><li>使用start tracnsaction with consistent snapshot时，执行命令时创建事务</li><li>像update此类语句，本身就是一个事务，在执行时创建，语句完成后自动提交、</li></ul><p><strong>视图</strong>：</p><p>MYSQL中有两个视图的概念</p><ul><li>一个时view，用查询结果定义的一个虚拟表，查询他的方法与查询表一致</li><li>另一个时InnoDB在实现MVCC时使用的一致性视图(consistent read view)，用于支持 RC（Read Committed，读提交）和 RR（Repeatable Read，可重复读）隔离级别的实现。没有物理结构，作用是事务执行期间用来定义“我能看到什么数据”。</li></ul><p><strong>多版本数据</strong></p><p>每行数据存在多个版本，每个事务在创建时，会获取一个自增的transaction id，用于表明事务的创建时间而在对行内数据进行更新时，会将该transaction id赋值给这个数据版本的事务id，记为row trx_id</p><p>数据表中的一行记录，有多个版本，每个版本都用自己的row trx_id，并且可以通过回滚日志计算出上一个版本</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/68d08d277a6f7926a41cc5541d3dfced-16547818091355.png?raw=true" alt="img"></p><blockquote><p>在实现上， InnoDB 为每个事务构造了一个数组，用来保存这个事务启动瞬间，当前正在“活跃”的所有事务 ID。“活跃”指的就是，启动了但还没提交。该视图数组把所有的row trx_id分为了一下几种状态</p><ol><li>如果落在绿色部分，表示这个版本是已提交的事务或者是当前事务自己生成的，这个数据是可见的；</li><li>如果落在红色部分，表示这个版本是由将来启动的事务生成的，是肯定不可见的；</li><li>如果落在黄色部分，那就包括两种情况<ol><li>a. 若 row trx_id 在数组中，表示这个版本是由还没提交的事务生成的，不可见；</li><li>b. 若 row trx_id 不在数组中，表示这个版本是已经提交了的事务生成的，可见。</li></ol></li></ol></blockquote><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/882114aaf55861832b4270d44507695e.png?raw=true" alt="img"></p><p>说明：</p><ol><li>在活跃数组中数值最小的row trx_id称为低水位，最大的称为高水位，对于低水位高水位这种说法，只是其一个限定作用，用于确定已经完成的事务和还未开始的事务，而在低水位和高水位这个范围之内的row trx_id，只有真正存在于活跃数组中的才是未提交的事务</li><li>水位只能表明未提交事务的范围，即低水位只能表明未提交事务中的最小值，比低水位大但是为一个短事务，在低水位之前进行提交，那么他便不存在与活跃数组当中</li><li>对于已经提交的事务，自然能够获取到他的值，同样未开始的事务无法获取，如果当前的版本的row trx_id的只位于红色区域，则需要通过undo log寻找到一个小于高水位并且不存在于活跃数组中的row trx_id，此时才为能够获取到的真正的值</li><li>对于在未提交事务中的修改，只有当前线程进行了写操作后（在未提交事务的版本后进行追加，否则未提交事务的版本就会丢失），再次进行读取才能获取到基于未提交事务的值（获取的为当前线程的数据版本），即更新数据时都先读后写，读当前最新版本值，这种特性称之为<strong>当前读</strong></li></ol><blockquote><p>一个数据版本，对于一个事务视图来说，除了自己的更新总是可见以外，有三种情况：</p><ol><li>版本未提交，不可见；</li><li>版本已提交，但是是在视图创建后提交的，不可见；</li><li>版本已提交，而且是在视图创建前提交的，可见。</li></ol></blockquote><p><strong>两阶段锁协议</strong></p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/cda2a0d7decb61e59dddc83ac51efb6e.png?raw=true" alt="img"></p><p>事务B的更新数据(当前读)操作在C’之前受到阻塞，需要等待事务C的提交释放掉占用的行锁</p><blockquote><p>可重复读的核心就是一致性读（consistent read）；而事务更新数据的时候，只能用当前读。如果当前的记录的行锁被其他事务占用的话，就需要进入锁等待。</p></blockquote><p><strong>共享锁&#x2F;排他锁</strong></p><p>加上共享锁和排他锁之后，单独的select也可以变成当前读的状态，从而获取到最新的数据</p><h2 id="实践篇"><a href="#实践篇" class="headerlink" title="实践篇"></a>实践篇</h2><h3 id="普通索引与唯一索引"><a href="#普通索引与唯一索引" class="headerlink" title="普通索引与唯一索引"></a>普通索引与唯一索引</h3><p>在查询过程当中，唯一索引遇到条件满足时则立即停止搜索，而普通索引需要寻找到第一个不满足条件的数据，在查询性能上几乎没有差距</p><p><strong>change buffer</strong></p><p>作用：无change buffer 时，要进行数据的修改需要将对应的数据页读到内存当中，修改后再重新写回内存，加入change buffer后避免了这个过程，减少了磁盘的随机访问，提高执行速度</p><p>在更新数据页时，如果该数据页位于内存当中，则直接进行更新，如果该数据页不存在与内存中，则先写入change buffer，等待下一次查询将数据页调入到内存当中时，再将操作应用到数据页中，这个过程称为merge</p><p>触发merge：</p><ul><li>访问数据页将数据页调入到内存当中</li><li>系统后台线程定期merge</li><li>数据库正常关闭(shutdown)</li></ul><p>merge的具体流程：</p><ol><li>将数据页从磁盘中读入到内存当中</li><li>从change buffer中找到这个数据页对应的记录，应用得到新的数据页</li><li>将数据变更和change buffer变更(删除记录)的过程写入到redo log当中</li><li>修改数据后的脏页写会到内存当中(另外一个故事，flush)</li></ol><p>作用范围：只有在普通索引(二级索引)时才能使用(唯一索引在插入时需要重复性判断，必须将数据页调入到内存当中)</p><p>当要更新的记录不存在与内存当中：</p><ul><li>唯一索引：将存放索引的数据页(索引的B+树)调入到内存中，判断是否存在冲突，进行插入，结束</li><li>普通索引：将更新记录写入到change buffer中，结束</li></ul><p>使用场景：写多读少的业务，便于多次写入change buffer后再进行一次merge，与磁盘访问次数少</p><p>存储位置：change buffer是可以持久化的数据。在内存中有拷贝，也会被写入到磁盘上，在磁盘上占据了系统表空间ibdata</p><p>update语句需要根据条件更新，具体执行流程如下：</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/image-20220610002347123.png?raw=true" alt="image-20220610002347123"></p><p><strong>change buffer&#x2F;redo log</strong></p><ul><li><p>对于一条更新数据，数据页存在于内存当中直接写入，若不在则写入change buffer</p></li><li><p>而redo log 对于上述两种动作进行记录，再根据这些记录，对磁盘进行更新，即redolog有两种，一种记录普通数据页的改动，一种记录changebuffer的改动</p></li><li><p>真正对磁盘数据页的修改是通过将内存里脏页的数据刷回磁盘来完成的，而不是根据redolog 真正刷新磁盘的工作为innodb中的存储引擎中的线程去做</p></li><li><p>change buffer 和redo log 二者的作用目标不同，关注的为两个事情</p><ul><li>redo log作为一个日志系统，最大的作用为再数据库宕机后的恢复工作</li><li>change buffer 的目的是为了减少磁盘IO开销</li></ul></li></ul><h3 id="优化器索引选择"><a href="#优化器索引选择" class="headerlink" title="优化器索引选择"></a>优化器索引选择</h3><p><strong>优化器的逻辑</strong></p><p>索引的选择为优化器的工作，在绝大多数情况下，优化器都能够正确选择索引</p><p>判断标准：扫描的行数，使用临时表，是否排序，同时还有回表所带来的代价</p><p>扫描行数：通过基数进行估算</p><p><strong>基数</strong></p><ul><li>概念：一个索引上不同的值的个数</li><li>基数越大，索引的区分度越好</li><li>计算方式：抽样统计，选取N个数据页，统计页面上的不同值，最后平均，当变更的行数超过了1&#x2F;M后，会重新进行一次统计<ul><li>统计方式：通过设置参数 innodb_stats_persistent 的值来选择<ul><li>设置为 on 的时候，表示统计信息会持久化存储。这时，默认的 N 是 20，M 是 10。</li><li>设置为 off 的时候，表示统计信息只存储在内存中。这时，默认的 N 是 8，M 是 16。</li></ul></li></ul></li></ul><p>对于以下语句：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">explain select * from t where (a between 1 and 1000) and (b between 50000 and 100000) order by b limit 1;<br></code></pre></td></tr></table></figure><ul><li><p>扫描a索引需要1000行然后再进行回表</p></li><li><p>扫描b索引需要50000行然后再进行回表</p></li></ul><p>然而最后优化器依然选择了b索引</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/image-20220610172752905.png?raw=true" alt="image-20220610172752905"></p><p>解决方案：</p><ul><li>force index强制选用a索引</li><li>优化器之所以选择b索引是因为b索引本身有序，如果使用b则可以避免排序的过程，因此，可以修改sql语句去引导优化器：把“order by b limit 1” 改成 “order by b,a limit 1，逻辑上一致</li></ul><h3 id="字符串字段索引"><a href="#字符串字段索引" class="headerlink" title="字符串字段索引"></a>字符串字段索引</h3><p>由于MYSQL支持前缀索引，故可以选择字符串的一部分来创建索引</p><ul><li>优点：索引占用的空间更小</li><li>缺点：<ul><li>增加匹配次数（前缀部分相同，需要到主键索引上获取完整信息，多次比较）</li><li>无法使用索引覆盖</li></ul></li></ul><p>使用前缀索引时应关注区分度，确定一个可以接受的损失比例，适当截取</p><p><strong>其他处理方式</strong></p><ul><li><p>如身份证，学号等前几位高度重合的字段，可以选择使用倒叙存储，则查询时：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">mysql&gt; select field_list from t where id_card = reverse(&#x27;input_id_card_string&#x27;);<br></code></pre></td></tr></table></figure></li><li><p>hash字段：在表上再创建一个整数字段，来保存身份证的校验码，同时在这个字段上创建索引。此时只需要四个字节</p></li><li><p>数字型</p></li></ul><h3 id="脏页回写"><a href="#脏页回写" class="headerlink" title="脏页回写"></a>脏页回写</h3><p>当数据页在内存中经过修改，和硬盘中数据不一样时，称为脏页，需要将脏页会写回硬盘当中，这个过程称为刷脏页(flush)</p><p>引发刷脏页的时机：</p><ul><li>redo log写满，需要推进checkpoint，将写在redo log中的操作应用到硬盘之上，将推进部分对应的所有脏页刷到磁盘上</li><li>系统内存不足，进行淘汰页时，如果淘汰的为脏页，则进行flush，但是该过程中不会删除redo log中的记录，redo log在向硬盘中写入时，如果一个数据页被刷过了，则直接跳过<ul><li>保证了数据页只有两种情况，如果存在于内存之中，那么内存之中的即为正确数据，如果不存在于内存当中，从硬盘中读取的数据页也为正确数据，均可以直接返回，无需再重新应用redo log中记录的操作</li><li>如果不是在淘汰脏页时就进行flush，而是选择直接覆盖的话，那么还需要将redo log中记录的操作重新应用于内存中的数据页才能保证数据的正确性，多此一举，不如淘汰时直接进行flush</li></ul></li><li>MYSQL认为系统空闲时</li><li>MYSQL关闭时</li></ul><p>明显影响性能：</p><ul><li>redo log被写满时，整个系统的更新被阻塞</li><li>一次查询涉及要淘汰的脏页数量过多，查询时间变慢</li></ul><p><strong>刷脏页控制策略</strong></p><p>通过设置innodb_io_capacity的值，告诉innodb的磁盘读写能力，从而合理设置速度</p><p>刷脏页速度的参考因素：</p><ul><li>脏页比例通过公式计算得来F1</li><li>redo log写盘速度 计算得来F2</li></ul><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/cc44c1d080141aa50df6a91067475374.png?raw=true" alt="img"></p><blockquote><p>一旦一个查询请求需要在执行过程中先 flush 掉一个脏页时，这个查询就可能要比平时慢了。而 MySQL 中的一个机制，可能让你的查询会更慢：在准备刷一个脏页的时候，如果这个数据页旁边的数据页刚好是脏页，就会把这个“邻居”也带着一起刷掉；而且这个把“邻居”拖下水的逻辑还可以继续蔓延，也就是对于每个邻居数据页，如果跟它相邻的数据页也还是脏页的话，也会被放到一起刷。</p><p>在 InnoDB 中，innodb_flush_neighbors 参数就是用来控制这个行为的，值为 1 的时候会有上述的“连坐”机制，值为 0 时表示不找邻居，自己刷自己的。</p><p>使用SSD时建议设置为0关闭连坐机制</p></blockquote><h3 id="数据库表空间"><a href="#数据库表空间" class="headerlink" title="数据库表空间"></a>数据库表空间</h3><p>表数据可以放在共享表空间，也可每一个表单独一个文件，通过innodb_file_per_table进行设置，MYSQL 5.6 之后，默认为单独存储(on) </p><p>在删除一个数据&#x2F;数据页时，只是将该处标记为可复用，而不是将空间进行回收，因此即使删除了数据，所占的空间也不会变小</p><p>而对于插入&#x2F;更新的操作，由于B+树算法所涉及的叶子的分裂的问题，原本叶数据页满时，插入会分类成两个新的叶子节点，但每个叶子节点所占的空间仍为整个数据页的大小，末尾存在未分配的空间，更新操作视为先删除后插入</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/8083f05a4a4c0372833a6e01d5a8e6ea.png?raw=true" alt="img"></p><p><strong>重建表</strong></p><p>为了进行空间收缩，只能选择去进行重建表，B作为临时表，从A中按照主键顺序一条条读取数据插入到B中，再使用B替换A，此时原本未分配的空间便不在存在</p><ul><li>问题：在A向B中复制数据的过程中，A不能有新的数据插入</li><li>解决方式：引入online DDL</li></ul><p><strong>online DDL</strong></p><ul><li>将A中的记录生成为B+树，保存到临时文件之中</li><li>在复制期间的对A的操作保存到一个日志文件之中，临时文件生成之后再将日志应用到临时文件之中</li><li>alter语句启动时会获取MDL(元数据锁)写锁,但是在此拷贝数据前会退化成读锁：<ul><li>读锁不会阻塞CRUD，因此保证了Online</li><li>读写锁之间会相互阻塞，别的线程想要DDL时获取写锁被阻塞，防止了别的线程对该DDL</li></ul></li></ul><p><strong>online&#x2F;inplace</strong></p><ul><li><p>在非online 重建表时是在server层创建了一个<strong>临时表</strong>，将A中的数据一条条的插入其中</p></li><li><p>而online DDL时，在innoDB内部创建一个<strong>临时文件</strong>，整个过程发生在innoDB内部，在innodb内部，表A对应的文件进行了替换而已，在server层看来，并不存在一个创建临时表的过程，始终都只有A这一张表像是一个”原地”的操作 ，称为inplace</p></li><li><p>online的一定是inplace的，但inplace的操作并非全为online的</p></li></ul><h3 id="count"><a href="#count" class="headerlink" title="count"></a>count</h3><ul><li><p>innoDB之中，每次使用count是都需要遍历统计数量，因此在记录多时速度会变慢</p></li><li><p>MyISAM会统计行的总数，但也只针对一无条件的统计时才能快速获取结果，对于附带过滤条件的情况，依旧需要遍历，并且不支持事务</p></li><li><p>show table statues返回结果很快，但获取的数据存在误差，无法替代count</p></li></ul><p><strong>改进方案</strong></p><ul><li>引入缓存，通过redis对其进行计数<ul><li>问题：<ul><li>redis异常重启时丢失更新</li><li>计数和添加数据无法保证原子性，查询可能位于二者之间</li></ul></li></ul></li><li>单独添加一张计数表：由于innodb中的事务隔离的特性，还未提交的事务对其不可见</li></ul><p><strong>各类count</strong></p><ul><li>count(主键id) 遍历整张表取出id返回给server层，判断不能为空之后，按行累加</li><li>count(字段),取出字段，判断是否为null，若不为null，则累加</li><li>count(1)，遍历不取值，server 层对于返回的每一行，放一个数字“1”进去，判断是不可能为空的，按行累加。</li><li>count(*) 由于 count(*)一定不为null 对其进行了专门优化，只统计按行取值，并不将值取出</li></ul><p>按照效率排序的话，count(字段)&lt;count(主键 id)&lt;count(1)≈count(*),建议使用count(*)</p><h3 id="order-by"><a href="#order-by" class="headerlink" title="order by"></a>order by</h3><p>sort_buffer：mysql为每个线程分配一块内存用于进行排序，存储排序的字段等</p><p><strong>全字段排序</strong></p><p>将要查询的所有字段放入到sort_buffer之中，排序后将结果返回给客户端</p><p>如:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">select city,name,age from t where city=&#x27;杭州&#x27; order by name limit 1000  ;<br></code></pre></td></tr></table></figure><p>通过city的索引找到所有满足city等于’杭州’的行，将行上的city name age字段放入的sort_buffer之中，进行快排</p><blockquote><p>sort_buffer_size，就是 MySQL 为排序开辟的内存（sort_buffer）的大小。如果要排序的数据量小于 sort_buffer_size，排序就在内存中完成。但如果排序数据量太大，内存放不下，则不得不利用磁盘临时文件辅助排序。</p><p>number_of_tmp_files 表示的是，排序过程中使用的临时文件数，通过对多个文件进行排序，再讲多个有序文件合并成一个文件(归并？)</p></blockquote><p><strong>rowid排序</strong></p><p>由于全字段排序需要将多个字段放入到sorr_buffer当中,如果要查询的字段较多开销太大，因此提出了rowid排序方式</p><ul><li>只将要排序的那一个字段和主键id放入到sort_buffer当中</li><li>排序后再通过回表的方式获取到完整数据</li><li>max_length_for_sort_data：如果单行的长度超过这个值，MySQL 就认为单行太大，开始使用rowid排序</li></ul><p><strong>总结比较</strong></p><p>全字段排序和rowid排序的选择取决于内存的大小</p><p>mysql的理念是如果内存够用就尽量使用内存，减少磁盘的访问，从而提高效率</p><p>如果排序字段为索引，由于索引本身有序，则无需排序直接返回给客户端，可以看到无use fliesort，只剩use index</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/image-20220615210049646.png?raw=true" alt="image-20220615210049646"></p><p>rowid在排序时相比于全字段排序多了一次回表获取完整信息的过程，属于是以时间换空间</p><p>对于 InnoDB 表来说，rowid 排序会要求回表多造成磁盘读，因此不会被优先选择。</p><h3 id="显示随机消息"><a href="#显示随机消息" class="headerlink" title="显示随机消息"></a>显示随机消息</h3><p>在数据表中获取随机的行：order by rand() limit </p><p>oreder by rand()大致流程：</p><ol><li>创建一张内存临时表，表的字段为要查询的字段和一个0到1的随机小数R(用于随机获取)，扫描填充内存临时表(该内存临时表无索引)</li><li>初始化sort_buffer 字段为一个double类型用于存放R进行排序，另外一个为一个整形(存放位置信息)</li><li>将内存临时表中的R和<strong>位置信息</strong>放入到sort_buffer当中</li><li>在sort_buffer中对R进行排序</li><li>取出前三个去临时数据表当中获取要查询的所有字段，返回给客户端</li></ol><p><strong>位置信息</strong></p><p>表明一行数据的位置的信息</p><p>一个表在没有主键时，innoDB会自动创建一个rowid作为主键，故对于位置信息：</p><ul><li>在有主键时，位置信息即为主键</li><li>无主键时，即为系统生成的rowid</li></ul><p><strong>磁盘临时表</strong></p><p>在临时表过大时，使用磁盘临时表而不是内存临时表，由于获取随机数时，大多数情况只需要几个元素，故对所有元素全部进行排序便造成了浪费，因此mysql5.6之后，对于磁盘临时表的情况，采用了优先队列排序(堆排序)</p><p>如果超出了sort_buffer_size大小之后，仍会选择临时文件+归并排序的方法</p><h3 id="索引未被使用"><a href="#索引未被使用" class="headerlink" title="索引未被使用"></a>索引未被使用</h3><p><strong>对索引字段使用了函数</strong></p><p>如果对于在进行条件筛选时，对索引字段进行了函数统计，则用不上索引了，索引能够快速查询时依托于索引的搜索树结构，而如果使用了函数，对索引本身的值进行了改变，破坏了索引的有序性，因此无法在通过搜索树快速定位，只能遍历该索引</p><p>注：索引未被只用指的是索引的搜索树为生效，只能通过遍历的方式确定位置</p><p>例：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">mysql&gt; select count(*) from tradelog where month(t_modified)=7;<br></code></pre></td></tr></table></figure><p>取月份之后传入一个7无法在搜索树内判断大小</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/3e30d9a5e67f711f5af2e2599e800286.png?raw=true" alt="img"></p><p><strong>隐式转换类型</strong></p><p>如果索引字段的数据类型和条件不同时，会触发隐式条件转换，</p><p>在mysql中，字符串和数字做比较的话，是将字符串转换成数字：</p><ul><li><p>索引为字符串，条件为整形，将索引转换为字符串，使用了函数，故不走搜索树</p></li><li><p>索引为整形，而判断条件为字符串，此时对判断条件进行类型转换，索引不受影响，继续走搜索树</p></li></ul><p><strong>隐式字符编码转换</strong></p><p>字符编码不同时，会转换成超集的编码，如utf-8和utf8bm4之间，会转换成utf8mb4</p><p>在两个表使用的字符集不同时，如果索引字段为utf-8而条件为utf8bm4，则会触发转换，使用函数</p><p>在无法对表结构进行修改时，可以主动对条件进行转换，转换成u8和索引一致，就避免了索引进行转换的情况</p><p><strong>隐式类型转换和字符编码转换本质上都是因为触发使用了函数</strong></p><h3 id="查询等待"><a href="#查询等待" class="headerlink" title="查询等待"></a>查询等待</h3><p><strong>查询阻塞</strong></p><ul><li>等待MDL锁：查询时需获取读锁，MDL读写锁之间冲突</li><li>等flush：进行flush脏页回写时，需要关闭当前表，而如果当前表有一个很慢的事务未提交，则被阻塞</li><li>等行锁：<code>select * from t where id = 1 lock in share mode</code>读取时需要加行锁，需其他线程的读锁冲突，等待其他线程</li></ul><p><strong>查询慢</strong></p><ul><li>查询多行但是不走索引</li><li>事务A查询<code>select * from t where id =1</code>，只查询一行，在事务开始到最后查询之间，其他线程在不断对该行进行修改，如果事务A使用的为可重复读，则需要根据undo log去不断向前计算出在事务启动时那一时刻的值，耗时巨大，相反的，如果A使用了当前读<code>select * from t where id = lock in share mode</code>读取当前值，则没有回退计算的过程，查询效率反而高（在使用共享锁之后select会采用当前读的模式）</li></ul><h3 id="幻读"><a href="#幻读" class="headerlink" title="幻读"></a>幻读</h3><blockquote><p>也就是说，幻读指的是一个事务在前后两次查询同一个范围的时候，后一次查询看到了前一次查询没有看到的行</p></blockquote><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/dcea7845ff0bdbee2622bf3c67d31d92.png?raw=true" alt="img"></p><ul><li>在上图中，由于<code>for update</code>将其加上了排他锁，采用的为当前读，因此会将insert的新值读出，称为当前读</li><li>幻读只会在当前读下才会出现(无当前读由于存在事务隔离因此不会读取到最新的值)</li><li>幻读专指新插入的行</li></ul><p><strong>幻读产生的问题</strong></p><ul><li><p>锁结果被破坏</p><p>如果只对访问的数据进行加锁，如在上面的表上，由于只有id&#x3D;5着一行的d&#x3D;5，因此只锁上了这一行，但是在C插入数据之后，d&#x3D;5的行就不止id&#x3D;5这一行，“锁住d&#x3D;5的所有行”这个锁的结构就被破坏</p></li><li><p>数据库中的数据与日志中的数据产生不一致的问题</p><ul><li>在数据库中按照时间的发生顺序执行，只有一行当前状态下d&#x3D;5的会被修改为100</li><li>在日志中，按照事务的提交顺序进行记录，事务C的插入在A的更新之前，导致A把C新插入的数据也一并修改，导致了数据不一致的问题</li></ul><p>数据不一致的问题无论是锁满足条件的行还是锁住所有行均无法解决，究其本质时因为新插入的数据无法锁上，从而引出的间隙锁的概念</p></li></ul><p><strong>间隙锁</strong></p><p>只在可重复读的隔离条件下产生          </p><p>通过间隙锁，保证新记录的插入被阻塞，在此区间内插入数据均会被阻塞</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/e7f7ca0d3dab2f48c588d714ee3ac861.png?raw=true" alt="img"></p><p>间隙锁与间隙锁之间不存在冲突问题，不同的线程可以同时对同一个间隙进行加锁操作，只有加间隙锁和想间隙中插入数据这两个操作之间存在冲突，插入数据的过程会被阻塞</p><blockquote><p>间隙锁和行锁合称 <strong>next-key lock</strong>，每个 next-key lock 区间为<strong>左开右闭</strong>。也就是说，我们的表 t 初始化以后，如果用 select * from t for update 要把整个表所有记录锁起来，就形成了 7 个 next-key lock，分别是 (-∞,0]、(0,5]、(5,10]、(10,15]、(15,20]、(20, 25]、(25, +supremum]。</p></blockquote><p><strong>间隙锁和next-key lock存在的问题</strong></p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/df37bf0bb9f85ea59f0540e24eb6bcbe.png?raw=true" alt="img"></p><p>A和B在扫描到id &#x3D; 9时发现不存在，均加上了间隙锁，此时AB的间隙锁互相阻止对方插入，两阶段锁协议又导致事务不提交锁不释放，死锁产生</p><h3 id="行锁与间隙锁加锁规则"><a href="#行锁与间隙锁加锁规则" class="headerlink" title="行锁与间隙锁加锁规则"></a>行锁与间隙锁加锁规则</h3><p>间隙锁并非行锁，只是对于某一个索引的排序规则下的间隙进行上锁，况且，由于不同索引的排序也不同，因此也无法笼统的将两行之间进行上锁，只能基于某一个索引的排序规则。因此在对一个索引上了间隙锁之后，对于另外一个索引的操作并不受影响。</p><p><strong>两个原则、两个优化、一个bug</strong></p><blockquote><p>原则 1：加锁的基本单位是 next-key lock。next-key lock 是前开后闭区间。</p><p>原则 2：查找过程中访问到的对象才会加锁。</p><p>优化 1：索引上的等值查询，给唯一索引加锁的时候，next-key lock 退化为行锁。</p><p>优化 2：索引上的等值查询，向右遍历时且最后一个值不满足等值条件的时候，next-key lock 退化为间隙锁。</p><p>一个 bug：唯一索引上的范围查询会访问到不满足条件的第一个值为止。</p></blockquote><p><strong>解释</strong></p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/e7f7ca0d3dab2f48c588d714ee3ac861.png?raw=true" alt="img"></p><p>对于原则二，假设表中c为普通索引，在执行<code>select c from t where c =15</code>：</p><ul><li>访问到了c&#x3D;15，加上next-key lock,区间为(10,15]</li><li>并且由于c为普通索引，因此会继续遍历直至第一个不满足条件的值，因此找到了(15,20],又因为为最后一个不满足条件的值，此时退化成(15,20)的间隙锁</li><li><strong>只有访问到的对象才会加锁</strong>查询的字段只有C，因此存在覆盖索引，无需访问主键索引，因此在主键索引上就不存在锁</li></ul><p>bug问题，对于一个范围查询<code>select * from where id&gt;10 and id&lt;=15 for update</code>：</p><ul><li>由于是范围查询，因此在最后一个不满足条件的20并不会退化为间隙锁，导致对于20的修改也无法进行，但是对20修改并不会产生什么负面影响</li></ul><p>limit加锁问题：</p><p>​    通过limit，可以提前终止遍历过程，无需遇到第一个不满足条件的值，因此可以少加一个next-key lock，增加并发度</p><p>两阶段加锁：</p><p>​    next-key lock的加锁过程分为间隙锁和行锁两阶段来执行</p><p><strong>共享锁&#x2F;排他锁</strong></p><p>共享锁和排他锁的逻辑关系与表锁中的读写锁一致，只不过粒度不同，为行锁</p><p>读写，写写之间存在冲突，多个共享锁之间不存在冲突</p><p>MySQL InnoDB引擎默认的update，delete，insert语句会自动给涉及到的数据加上排他锁，select语句默认不会加任何锁。加排他锁可以使用select …for update语句，加共享锁可以使用select … lock in share mode语句。</p><ul><li>共享锁：<ul><li>又称为读锁，多个事务可以对于同一事务共享一把锁，进行数据访问，只能读不能修改</li><li>使用方式 <code>slect * from t lock in share mode</code></li></ul></li><li>排他锁：<ul><li>称为写锁，保证对数据的独占性，加上排他锁之后其他事务无法进行读和写的操作</li><li>使用方式 <code>select * from for update</code></li></ul></li></ul><p><strong>案例分析</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs mysql">CREATE TABLE `t` (<br>  `id` int(11) NOT NULL,<br>  `c` int(11) DEFAULT NULL,<br>  `d` int(11) DEFAULT NULL,<br>  PRIMARY KEY (`id`),<br>  KEY `c` (`c`)<br>) ENGINE=InnoDB;<br><br>insert into t values(0,0,0),(5,5,5),<br>(10,10,10),(15,15,15),(20,20,20),(25,25,25);<br></code></pre></td></tr></table></figure><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/465990fe8f6b418ca3f9992bd1bb5465.png?raw=true" alt="img"></p><ol><li>事务A查询id，直接使用C覆盖索引，因此并未对主键进行加锁的操作</li><li>事务B通过主键索引进行更新，发现没有锁，更新成功</li></ol><p>若A的查询语句为<code>select * from t where c=5 lock in share mode</code>：</p><ol><li>因为涉及回表操作，故同样对主键索引加上了锁</li><li>此时事务B再进行根据主键索引进行更新时，则会被阻塞</li></ol><p>若A的查询语句为<code>select * from t where c=5 for update</code>：</p><p>​    系统会认为你接下来要更新数据，因此会顺便给主键索引上满足条件的行加上行锁，此时事务B也同样会被阻塞</p><h3 id="日志流程"><a href="#日志流程" class="headerlink" title="日志流程"></a>日志流程</h3><p><strong>binlog写入流程</strong></p><p> binlog首先写入binlog cache，每个线程独有一个binlog cache，所有线程共享一份binlog文件</p><p>写入binlog文件的过程分为write和fsync，write只写入到page cache(binlog files)中，fsync才是真正对数据进行持久化，可以通过sync_binlog来指定积累几个事务再进行一次持久化(page cache为页缓存，存在于内存当中,具体知识见操作系统)</p><p>在主机发生异常重启时，未fsync持久化的binlog会丢失，mysql重启时则不会</p><p><strong>redo log 写入机制</strong></p><p>redo log也存在redo log buffer,page cache ,和真正持久化这三种状态，可通过innodb_flush_log_at_trx_commit来控制是留在redo log buffer内还是直接持久化还是写入到page buffer当中去</p><blockquote><p>事务执行中间过程的 redo log 也是直接写在 redo log buffer 中的，这些 redo log 也会被后台线程一起持久化到磁盘。也就是说，一个没有提交的事务的 redo log，也是可能已经持久化到磁盘的。</p></blockquote><p>redo log共有三种方式将redo log buffer中的日志信息(包含未提交事务)写到磁盘当中</p><ul><li>redo log buffer后台有一个线程，每一秒将redo log buffer 中的信息write后fsync</li><li>redo log buffer 占用的空间即将达到 innodb_log_buffer_size 一半的时候，后台线程会主动写盘</li><li>并行的事务提交的时候，顺带将这个事务的 redo log buffer 持久化到磁盘，事务A执行了一半写到buffer中的redo log会随着事务B的提交一并写到磁盘当中(innodb_flush_log_at_trx_commit 设置为1)</li></ul><blockquote><p>每秒一次后台轮询刷盘，再加上崩溃恢复这个逻辑，InnoDB 就认为 redo log 在 commit 的时候就不需要 fsync 了，只会 write 到文件系统的 page cache 中就够了。</p><p>redo log 状态改为commit的时候不会进行fsync，因为只要binlog 写磁盘成功，就算redo log 的状态还是prepare也没有关系会被认为事务已经执行成功，所以只需要write 到page cache就ok了，没必要再浪费io主动去进行一次fsync。通过后台线程进行fsync即可</p></blockquote><p><strong>组提交</strong></p><p>LSN：日志逻辑序列号，单调递增，每次写入新的长为lentgh的redo log 都会加length</p><p>第一个到达的会作为这组的leader，后续还有其他事务到达时，它的LSN会递增当它进行提交时，会将所有LSN小于等于leaderLSN的redo log一同持久化到磁盘，以组的形式进行提交</p><h3 id="主备一致"><a href="#主备一致" class="headerlink" title="主备一致"></a>主备一致</h3><p>MySQL通过binlog的归档功能完成了主库从库的一致性</p><p>分为MS和双M两种模式</p><p>在MS(master-slave)模式下，A为M，B为S，则B获取A的binlog进行备份</p><p>备库B建议设为只读状态，防止误操作和双写时导致的数据不一致问题，用于AB之间同步更新的线程拥有超级权限，不受其影响</p><p><strong>binlog的三种格式</strong></p><ul><li>statement<ul><li>binlog中记录的为sql语句</li><li>主从可能存在不一致的情况(如使用limit时优化器索引选择的不一致)</li></ul></li><li>row<ul><li>记录的为事件(Delete_rows event，用于定义删除的行为)</li><li>删除时记录的为真实删除行的主键id，因此不存在数据不一致的情况</li><li>相比于statement形式下，占用的空间较大</li></ul></li><li>mixed<ul><li>由mysql自己判断是否存在主从不一致的情况，从而选择row还是statement</li></ul></li></ul><p><strong>数据恢复</strong></p><p>使用row格式的binlog完成数据恢复，由于row格式会完整的保存信息，对于一条delete，只需要将delete重新变为insert即可将信息恢复，(update可前后对调，insert变为delete)</p><p><strong>循环复制问题</strong></p><p>在使用双M模式时，存在循环复制问题，即B获取了A的新生成的binlog执行，A又会获取B执行了binlog后的binlog重新执行一遍，导致了循环复制</p><p>解决规则：</p><blockquote><ol><li>规定两个库的 server id 必须不同，如果相同，则它们之间不能设定为主备关系；</li><li>一个备库接到 binlog 并在重放的过程中，生成与原 binlog 的 server id 相同的新的 binlog；</li><li>每个库在收到从自己的主库发过来的日志后，先判断 server id，如果跟自己的相同，表示这个日志是自己生成的，就直接丢弃这个日志。</li></ol></blockquote><h3 id="备库并行复制"><a href="#备库并行复制" class="headerlink" title="备库并行复制"></a>备库并行复制</h3><p>为了保证备库能对主库的数据进行快速备份，因此在MySQL5.6之后，支持多线程复制，通过多线程的方式将主库的日志在备库上执行</p><p>原本的sql_thread变为coordinator，只负责读取中转日志和分发事务，事务的具体执行由worker来负责</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/bcf75aa3b0f496699fd7885426bc6245.png?raw=true" alt="img"></p><p>规则：</p><ul><li>更新同一行的两个事务，必须分发给同一个worker，防止执行次序问题导致覆盖</li><li>一个事务不可拆分，必须放到同一个worker当中</li></ul><p><strong>MySQL5.6的并行策略</strong></p><p>MySQL5.6的并行复制的粒度为按库并行，即以库为单位分发给worker，通过key-value的标记当前这个worker中事务所涉及的库，key为库名，value为涉及的库的数量</p><p>但如果主库中的表都放在同一个库内，这个策略则失效，重新变为单线程模式，或者不同库的热点不同，一个为业务逻辑库，一个为配置库，则只有业务逻辑库存在较大的并发，重回单线程</p><h3 id="误删数据"><a href="#误删数据" class="headerlink" title="误删数据"></a>误删数据</h3><p><strong>误删行</strong></p><p>在binlog为row形式时，可以直接将操作进行逆向运行，delete变insert，各个事务之间按时间逆序执行，即可恢复</p><p>建议在临时库上进行操作，然后再将恢复后确认过的数据再从临时库恢复回主库</p><p><strong>误删库&#x2F;表</strong></p><p>通过备份+binlog的方式</p><p>需要借助binlog的全盘备份，进行恢复，要求线上有定期的全量备份，并实时备份binlog</p><p>先将上一次全量备份的日志应用于临时库，再将上一次全量备份到当前的实时备份的除去误删除操作的语句，全部应用到临时库上，之后再将临时库的数据恢复到主库当中去</p><p>​    <strong>延迟复制备库</strong></p><p>如如果复制备库和主库之间延迟一个小时，则如果主库出现了误删的情况，只需要通过备库再追加一个小时即可恢复数据</p><h3 id="kill进程"><a href="#kill进程" class="headerlink" title="kill进程"></a>kill进程</h3><p>在执行了 kill query thread_id_B时，进行如下处理</p><ul><li><p>将进程B的运行状态改为THD::KILL_QUERY</p></li><li><p>向事务B的执行进程发送一个信号，用于告知进程的状态发生了变化，让其来处理THD::KILL_QUERY</p></li></ul><ol><li><p>一个进程的执行过程中存在埋点，只有在埋点才会判断线程的状态</p></li><li><p>只有能够被唤醒的等待最后才能执行到埋点出，如处于wait状态的线程便是可以唤醒的，但是处于IO等待或者达到InnoDB并发上限，未被执行在等待的线程均为不可唤醒的</p></li><li><p>语句从进入终止逻辑，到最后终止逻辑完成，存在一个过程</p></li></ol><p>对于达到InnoDB并发线程上限后还未执行的线程，只有在被选中执行后，才能检查线程的状态，发现了KILL_QUERY 或者KILL_CONNECTION后，才会进入终止逻辑</p><h3 id="查询结果的处理逻辑"><a href="#查询结果的处理逻辑" class="headerlink" title="查询结果的处理逻辑"></a>查询结果的处理逻辑</h3><p><strong>server层</strong></p><p>查询结果返回给客户端的过程是边读边发的</p><ol><li>对于查询的结果，像将其写入到net_buffer当中，直至net_buffer写满</li><li>调用网络接口发出去，发送给客户端</li><li>发送成功则清空net_buffer，继续读取</li><li>如果发送函数返回 EAGAIN 或 WSAEWOULDBLOCK，就表示本地网络栈（socket send buffer）写满了，进入等待。直到网络栈重新可写，再继续发送。</li></ol><p>sending to client表示net_buffer已经写满</p><p>只有当查询结果不能够全部放入到net_buffer当中时，需要等待net_buffer清空后再执行时，才显示sending to client，只要查询结果能全部放入到net_buffer当中，执行器就可认为执行结束，与net_buffer是否能放入到socket send buffer这个过程无关，而socket send buffer不仅只有MySQL在使用</p><p><strong>InnoDB</strong></p><p>内存的数据页在Buffer Pool中管理，借助于内存，减少磁盘IO，从而加速查询</p><p>InnoDB的内存管理采用的为改进LRU算法，即按照 5:3 的比例把整个 LRU 链表分成了 young 区域和 old 区域用于解决全盘扫描导致的内存页淘汰从而命中率急剧下降的问题</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/21f64a6799645b1410ed40d016139828.png?raw=true" alt="img"></p><ul><li>新加入的数据页，放在old区，便于如全盘扫描等情况下对其进行快速淘汰</li><li>处于old区的数据页，如果在链表中存在超出的一定时间，则移动到链表的头部去，默认为1000ms，由innode_old_blocks_time控制，与操作系统当中的多级反馈队列调度方式的设计思想由相似之处</li></ul><h3 id="Join执行过程"><a href="#Join执行过程" class="headerlink" title="Join执行过程"></a>Join执行过程</h3><p>执行Join时，主要可以根据是否使用索引分为两种情况，对应不同的算法</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs mysql">CREATE TABLE `t2` (<br>  `id` int(11) NOT NULL,<br>  `a` int(11) DEFAULT NULL,<br>  `b` int(11) DEFAULT NULL,<br>  PRIMARY KEY (`id`),<br>  KEY `a` (`a`)<br>) ENGINE=InnoDB;<br><br>drop procedure idata;<br>delimiter ;;<br>create procedure idata()<br>begin<br>  declare i int;<br>  set i=1;<br>  while(i&lt;=1000)do<br>    insert into t2 values(i, i, i);<br>    set i=i+1;<br>  end while;<br>end;;<br>delimiter ;<br>call idata();<br><br>create table t1 like t2;<br>insert into t1 (select * from t2 where id&lt;=100)<br></code></pre></td></tr></table></figure><p><strong>使用索引：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">select * from t1 straight_join t2 on (t1.a=t2.a);<br></code></pre></td></tr></table></figure><p>对应的算法为Index Nested-Loop Join</p><p>执行过程：</p><ol><li><p>从t1中取出一条数据</p></li><li><p>根据t1.a去t2中查询</p></li><li><p>取出t2中满足条件的行，和t1的查询结果组成一个字段，作为结果集</p></li><li><p>重复上述结果查询出所有</p></li></ol><p>如果不使用join,则需要先从t1中查询出所有结果，再遍历t1的结果去t2中查询，总共执行了101条语句，比直接使用Join多了100次交互（扫描的行数的查询的次数相同，但是多了执行sql的开销，如优化器和执行器部分），因此不如直接使用Join语句</p><p>​    <strong>驱动表的选择</strong></p><p>对于驱动表，需要进行全表扫描获得所有行，而对于被驱动表，只需根据条件走索引查询即可，综合复杂度为:N + N<em>2</em>log2(M)。(N为驱动，M被驱动)，因此选取小表作为驱动表的复杂度较低</p><p><strong>不使用索引：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">select * from t1 straight_join t2 on (t1.a=t2.b);<br></code></pre></td></tr></table></figure><p>对应的算法为Block Nested-Loop Join</p><ol><li>把t1的所有的数据读入到线程内存join_buffer当中，由于为select *,因此放入所有的字段，</li><li>扫描表t2，取出t2的每一行，在于join_buffer中的数据进行对比</li></ol><p>扫描次数为M*N次，但是为内存级，比不使用join_Buffer直接访问磁盘性能要好</p><p>两个表都做一次全表扫描，所以总的扫描行数是 M+N；内存中的判断次数是 M*N。</p><p>如果join_buffer无法放下t1的所有行，则分段进行，先放入一部分，然后查询t2，之后清空join_buffer再继续放入,判断的总行数不变，但是由于t2表涉及多次与join_buffer进行比较，因此扫描的行数为N+K*M,K为分段数</p><p><strong>选择</strong></p><ul><li>如果是 Index Nested-Loop Join 算法，应该选择小表做驱动表；</li><li>如果是 Block Nested-Loop Join 算法：<ul><li>在 join_buffer_size 足够大的时候，是一样的；</li><li>在 join_buffer_size 不够大的时候（这种情况更常见），应该选择小表做驱动表。</li></ul></li></ul><p>小表：查询出的字段较少，行较少的表，总数据量为字段*行数，总数量较小的为小表</p><p><strong>left join&#x2F;where</strong></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">select</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">from</span> a <span class="hljs-keyword">left</span> <span class="hljs-keyword">join</span> b <span class="hljs-keyword">on</span>(a.f1<span class="hljs-operator">=</span>b.f1) <span class="hljs-keyword">and</span> (a.f2<span class="hljs-operator">=</span>b.f2); <span class="hljs-comment">/*Q1*/</span><br><span class="hljs-keyword">select</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">from</span> a <span class="hljs-keyword">left</span> <span class="hljs-keyword">join</span> b <span class="hljs-keyword">on</span>(a.f1<span class="hljs-operator">=</span>b.f1) <span class="hljs-keyword">where</span> (a.f2<span class="hljs-operator">=</span>b.f2);<span class="hljs-comment">/*Q2*/</span><br></code></pre></td></tr></table></figure><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/93a7294dd9ea8dc1016f69c0fd223f0387cf1067.jpg?raw=true" alt="2022-07-17-20-03-55-8966773b5370b0c514d019c9dcc4898.jpg"> </p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/b7f27917ceb0be90ef7b201f2794c817.png?raw=true" alt="img"></p><p>在条件全部写在join 的on内时，先将驱动表的全部数据导入join_buffer再通过on 进行匹配，如果匹配成功则填入数值，如果无匹配结果则通过null填充</p><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/f5712c56dc84d331990409a5c313ea9c.png?raw=true" alt="img"></p><p>使用 NLJ算法，将b中的值对应到表a中查询，匹配到记录后在判断where的条件，如果不满则则被过滤掉，因此不会存在null的情况</p><p>由于where的语义为匹配成功就放弃，与join 的语义一致，因此优化器会将left join优化为join,因此，left join的左边不一定为驱动表</p><h3 id="Join优化"><a href="#Join优化" class="headerlink" title="Join优化"></a>Join优化</h3><p><strong>Multi-Range Read</strong></p><p>主要思想为将随机读盘优化为顺序读盘</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">select * from t1 where a&gt;=1 and a&lt;=100;<br></code></pre></td></tr></table></figure><p>其中，a为唯一索引，当a的顺序与主键索引不一致时，则回表的查询过程为随机访问，开销较大，优化思路为将其优化成顺序读盘</p><p>具体过程：</p><ol><li>根据索引a，定位到满足条件的行，将id放入read_rnd_buffer</li><li>将read_rnd_buffer中的id进行递增排序</li><li>在根据有序的id去回表查询，此时随机访问变为顺序访问，减小开销</li></ol><p><strong>Batched Key Access</strong></p><p>在Join时，为一行行的从t1中获取数据再到t2中比对，此时便无法再使用MRR进行优化</p><p>解决方案为将t1中的数据查询先放到一个临时内存(Join_buffer)当中，之后便可以使用MRR进行优化</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">select * from t1 join t2 on (t1.b=t2.b) where t2.b&gt;=1 and t2.b&lt;=2000;<br></code></pre></td></tr></table></figure><ol><li><p>将t2中满足条件的行放入到临时表当中tmp_t</p></li><li><p>为使用BKA，给临时表的b加上索引(B+树搜索，join时无需遍历)</p></li><li><p>t1和tmp_t进行join</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">create</span> temporary <span class="hljs-keyword">table</span> temp_t(id <span class="hljs-type">int</span> <span class="hljs-keyword">primary</span> key, a <span class="hljs-type">int</span>, b <span class="hljs-type">int</span>, index(b))engine<span class="hljs-operator">=</span>innodb;<br><span class="hljs-keyword">insert</span> <span class="hljs-keyword">into</span> temp_t <span class="hljs-keyword">select</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">from</span> t2 <span class="hljs-keyword">where</span> b<span class="hljs-operator">&gt;=</span><span class="hljs-number">1</span> <span class="hljs-keyword">and</span> b<span class="hljs-operator">&lt;=</span><span class="hljs-number">2000</span>;<br><span class="hljs-keyword">select</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">from</span> t1 <span class="hljs-keyword">join</span> temp_t <span class="hljs-keyword">on</span> (t1.b<span class="hljs-operator">=</span>temp_t.b);<br></code></pre></td></tr></table></figure></li></ol><h3 id="临时表"><a href="#临时表" class="headerlink" title="临时表"></a>临时表</h3><p><strong>内存表&#x2F;临时表</strong></p><p>内存表指的是使用Memory引擎的表，数据保存在内存当中，而临时表只是表明表为临时的，存储的位置根据使用的引擎而定，二者之间并无直接的联系</p><p>特性：</p><ul><li>建表语句：create temporary table</li><li>之对于创建他的线程可见，其他线程不可见</li><li>可以与普通表同名(存储时通过线程创建前缀)</li><li>与同名的普通表存在时，CRUD访问的是临时表</li><li>show tables 不显示临时表</li><li>发生异常断开或者正常关闭时会自动清理临时表</li></ul><p><strong>临时表存储</strong></p><p>MySQL通过frm文件来保存表结构的定义，同时还有对应的保存表数据，因此这个 frm 文件放在临时文件目录下，文件名的后缀是.frm，前缀是“#sql{进程 id}_{线程 id}_ 序列号”,通过前缀即可与普通表区分出，因此可以与普通表重名</p><ul><li>在 5.6 以及之前的版本里，MySQL 会在临时文件目录下创建一个相同前缀、以.ibd 为后缀的文件，用来存放数据文件；</li><li>而从 5.7 版本开始，MySQL 引入了一个临时文件表空间，专门用来存放临时文件的数据。因此，我们就不需要再创建 ibd 文件了。</li></ul><p><strong>主备复制</strong></p><p>当binlog为row形式时，不存储临时表有关的操作，需要在statement&#x2F;mix形式时，按照sql语句进行存储，才会记录有关临时表的相关操作</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs mysql">insert into temp_t values(1,1);/*Q3*/<br>insert into t_normal select * from temp_t;/*Q4*/<br></code></pre></td></tr></table></figure><p>Q3Q4对应的row形式为write_row event里面记录的逻辑时插入一行数据(1,1)，未体现出临时表</p><p>由于备库上执行日志为单线程，因此记录binlog时，会将主库中的线程一并写入到binlog当中，进行区分不同线程的临时表</p><p><strong>使用场景</strong></p><p>使用时机：处理复杂的逻辑计算，对查询结果先进行处理再返回给客户端，如group by，union等</p><p>​    <strong>union</strong></p><p>union为两个查询的并集，因此使用临时表，对其进行一个取并集的操作，即先将第一个子查询的存入临时表，再将另外一个子查询的结果判重后插入到其中</p><p>​    <strong>group by</strong></p><p>创建临时表和对应的字段，将查询到的结果放入到临时表中对应的分组下</p><p>例：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">select id%10 as m, count(*) as c from t1 group by m <br></code></pre></td></tr></table></figure><p>group by 默认进行排序，如果想乱序，则<code>order by null</code></p><p>​    <strong>group by 优化</strong></p><p>索引：</p><p>group by是统计不同的值出现的个数。但是，由于每一行的 id%10 的结果是无序的，所以我们就需要有一个临时表，来记录并统计结果。因此优化的方向便是令id%10处于一种有序的结果，共有两种思路：一是创建索引，二是通过SQL_BIG_RESULT指定进行filesort</p><blockquote><p>在 MySQL 5.7 版本支持了 generated column 机制，用来实现列数据的关联更新。你可以用下面的方法创建一个列 z，然后在 z 列上创建一个索引（如果是 MySQL 5.6 及之前的版本，你也可以创建普通列和索引，来解决这个问题）。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs mysql">alter table t1 add column z int generated always as(id % 100), add index(z);<br><br>select z, count(*) as c from t1 group by z;<br></code></pre></td></tr></table></figure></blockquote><p>直接排序：</p><p>直接使用sort buffer进行排序，不再使用临时表，便按照sort_buffer的排序原则，使用filesort算法进行排序</p><blockquote><p>在 group by 语句中加入 SQL_BIG_RESULT 这个提示（hint），就可以告诉优化器：这个语句涉及的数据量很大。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">select SQL_BIG_RESULT id%100 as m, count(*) as c from t1 group by m;<br></code></pre></td></tr></table></figure><p>扫描表 t1 的索引 a，依次取出里面的 id 值, 将 id%100 的值存入 sort_buffer 中,排序后即可得到一个有序<strong>数组</strong></p></blockquote><h3 id="Memory引擎"><a href="#Memory引擎" class="headerlink" title="Memory引擎"></a>Memory引擎</h3><p>Memory引擎对应的表为内存表</p><p><strong>特点</strong>：</p><ul><li>组织方式：(堆组织表)数据和索引分开，数据以数组的形式存放，按照插入的顺序，删除之后会留下空位，后续新插入的可以利用之前的空位，索引通过哈希表的方式组织，InnoDB数据放在主键索引上的组织方式称为索引组织表</li><li>索引：通过哈希表的形式组织，并且所有索引的地位都相同，不存在回表的过程，数据<strong>位置</strong>发生变化是，所有的索引都需要进行修改</li><li>数据：内存表不支持 Blob 和 Text 字段，并且即使定义了 varchar(N)，实际也当作 char(N)，也就是固定长度字符串来存储，因此内存表的每行数据长度相同。</li></ul><p>Memory引擎可以手动指定索引为btree,与MySQL情况相同</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs mysql">alter table t1 add index a_btree_index using btree (id);<br></code></pre></td></tr></table></figure><p><img src="https://github.com/Fischer0522/note/blob/master/mysql45%E8%AE%B2/mysql45%E8%AE%B2.assets/1788deca56cb83c114d8353c92e3bde3.jpg?raw=true" alt="img"></p><p><strong>锁</strong></p><p>只支持表级锁，较为影响并发度</p><p><strong>数据持久性</strong></p><p>重启后会表会被清空，主要在主库和备库之间的同步存在一定的问题</p><ul><li>MS架构下，如果备库重启了，主库再操作内存表，则会在备库上找不到对应的表</li><li>MM架构下，备库重启，会向备库的binlog写入一条<code>delete from t</code>,如果再发生主备切换，主库再将备库的binlog拿出来执行，此时导致主库上面还存在的内存表再切换时被清空</li></ul><p>建议除了在数据量可控，不会耗费过多内存的情况下，考虑使用内存表。其他时候全部使用InnoDB,由于InnoDB的buffer pool,因此性能也不会过差，并且InnoDB 在并发度上的支持度更好</p><h3 id="自增主键"><a href="#自增主键" class="headerlink" title="自增主键"></a>自增主键</h3><p>使用InnoDB引擎时，自增值保存在内存当中，并未持久化，每次重启后都寻找自增值的最大值，将最大值+1作为当前的自增值，8.0版本之后，自增值保存在redolog当中，有了持久化的能力</p><p><strong>自增修改机制</strong>：</p><ul><li>id为0，null时，AUTO_INCREMENT填到当前字段当中</li><li>如果指定则使用指定值<ul><li>如果指定值比AUTO_INCREMENT大，则对AUTO_INCREMENT进行更新</li></ul></li><li>新自增值生成算法：auto_increment_offset 开始，每次叠加auto_increment_increment，直至大于表前的最大值，这两个值的默认值均为1</li></ul><p><strong>修改时机</strong></p><p>在真正数据插入之前，先对AUTO_INCREMENT进行修改，基本插入失败，修改的AUTO_INCREMENT值也不会再回滚</p><p>如果进行回滚，则会导致冲突问题，如果当前为2，事务A插入数据改为3,事务A由于唯一键冲突进行回滚，在AUTO_INCREMENT改回2之前事务B插入一条id为3的数据，此时AUTO_INCREMENT的值为2，再重新插入自增数据则导致duplicate key</p><p><strong>自增锁</strong></p><p>语句级别，需要等待该条获取锁的语句执行结束后再释放锁</p><p>innodb_autoinc_lock_mode:</p><ul><li>0：语句结束后才释放锁</li><li>1：<ul><li>普通insert：锁申请之后立马释放</li><li>批量插入数据：语句执行结束后再释放(防止其他线程中途插入值，导致binlog在statement的形式下的数据不一致问题) <ul><li>解决思路1：语句结束后再释放锁</li><li>解决思路2：binlog设置为row形式，innodb_autoinc_lock_mode设置为2,解决数据不一致问题，并且提高并发度</li></ul></li></ul></li><li>2：所有的情况均为申请后立刻释放锁</li></ul><p>批量插入数据，包含的语句类型是 insert … select、replace … select 和 load data 语句。innodb_autoinc_lock_mode 设置为 1时，每次申请的id为上一次申请的id的2倍，从而导致了浪费和不连续的问题</p><p><strong>备库自增主键</strong></p><p>由于binlog写入的顺序取决于事务的提交顺序，与真实的执行顺序不一致，因为为了防止由于执行顺序导致的自增主键的值不一样的问题,binlog中在写入插入语句之间回添加一条set INSER_ID &#x3D; ***,保证不受顺序的影响</p><p><strong>id不连续的原因</strong></p><ol><li>数据插入失败时AUTO_INCREMENT不会回退</li><li>事务回滚AUTO_INCRMENT不会回退</li><li>innodb_autoinc_lock_mode为1时批量申请</li></ol><h3 id="赋权"><a href="#赋权" class="headerlink" title="赋权"></a>赋权</h3><p>grant用于给用户进行赋权，通常无需像部分文档说的那样在后面补充上一个flush privileges,将内存中的改变给flush到硬盘之中</p><p><strong>全局权限</strong></p><p>对于该用户在mysql全局有效，信息保存在mysql的user表当中，通过grant对其进行赋权,给予all privileges最高权限</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">grant</span> <span class="hljs-keyword">all</span> privileges <span class="hljs-keyword">on</span> <span class="hljs-operator">*</span>.<span class="hljs-operator">*</span> <span class="hljs-keyword">to</span> <span class="hljs-string">&#x27;ua&#x27;</span>@<span class="hljs-string">&#x27;%&#x27;</span> <span class="hljs-keyword">with</span> <span class="hljs-keyword">grant</span> option;<br></code></pre></td></tr></table></figure><ol><li><p>磁盘上将mysql.user表里，用户的所有的对应的权限全改为Y</p></li><li><p>内存中，从数组acl_users 中找到这个用户对应的对象，将access(权限位)修改为1</p></li></ol><p>通过新的客户端登录该用户时，MySQL创建一个新的连接维护线程对象,通过acl_user进行权限的确认，然后保存到该线程<strong>内部</strong>的权限位，因此该用户已经登录之后，全局权限不受grant的影响</p><p>revoke用于撤销权限，作用效果和所作的工作与grant完全相反</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">revoke</span> <span class="hljs-keyword">all</span> privileges <span class="hljs-keyword">on</span> <span class="hljs-operator">*</span>.<span class="hljs-operator">*</span> <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;ua&#x27;</span>@<span class="hljs-string">&#x27;%&#x27;</span>;<br></code></pre></td></tr></table></figure><p><strong>数据库权限</strong></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">grant</span> <span class="hljs-keyword">all</span> privileges <span class="hljs-keyword">on</span> db1.<span class="hljs-operator">*</span> <span class="hljs-keyword">to</span> <span class="hljs-string">&#x27;ua&#x27;</span>@<span class="hljs-string">&#x27;%&#x27;</span> <span class="hljs-keyword">with</span> <span class="hljs-keyword">grant</span> option;<br></code></pre></td></tr></table></figure><p>对内存和磁盘同时生效：</p><ol><li><p>磁盘上 向mysql.db表中插入一条记录,所有的权限字段全部设置为Y</p></li><li><p>内存中 增加一个对象到数组acl_dbs中，access权限位全为1</p></li></ol><p>在此之后的权限判断借助于acl_dbs这个全局数组，yinci对应已经登录的用户,grant和revoke都会影响当前的权限状态</p><p><strong>表&#x2F;列权限</strong></p><blockquote><p>除了 db 级别的权限外，MySQL 支持更细粒度的表权限和列权限。其中，表权限定义存放在表 mysql.tables_priv 中，列权限定义存放在表 mysql.columns_priv 中。这两类权限，组合起来存放在内存的 hash 结构 column_priv_hash 中。</p><p>跟 db 权限类似，这两个权限每次 grant 的时候都会修改数据表，也会同步修改内存中的 hash 结构。因此，对这两类权限的操作，也会马上影响到已经存在的连接。</p></blockquote><p><strong>flush privileges使用场景</strong></p><p>flush privileges为清空 当前内存中的全局数组(acl_users,acl_dbs),从硬盘的表中重新加载一遍数据，此后再从内存的数组中读取数据时便可读取到最新的权限，达到内存与硬盘的数据一致，但是并不会去更新线程对象中维护的权限，因此对全局权限仍无影响。</p><p>在通常情况下，使用grant和revoke可以保证内存和硬盘中的权限对应一致，无需再语句后面跟随flush privileges。</p><p>在直接通过删除表中数据的方式进行权限的修改会导致内存中的数据未被更新，此时需要flush privileges来进行硬盘和内存的数据同步</p><h3 id="自增id上界"><a href="#自增id上界" class="headerlink" title="自增id上界"></a>自增id上界</h3><p><strong>表中的自增的AUTO_INCREMENT</strong></p><ul><li>上限根据字段的数据类型而定，像int 即为2^32^-1</li><li>达到上限后值将不再变化，如果为唯一值则会存在冲突</li></ul><p><strong>InnoDB系统自增的row_id</strong></p><ul><li>用于进行提供位置信息(无主键时会生成一个row_id)</li><li>整个InnoDB系统共享这一个row_id</li><li>冲突之后则会从0开始，row_id相同的行新数据会把旧数据给覆盖掉</li><li>上限为2^48^-1</li></ul><p><strong>Xid</strong></p><ul><li>对应事务，用于进行redolog 和binlog之间的统一，同一个事务写入binlog和redolog时会使用同一个Xid,从而关联起来</li><li>MySQL内部维护了global_query_id，执行语句时复制给Query_id,事务的第一条语句会将该语句的query_id赋值给事务的Xid</li><li>global_query_id为纯内存变量，随着重启清零，但是重启后会生成新的binlog文件，因此在同一个binlog中不存在Xid冲突的问题</li><li>上限为2^64^-1</li><li>理论上存在冲突，实际上难以达到，冲突后从0开始</li></ul><p><strong>InnoDB trx_id</strong></p><ul><li>用于判断事务的可见性，通过低水位和高水位来生成活跃数组</li><li>InnoDB内部维护max_trx_id,赋值给新的trx_id</li><li>由于只读事务不影响事务可见性，因此对于只读事务不分配trx_id<ul><li>减少申请trx_id的并发度，减小锁冲突</li><li>减少活跃事务数组的大小</li></ul></li><li>在未分配事务时，trx_id为把当前事务的 trx 变量的指针地址转成整数，再加上 2^48^，<ul><li>指针地址保证了不会重复，</li><li>2^48^将其与正常对可见性有影响的trx_id区分开</li></ul></li><li>trx_id有时不是按照+1来递增：<ol><li>update delete涉及到标记删除旧数据，也就是要把数据放到 purge 队列里等待后续物理删除，这个操作也会把 max_trx_id+1， 因此在一个事务中至少加 2；</li><li>后台操作启动内部事务</li></ol></li><li>max_trx_id持久化存储，重启不会清零，达到上限后清零,清零后导致新的事务可能会到达低水位以下，从而变为可见的事务</li><li>上限为2^48^-1</li><li>理论上存在脏读问题，但是达到上限的时间过长</li></ul><p><strong>thread_id</strong></p><ul><li>维护了全局变量thread_id_counter</li><li>上限2^32^-1,到达上限后重置为0</li><li>存在一个算法来保证不冲突的情况</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">do</span> &#123;<br>  new_id= thread_id_counter++;<br>&#125; <span class="hljs-keyword">while</span> (!thread_ids.insert_unique(new_id).second);    <br></code></pre></td></tr></table></figure>]]></content>
    
    
    
    <tags>
      
      <tag>数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis设计与实现</title>
    <link href="/2022/08/08/%E2%80%9CRedis%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0%E2%80%9D/"/>
    <url>/2022/08/08/%E2%80%9CRedis%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0%E2%80%9D/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis设计与实现"><a href="#Redis设计与实现" class="headerlink" title="Redis设计与实现"></a>Redis设计与实现</h1><h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><p>redis中的数据结构主要分为两个层次，一是底层实现，为数据的真实存储方式，如链表，SDS，跳表等，二是对象系统，在底层数据结构之上进行创建，将底层数据结构作为对象的实现方式。对象系统作为Value存储在K-V数据库当中</p><h3 id="底层实现"><a href="#底层实现" class="headerlink" title="底层实现"></a>底层实现</h3><h4 id="简单字符串SDS"><a href="#简单字符串SDS" class="headerlink" title="简单字符串SDS"></a>简单字符串SDS</h4><p>定义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">sdshdr</span> &#123;</span><br>    <span class="hljs-comment">// buf数组中已经使用的长度</span><br>    <span class="hljs-type">int</span> len;<br>    <span class="hljs-comment">// buf数组中空余的字节数</span><br>    <span class="hljs-type">int</span> <span class="hljs-built_in">free</span>;<br>    <span class="hljs-comment">//真实存储的字符数组，符合C的规范，以\0结尾</span><br>    <span class="hljs-type">char</span> buf[];<br>&#125;<br></code></pre></td></tr></table></figure><p>特点：</p><ul><li>杜绝缓冲区溢出<ul><li>对于传统的C字符串，两个相邻的字符串，对第一个进行修改时如果超出边界则会溢出到后一个字符串上，导致第二个的内容被修改</li><li>SDS在修改时会判断是否超限，超限则自动扩容</li></ul></li><li>通过free属性，减少重新分配内存的操作，分配时预先分配，预先分配的空间计入到free其中，回收时惰性回收，回收但未释放的空间计入到free当中</li><li>二进制安全</li><li>buf遵循C字符串的规则，因此兼容C的字符串函数</li></ul><h4 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h4><p>Redis中实现为双向链表，分为节点和链表本身，定义如下</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">listNode</span> &#123;</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">listNode</span> *<span class="hljs-title">prev</span>;</span><br>    <br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">listNode</span> *<span class="hljs-title">next</span>;</span><br>    <br>    <span class="hljs-type">void</span> *vaule;<br>&#125;<br></code></pre></td></tr></table></figure><p>链表：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">list</span> &#123;</span><br>    listNode *head;<br>    <br>    listNode *tail;<br>    <br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> len;<br>    <span class="hljs-comment">// 复制节点保存的值</span><br>    <span class="hljs-type">void</span> *(*dup) (<span class="hljs-type">void</span> *ptr);<br>    <span class="hljs-comment">// 释放节点</span><br>    <span class="hljs-type">void</span> (*<span class="hljs-built_in">free</span>) (<span class="hljs-type">void</span> *ptr);<br>    <span class="hljs-comment">// 比较节点和输入的值</span><br>    <span class="hljs-type">int</span> (*match) (<span class="hljs-type">void</span> *ptr,<span class="hljs-type">void</span> *key);<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>多态</strong>：其中 节点的值以及free dup match函数均使用void *无类型指针来表示，可以指向任意一种数据类型，从而实现了链表可以存储多种数据类型，不存在泛型，设计思想类似于Java中的Object类</p><h4 id="字典"><a href="#字典" class="headerlink" title="字典"></a>字典</h4><p>字典的底层实现为哈希表，字典再对于哈希表进行进一步封装，用于特定操作和复制</p><p><strong>哈希表</strong></p><p>实现：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictht</span> &#123;</span><br>    <br>    dictEntry **table;<br>    <br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> size;<br>    <span class="hljs-comment">// 哈希表大小掩码，用于计算索引值 等于size - 1</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> sizemask;<br>    <span class="hljs-comment">// 已使用的节点数</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> used;<br>    <br>&#125; dictht;<br></code></pre></td></tr></table></figure><p>table为一个数组，数组中的每一个元素指向一个dictEntry结构的指针，多个dictEntry，通过链表的形式组织起来形成哈希桶，结构如下</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictEntry</span> &#123;</span><br>    <span class="hljs-type">void</span> *key;<br>    <br>    <span class="hljs-class"><span class="hljs-keyword">union</span> &#123;</span><br>        <span class="hljs-type">void</span> *val;<br>        <span class="hljs-type">unit64_t</span> u64;<br>        <span class="hljs-type">int64_t</span> s64;<br>    &#125; v;<br>    <br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictEntry</span> *<span class="hljs-title">next</span>;</span><br>    <br>&#125;dictEntry;<br></code></pre></td></tr></table></figure><p>dictEntrty ，K-V类型V通过union的形式，存储的值为三者其中之一，三者共用同一段内存，存在内存覆盖</p><p><strong>字典实现</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dict</span> &#123;</span><br>    dictType *type;<br>    <br>    <span class="hljs-type">void</span> *privdata;<br>    <br>    dictht ht[<span class="hljs-number">2</span>];<br>    <br>    <span class="hljs-type">int</span> rehashidx;<br>&#125;<br></code></pre></td></tr></table></figure><p>其中type为一组特定函数封装成的结构体，privdata保存的为传给type中函数的参数，</p><p>ht为两个哈希表，正常情况下只使用ht[0]，在rehash复制时才使用到ht[1]</p><p>trehashidx为rehash的进度</p><p><strong>哈希算法</strong></p><p>K-V为(k0,v0),首先通过type中的hashFunction函数计算出哈希值,在根据sizemask计算出索引值</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs c">hash = dict-&gt;type-&gt;hashFunction(k0);<br>index = hash&amp;dict-&gt;ht[<span class="hljs-number">0</span>].sizemask <span class="hljs-comment">// </span><br></code></pre></td></tr></table></figure><p>通过位与运算，可将计算处的哈希值转换到[0,size-1]的范围之内，获得合适的索引</p><p><strong>冲突&#x2F;rehash</strong></p><p>redis中通过链表的形式来解决哈希冲突的问题，索引为同一个哈希桶时通过链表加到**表头(O(1)复杂度)**，但是由于没有对单个哈希桶降低复杂度的策略(像Java中HashMap中在链表过长时转换成红黑树)，导致后续复杂度过高，因此需要rehash进行解决</p><p>rehash：集中性的rehash时间消耗过大，导致服务器一段时间不可用，因此采用的为渐进多次rehash：</p><ol><li>开始rehash时为ht[1]分配空间，过程中一直保持ht[1]</li><li>修改rehashidx &#x3D; 0，表示rehash 正式开始</li><li>每次对字典CRUD时，将rehashidx位置上的k-v迁移到ht[1]之中，rehashidx++</li><li>完成后rehashidx置为-1</li></ol><h4 id="跳表"><a href="#跳表" class="headerlink" title="跳表"></a>跳表</h4><p>有序数据结构，本质上为一个有序链表，核心设计思想为通过分层的思想，设计不同级别的索引，实现每次跳跃不同的距离，层数越高，对应的索引每次的跳跃幅度就越大，从而达到快速搜索的目的</p><p><img src="/%E2%80%9CRedis%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0%E2%80%9D/1eca7135d38de2yy16681c2bbc4f3fb4.jpg" alt="img"></p><p><strong>实现</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplistNode</span> &#123;</span><br>    <span class="hljs-comment">// 后退指针</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplistNode</span> *<span class="hljs-title">backward</span>;</span><br>    <span class="hljs-comment">// 分值</span><br>    <span class="hljs-type">double</span> score;<br>    <span class="hljs-comment">// 成员对象</span><br>    robj *obj;<br>    <span class="hljs-comment">// 层</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplistLevel</span> &#123;</span><br>        <span class="hljs-comment">// 前进指针</span><br>        <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplistNode</span> *<span class="hljs-title">forward</span>;</span><br>        <span class="hljs-comment">// 跨度</span><br>        <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> span;<br>    &#125; level[];<br>    <br>&#125; zskiplistNode;<br></code></pre></td></tr></table></figure><p>其中，后退指针用于向后遍历，指向后面的一个节点，</p><p>分值用于进行排序，obj为真正存储的对象</p><p>level为层数组，表示每一层指向的下一个节点和这两个这点直接的跨度</p><p>通过跳表节点即可形成跳表</p><p>跳表：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplist</span> &#123;</span><br>   <br>    zskiplistNode * header;<br>    <br>    zskiplistNode *tail;<br>    <br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> length;<br>    <br>    <span class="hljs-type">int</span> level;<br>    <br>&#125; zskiplist<br></code></pre></td></tr></table></figure><p><strong>跳表构建</strong></p><p>除了level层索引之外，构建的过程和链表一致，而对于level,Redis设计的level的层数为1-32之间的随机整数，具体的实现过程为二分确定，即构建新节点时，新增加一个层的概率为1&#x2F;4，即三层的概率为 <code>1/4 * 1/4 * 3/4</code></p><p><strong>跳表定位</strong></p><p>跳表的搜索过程与二叉树的思想类似，通过不断缩小区间确定最终的值，在性能上也与二叉搜索相当，复杂度为O(logn)</p><p>过程：</p><ol><li><p>先搜索最高层索引,直至找到了目标值</p></li><li><p>相等则直接返回，如果目标值大于当前节点的值则继续向后遍历</p></li><li><p>如果当前节点的值小于目标值，而当前节点的下一个值大于目标值，即定位到了区间，通过回退指针找到下一层索引的上一个节点，继续重复上述操作</p></li></ol><p>相比二叉树的优点：</p><ul><li>使用了更少的指针，节约空间</li><li>更加适合区间遍历，适配ZRANGE等操作</li></ul><h4 id="整数集合"><a href="#整数集合" class="headerlink" title="整数集合"></a>整数集合</h4><p>存储的类型为int16_t,int32_t,int_64t,且不可重复出现</p><p>结构定义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">intset</span> &#123;</span><br>    <span class="hljs-type">uint32_t</span> encoding;<br>    <br>    <span class="hljs-type">uint32_t</span> length;<br>    <br>    <span class="hljs-type">int8_t</span> contents[];<br>&#125; intset;<br></code></pre></td></tr></table></figure><p>contents的类型定义为int8_t，但是实际的存储类型由编码格式而定，为int16_t,int32_t,int64_t的其中一种，并且整个集合的存储类型保持一致，为保持一致，涉及到升级和降级的过程</p><p> <strong>升级：</strong></p><p>若之前存储的为三个int16_t类型，在新增int64_t类型时， 首先分配空间64*4(原本+新增)，再将原本的进行转换，和新增的一同插入到新分配的空间当中。通过升级的策略，存储更加灵活且且节约内存</p><p>降级过程同理</p><h4 id="压缩列表"><a href="#压缩列表" class="headerlink" title="压缩列表"></a>压缩列表</h4><p>用于节约内存，分配连续空间来存放各个节点，各个节点保存一个整数值或者字节数组</p><p>节点由previous_length,encoding,content组成：</p><ul><li>previous_entry_length:本身为一个字节或五个字节，内容以字节为单位，保存前一个节点的在内存中所占的字节数，为previous_entry_length,encoding,content三个元素总字节数，而非content的长度，当前节点的起始指针减去previous_entry_length即可得到上一个节点的起始指针</li><li>encoding表示content中的数据类型和长度，</li><li>content保存节点的值</li></ul><p><strong>连锁更新</strong></p><p>假设存在连续的几个节点的previous_entry_length均使用一个字节，内容为254,但最前方插入一个新的长度&gt;255字节的节点时，1个字节无法表示255以上的值，从而需要换成5字节长度，previous_entry_length长度自身的增加导致节点长度超出了255，引发后续节点的prevous_enrty_length的改变扩容，导致连锁更新</p><p>删除节点同样也会引起连锁更新</p><p>最坏情况下(在列表头部插入),需要进行N次内存分配，每次内存分配的复杂度为O(N),最终的最坏复杂度为$O(N^2)$</p><h3 id="对象"><a href="#对象" class="headerlink" title="对象"></a>对象</h3><p>Redis中的每一个对象通过一个对象系统redisObject来表示</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">redisObject</span> &#123;</span><br>    <span class="hljs-type">unsigned</span>  type;<br>    <span class="hljs-type">unsigned</span> encoding;<br>    <span class="hljs-type">void</span> *ptr;<br>    <span class="hljs-comment">// ...</span><br>&#125; robj;<br></code></pre></td></tr></table></figure><p>type表示对象的类型，encoding表示底层编码的实现方式</p><h4 id="字符串对象"><a href="#字符串对象" class="headerlink" title="字符串对象"></a>字符串对象</h4><p>可作为元素嵌套在其他的对象其中</p><p>编码方式：</p><ul><li>int：用于表示整数形式，void* 指向一个long类型的数据</li><li>embstr：表示短字符串，长度小于39字节，一次性分配redisObject和sdshdr的空间</li><li>raw：通常存储较长的字符串，或者embstr中的内容经过修改后编码格式也变成raw，分两次分配redisObejct和sdshdr的空间，在内存中不连续</li></ul><p>编码转换：</p><ul><li>int中存储的字符串不再是整数时，转换为raw</li><li>embstr中内容修改后，转换为raw,embstr无相应的修改程序，embstr实际上为只读字符串，因此修改后转换为embstr</li></ul><h4 id="列表对象"><a href="#列表对象" class="headerlink" title="列表对象"></a>列表对象</h4><ul><li>ziplist编码，底层实现为压缩链表</li><li>linkedlist编码，底层实现为双端链表</li></ul><h4 id="哈希对象"><a href="#哈希对象" class="headerlink" title="哈希对象"></a>哈希对象</h4><ul><li>ziplist编码，底层实现为压缩尾端，K-V相邻存储，K在前，V在后，新节点追加在列表尾端</li><li>hashtable编码，底层实现为字典</li></ul><h4 id="集合对象"><a href="#集合对象" class="headerlink" title="集合对象"></a>集合对象</h4><ul><li>intset编码，整数集合，用于存储整数值</li><li>hashtable编码，底层实现为dict，K作为集合中的元素，V为null</li></ul><h4 id="有序集合对象"><a href="#有序集合对象" class="headerlink" title="有序集合对象"></a>有序集合对象</h4><ul><li>ziplist编码，每个原始使用两个相邻的压缩列表节点，第一个存储元素，第二个存储分值用于排序</li><li>skiplist编码，同时使用跳表和字典作为实现。<ul><li>跳表升序存储元素和值</li><li>字典的K为元素的成员，V为元素的分值，可以以O(1)的复杂度来查询</li><li>跳表和字典通过指针共享成员，不会浪费额外的内存</li></ul></li></ul><h4 id="内存回收"><a href="#内存回收" class="headerlink" title="内存回收"></a>内存回收</h4><p>通过维护引用计数确定对象的引用计数信息，计数为0时释放内存</p><h4 id="对象共享"><a href="#对象共享" class="headerlink" title="对象共享"></a>对象共享</h4><p>对于两个值相同的对象，通过指针的方式来进行共享，节省内存</p><p>新的键通过引用指向旧的键的对象，对象的引用计数+1</p><p>值得注意的是，在redis初始化服务器时，会创建[0,9999]个字符串对象，用于进行引用，在5.0.14版本时，引用计数为INT_MAX-1</p><h2 id="数据库基础架构"><a href="#数据库基础架构" class="headerlink" title="数据库基础架构"></a>数据库基础架构</h2><p>Redis主要分为redisClient和redisServer以及AOF&#x2F;RDB，事件处理主要部分：</p><ul><li>redisClient：用于保存数据库的客户端的连接状态，如目标数据库号等</li><li>redisServer：保存数据库的服务器状态，维护当前的数据库等</li><li>AOF&#x2F;RDB：持久化数据</li><li>事件处理：监听套接字&#x2F;定时，接受事件调用对应的文件事件处理器</li></ul><p> redisClient结构体维护着客户端信息，如当前整连接的数据库，套接字描述符等</p><p>redisServer结构体维护着服务端的信息，连接了的客户端状态，其中redisDb维护者数据库空间，通过两个字典保存当前所有的K-V和存在过期时间的K-V</p><h3 id="RDB持久化"><a href="#RDB持久化" class="headerlink" title="RDB持久化"></a>RDB持久化</h3><p>RDB文件中的databases保存的为数据库<strong>状态</strong>，即原始的键值对，以及一些额外的附加信息，相比之下：</p><ul><li>MySQL中的redolog记录的为物理操作，即对哪个数据进行了什么修改</li><li>MySQL中的binlog记录的为语句的原始逻辑 statement形势下记录的为原始是sql，row记录的为执行的操作</li><li>Redis中的AOF记录的Redis的命令，类似于MySQL中statement 形式的binlog</li></ul><p>通过对redolog binlog RDB AOF的比较，可以看出Redis中持久化和MySQL中日志的异同：</p><ul><li>二者都可以对数据库进行记录，并且恢复处当前的状态</li><li>但是日志系统是力求记录每一时刻的状态，最终能够恢复到特定(redolog 断电)&#x2F;指定(binlog 备份归档)的状态</li><li>而持久化侧重于将内存中的状态保存到磁盘当中，磁盘当中只存在一个状态，即便是AOF会和binlog一样，一条条的记录命令语句，但是也会因为AOF重写，重新读取状态生成新的AOF导致AOF语句之间的逻辑变得模糊</li></ul><h4 id="创建与载入"><a href="#创建与载入" class="headerlink" title="创建与载入"></a>创建与载入</h4><p>可以通过SAVE和BGSAVE两种命令创RDB文件，二者均为调用rdbSave()函数来进行RDB文件的创建，只不过BGSAVE(background) 是通过fork处一个子进程来进行创建，（fork相关知识点详见操作系统）</p><ul><li>BGSAVE在创建RDB文件的期间，Redis服务器在此期间依旧可以处理客户端发来的请求</li><li>BGSAVE执行时，为防止父子进程之间竞争，SAVE命令被禁止</li><li>AOF重写命令BGWRITEAOF会在BGSAVE执行结束后再执行</li></ul><p>载入：在服务器启动时，会载入RDB文件来恢复redis状态，并且服务器会阻塞，直至载入完成。但是RDB的优先级低于AOF文件，在开启了AOF持久化后会优先从AOF中载入</p><h4 id="保存"><a href="#保存" class="headerlink" title="保存"></a>保存</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">redisServer</span> &#123;</span><br>    <span class="hljs-comment">// ...</span><br>    <span class="hljs-comment">//条件数组</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">saveparm</span> *<span class="hljs-title">saveparams</span>;</span><br>    <span class="hljs-comment">// 修改计数器，距上一次rdbSave后修改次数</span><br>    <span class="hljs-type">long</span> <span class="hljs-type">long</span> dirty;<br>    <span class="hljs-comment">// 上次执行保存的时间</span><br>    <span class="hljs-type">time_t</span> lastsave;<br>&#125;<br></code></pre></td></tr></table></figure><p>通过配置条件来进行自动保存，在redisServer结构体中，通过saveparms数组来维护不同的保存条件</p><p>saveparm结构如下</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">saveparm</span> &#123;</span><br>    <span class="hljs-comment">// 时间段</span><br>    <span class="hljs-type">time_t</span> seconds;<br>    <span class="hljs-comment">// 修改次数</span><br>    <span class="hljs-type">int</span> changes;<br>&#125;<br></code></pre></td></tr></table></figure><p>表明在seconds时间段内,如果修改了changes次，则触发自动写RDB</p><p>serverCron函数每过100ms被调用一次，遍历saveparms判断条件是否满足</p><h4 id="文件结构"><a href="#文件结构" class="headerlink" title="文件结构"></a>文件结构</h4><p>二进制文件，并非C字符串</p><img src="“Redis设计与实现”.assets/image-20220724163629597.png" alt="image-20220724163629597" style="zoom:67%;" /><ul><li>REDIS为五个字符，用于辨别是否为RDB文件</li><li>db_version为当前的版本号</li><li>databases为数据库的状态信息</li><li>EOF:End Of Flie,一字节的标志位</li><li>check_sum:校验和，由前四个字段计算出</li></ul><p><strong>databases</strong></p><p>结构：</p><p>databases:</p><ul><li>selectdb：标志下一位为一个数据库号码</li><li>db_number：数据库号码</li><li>key_value_pairs：键值对 type为对象类型或底层编码，EXPIRETIME_MS为标志下一位为过期时间<ul><li>不带过期时间：<img src="“Redis设计与实现”.assets/image-20220724170955909.png" alt="image-20220724170955909" style="zoom: 33%;" /></li><li>带过期时间：<img src="“Redis设计与实现”.assets/image-20220724171042314.png" alt="image-20220724171042314" style="zoom:33%;" /></li></ul></li></ul><p>对于不同的type,value的具体结构也不同</p><h3 id="AOF持久化"><a href="#AOF持久化" class="headerlink" title="AOF持久化"></a>AOF持久化</h3><h4 id="写入与载入"><a href="#写入与载入" class="headerlink" title="写入与载入"></a>写入与载入</h4><p><strong>写入</strong></p><p>redisServer维护一个aof_buf,服务器在执行完一个写命令之后， 会以协议格式将被执行的写命令追加到服务器状态的 <code>aof_buf</code> 缓冲区的末尾：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">redisServer</span> &#123;</span><br><br>    <span class="hljs-comment">// AOF 缓冲区</span><br>    sds aof_buf;<br><br>    <span class="hljs-comment">// ...</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>服务器进程为一个事件循环(详情见事件)，每次循环结束之前，调用<code>flushAppendOnlyFile</code>把缓冲区中的内容写到硬盘中的AOF文件当中<code>flushAppendOnlyFile</code>的行为由appendfsync来控制：</p><ul><li>always:把缓冲区中所有的都写入并同步到AOF文件当中</li><li>ererysync:缓冲区内容全部写入，如果距上次同步事件超过一秒，再进行同步</li><li>no:全部写入，但是同步过程交给操作系统决定</li></ul><blockquote><p>在现代os中，为了提高文件的写入操作，当用户调用到write函数将数据写入文件时，os先将数据写入到一个内存缓冲区里，正常是等到缓冲区满了或是规定时间到了，才真正地将缓冲区里的数据写入磁盘，这时才是持久化完成。</p></blockquote><p><strong>载入</strong></p><p>创建一个不带网络连接的伪客户端，执行AOF中保存的命令</p><h4 id="AOF重写"><a href="#AOF重写" class="headerlink" title="AOF重写"></a>AOF重写</h4><p>对于多条对同一个K-V进行操作的命令，可以精简合并为一条命令，从而减少空间占用</p><p>具体实现方式为重新读取当前服务器中数据库的状态，重新生成一个AOF文件，替代原有的AOF文件</p><p>如果列表、哈希表、集合、有序集合中的元素超出了REDIS_AOF_REWRITE_ITEMS_PER_CMD的值时，会分成多条语句来进行记录</p><h4 id="后台重写"><a href="#后台重写" class="headerlink" title="后台重写"></a>后台重写</h4><p>创建一个子进程来进行AOF重写，父进程可以继续处理命令请求</p><p>再子进程进行后台AOF重写时，创建了一个AOF重写缓冲区，新添加的命令会同时发送给AOF缓冲区和AOF重写缓冲区，在子进程完成重写后，再将AOF重写缓冲区中的内容写入到新的AOF文件当中</p><h3 id="事件"><a href="#事件" class="headerlink" title="事件"></a>事件</h3><p>Redis为事件驱动程序，即针对不同的事件来进行响应和处理</p><p>主要分为文件事件和时间事件：</p><ul><li>文件事件：服务器对套接字操作的抽象，服务器和客户端之间通信便会产生响应的文件事件，服务器监听套接字并进行处理</li><li>时间事件：服务器中的定时执行的函数</li></ul><h4 id="文件事件"><a href="#文件事件" class="headerlink" title="文件事件"></a>文件事件</h4><p>通过I&#x2F;O多路复用来监听多个套接字，将套接字放到一个队列之中，并向文件事件分派器传送产生了事件的套接字，文件事件分派器再调用对应的事件处理器</p><blockquote><p>I&#x2F;O 多路复用程序可以监听多个套接字的 <code>ae.h/AE_READABLE</code> 事件和 <code>ae.h/AE_WRITABLE</code> 事件， 这两类事件和套接字操作之间的对应关系如下：</p><ul><li>当套接字变得可读时（客户端对套接字执行 <code>write</code> 操作，或者执行 <code>close</code> 操作）， 或者有新的可应答（acceptable）套接字出现时（客户端对服务器的监听套接字执行 <code>connect</code> 操作）， 套接字产生 <code>AE_READABLE</code> 事件。</li><li>当套接字变得可写时（客户端对套接字执行 <code>read</code> 操作）， 套接字产生 <code>AE_WRITABLE</code> 事件。</li></ul><p>I&#x2F;O 多路复用程序允许服务器同时监听套接字的 <code>AE_READABLE</code> 事件和 <code>AE_WRITABLE</code> 事件， 如果一个套接字同时产生了这两种事件， 那么文件事件分派器会优先处理 <code>AE_READABLE</code> 事件， 等到 <code>AE_READABLE</code> 事件处理完之后， 才处理 <code>AE_WRITABLE</code> 事件。</p><p>这也就是说， 如果一个套接字又可读又可写的话， 那么服务器将先读套接字， 后写套接字。</p></blockquote><p><strong>文件事件处理器</strong></p><ul><li>连接应答处理器</li><li>命令请求处理器</li><li>命令回复处理器</li></ul><h4 id="时间事件"><a href="#时间事件" class="headerlink" title="时间事件"></a>时间事件</h4><p>主要分为定时事件和周期事件</p><p>时间事件属性：</p><ul><li>id</li><li>when</li><li>timeProc:时间事件处理器</li></ul><p><strong>实现：</strong></p><p>服务器将所有的时间事件都放置在一个无序链表中，时间事件执行器运行时，遍历整个链表，，查找已经到达的时间事件，调用对应的处理器</p><h3 id="客户端"><a href="#客户端" class="headerlink" title="客户端"></a>客户端</h3><h4 id="客户端属性"><a href="#客户端属性" class="headerlink" title="客户端属性"></a>客户端属性</h4><p>redis客户端的信息主要靠redisClient结构体进行维护，其中保存着所连接的数据库号：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">redisClient</span> &#123;</span><br>    <span class="hljs-comment">// 当前正在连接的数据库，指向数据库数组中的一个元素</span><br>    redisDb *db;<br>    <span class="hljs-comment">// 正在使用的套接字描述符 -1为伪客户端 大于-1的整数为正常客户端</span><br>    <span class="hljs-type">int</span> fd;<br>    <span class="hljs-comment">// 客户端名称 正常情况下没名字，可手动设置</span><br>    robj *name<br>    <span class="hljs-comment">// 客户端角色和客户端当前处于的状态</span><br>    <span class="hljs-type">int</span> flags;<br>    <span class="hljs-comment">// 输入缓冲区 存放命令</span><br>    sds querybuf;<br>    <span class="hljs-comment">// 命令参数</span><br>    robj **argv;<br>    <span class="hljs-comment">// 命令参数的个数</span><br>    <span class="hljs-type">int</span> argc;<br>    <span class="hljs-comment">// 命令的实现函数</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">redisCommand</span> *<span class="hljs-title">cmd</span>;</span><br>    <span class="hljs-comment">// 存放给客户端的回复 固定长度</span><br>    <span class="hljs-type">char</span> buf[REDIS_REPLY_CHUNK_BYTES];<br>    <span class="hljs-comment">// 当前已经使用的字节数量</span><br>    <span class="hljs-type">int</span> bufpos;<br>    <span class="hljs-comment">// 可变大小缓冲区</span><br>    <span class="hljs-built_in">list</span> *reply<br>    <span class="hljs-comment">// 身份验证位，1为通过验证，0为未通过验证，则只能调用AUTH命令</span><br>    <span class="hljs-type">int</span> authenticated;<br>    <span class="hljs-comment">// 时间相关参数</span><br>    <span class="hljs-comment">// 客户端创建的时间</span><br>    <span class="hljs-type">time_t</span> ctime;<br>    <span class="hljs-comment">// 最后一次互动的时间</span><br>    <span class="hljs-type">time_t</span> lastinteration;<br>    <span class="hljs-comment">// 缓冲区第一次达到软限制的时间</span><br>    <span class="hljs-type">time_t</span> obuf_soft_limit_reached_time;<br>    <br>    <span class="hljs-comment">// ...</span><br>    <br>&#125;<br></code></pre></td></tr></table></figure><p><strong>解释</strong></p><ul><li><p>fd ：</p><ul><li>-1为伪客户端，通过 AOF文件或lua脚本创建</li><li>大于-1的整数为正常客户端，通过套接字和服务器通信</li></ul></li><li><p>flags：可以为单个标志，也可为多个标志，通过二进制的各个位来表述是否处于当前的状态</p></li><li><p>querybuf：根据客户端发送的命令请求动态变化大小，但是最大不超过1GB，否则关闭客户端</p></li><li><p>参数相关：</p><ul><li>argv 当前命令解析成的参数数组 </li><li>argc 参数数量</li></ul><p>例：<code>SET KEY VALUE</code>解析成：</p><p><img src="/%E2%80%9CRedis%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0%E2%80%9D/image-20220725210114777.png" alt="image-20220725210114777"></p></li><li><p>命令实现函数：在命令解析完成后，通过argv[0],即具体的命令动作，区查询命令表，找到对应的实现函数，将其赋给cmd</p><p><img src="/%E2%80%9CRedis%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0%E2%80%9D/image-20220725211303884.png" alt="image-20220725211303884"></p></li><li><p>回复缓冲区：存储回复给客户端的命令，较短的使用固定长度的字节数组，REDIS_REPLY_CHUNK_BYTES默认大小为16*1024，即大小为16KB，当固定数组存慢了之后便改用可变长度，使用reply链表，串联起多个字符串对象</p></li></ul><h4 id="创建与关闭"><a href="#创建与关闭" class="headerlink" title="创建与关闭"></a>创建与关闭</h4><p><strong>创建：</strong></p><p>客户端使用connect函数连接，服务器调用连接事件处理器，为客户端创建连接状态，，将连接状态追加到 clients链表的尾部</p><p><strong>关闭：</strong></p><p>客户端因为多种原因被关闭，如客户端进程退出或被杀死，不符合协议格式的命令请求，或者输出，输入缓冲区大小超出限制</p><p>输出硬性限制(hard limit)：超出之后直接关闭客户端</p><p>输出软性限制(soft limit)：超出之后如果在指定时间内不再超出，则不会关闭客户端</p><h3 id="服务器"><a href="#服务器" class="headerlink" title="服务器"></a>服务器</h3><h4 id="服务器属性"><a href="#服务器属性" class="headerlink" title="服务器属性"></a>服务器属性</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">redisServer</span> &#123;</span><br>    <span class="hljs-comment">// db数组，元素为redisDb结构，代表一个数据库</span><br>    redisDb *db;<br>    <span class="hljs-comment">// 数据库数量根据配置的database选项决定</span><br>    <span class="hljs-type">int</span> dbnum;<br>    <span class="hljs-comment">// 秒级unix时间戳</span><br>    <span class="hljs-type">time_t</span> unixtime;<br>    <span class="hljs-comment">// 毫秒级unix时间戳</span><br>    <span class="hljs-type">long</span> <span class="hljs-type">long</span> mstime;<br>    <span class="hljs-comment">// 内存峰值</span><br>    <span class="hljs-type">size_t</span> stat_peak_memory；<br>    <br>&#125;<br></code></pre></td></tr></table></figure><h4 id="数据库键空间"><a href="#数据库键空间" class="headerlink" title="数据库键空间"></a>数据库键空间</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">redisDb</span> &#123;</span><br>    <span class="hljs-comment">// ...</span><br>    <span class="hljs-comment">// 数据库键空间,保存数据库中所有的K-V</span><br>    dict *dict;<br>    <span class="hljs-comment">// 过期字典，保存过期时间</span><br>    dict *expires;<br>&#125;<br></code></pre></td></tr></table></figure><p>dict和expires为两个键指向同一个键对象，不造成空间浪费</p><p>CRUD即为对这两个字典进行操作</p><p><strong>过期策略</strong></p><p>单独定时删除对CPU不友好，占用CPU事件检查，单独惰性删除对内存不友好，过期的键仍存在内存当中，因此Redis采用二者相结合的方式</p><p>惰性删除：Redis在命令执行前先调用<code>expireIfNeeded</code>函数对输入键进行检查，过期则删除，否则则不采取动作，类似加了一层拦截器，判断完键的状态再放行</p><p>定期删除：规定时间内分多次遍历各个数据库，随机检查一部分键的过期时间，删除过期键</p><p>RDB文件不会保存过期但还未从内存中删除的键</p><p>载入时：</p><ul><li>主服务器模式下运行时，过期的键不进行载入</li><li>从服务器模式下运行时，所有的键全部进行载入</li></ul><p>AOF在键过期时追加一条命令显示记录键被删除，重写时不会对过期的键进行重写</p><h4 id="命令请求"><a href="#命令请求" class="headerlink" title="命令请求"></a>命令请求</h4><ol><li>用户输入命令，客户端将命令转换为协议，然后将协议发送给服务器</li><li>协议保存到缓冲区之后对其进行解析，填充argv和argc</li><li>调用命令执行器<ol><li>通过argv[0]来查找命令表找到对应的命令，并将对应的命令保存到cmd当中</li><li>执行预备操作，检查cmd指针是否为null,argv是否大于等于三等</li><li>调用相关实现函数</li><li>执行后续工作，慢查询，AOF等</li></ol></li><li>将命令回复发送给客户端，客户端接收打印显示</li></ol><h2 id="多机数据库的实现"><a href="#多机数据库的实现" class="headerlink" title="多机数据库的实现"></a>多机数据库的实现</h2><p>Redis主从配置：</p><figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs stata">2.2配置文件<br>配置主redis配置vim redis_m.<span class="hljs-keyword">conf</span><br>bind 0.0.0.0 <span class="hljs-comment">//允许任意IP连接 可以根据需要调整本机127.0.0.1 或者任意指定ip</span><br>port 10001 <span class="hljs-comment">//最好调整下默认端口号 10000-60000均可</span><br>daemonize yes  <span class="hljs-comment">//开启后台启动 使用docker时配置为no，否则会和-</span><br>pidfile /<span class="hljs-keyword">var</span>/<span class="hljs-keyword">run</span>/redis_10001.pid  <span class="hljs-comment">//独立的和端口号直接挂钩即可</span><br>logfile <span class="hljs-string">&quot;/*/10001.log&quot;</span> <span class="hljs-comment">//自定义独立日志文件即可</span><br><span class="hljs-keyword">dir</span> <span class="hljs-string">&quot;/*/10001&quot;</span> <span class="hljs-comment">//自定义独立目录即可</span><br>requirepass <span class="hljs-string">&quot;123456&quot;</span> <span class="hljs-comment">//配置redis密码</span><br><br>配置从redis配置  vim redis_s.<span class="hljs-keyword">conf</span><br>主redis配置均需要同样的配置<br>另新增<br>masterauth <span class="hljs-string">&quot;123456&quot;</span> <span class="hljs-comment">//主redis的密码</span><br>replicaof 127.0.0.1 10001 <span class="hljs-comment">//ip + 端口号 注意5.0版本前是slaveof命令</span><br><br></code></pre></td></tr></table></figure>]]></content>
    
    
    
    <tags>
      
      <tag>数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>操作系统</title>
    <link href="/2022/08/08/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    <url>/2022/08/08/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/</url>
    
    <content type="html"><![CDATA[<h1 id="操作系统"><a href="#操作系统" class="headerlink" title="操作系统"></a>操作系统</h1><h2 id="虚拟化"><a href="#虚拟化" class="headerlink" title="虚拟化"></a>虚拟化</h2><h3 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h3><p>进程：运行中的程序</p><p>进程的状态：(三状态模型)</p><ul><li>运行</li><li>就绪</li><li>阻塞</li></ul><p>上下文切换：当一个进程停止时，将其寄存器中的值保存到物理内存当中，通过恢复寄存器（将真实值从物理内存中放回寄存器当中），恢复运行该进程</p><p>进程控制块：PCB，储存进程的相关信息</p><h3 id="受限直接执行-LDE"><a href="#受限直接执行-LDE" class="headerlink" title="受限直接执行 LDE"></a>受限直接执行 LDE</h3><ol><li>CPU直接运行程序</li><li>操作系统掌控全局，对运行的程序加以限制</li></ol><p>受保护的控制权转移:硬件分别提供不同的执行模式给操作系统</p><ul><li>用户模式：在用户模式下，运行的代码会受限制，如果进行如IO等特权操作，操作系统会抛出异常，从而终止进程</li><li>内核模式：该模式下代码可以做任何想做的事，包括受限命令</li></ul><p>通过陷入跳入内核并且将特权级别提升到内核模式，以及从陷阱返回来回到用户模式</p><p>内核通过设置陷阱表来告诉陷阱该跳转到的地址和要执行的代码，操作系统会告诉硬件陷阱表的位置。设置陷阱表本身也是一个特权指令</p><p><strong>进程之间的切换</strong></p><p>如果一个进程正在运行，那么操作系统则未在运行，操作系统对全局的掌控失效</p><ul><li><p>协作方式：操作系统被动调用，相信进程，对于运行时间过长的进程会定期放弃CPU，操作系统接管</p></li><li><p>非协作方式：借助硬件，设置时钟中断，因此每过一段时间操作系统便可重新接管CPU</p></li></ul><p><strong>恢复和上下文切换</strong></p><p>A-&gt;B</p><ol><li>时钟中断</li><li>将A的寄存器保存至内核栈（A）</li><li>切换至内核模式<ul><li>将A的寄存器保存至A的进程结构当中</li><li>将B的寄存器从B的进程结构中恢复</li><li>从陷阱返回</li></ul></li><li>从B的内核栈之中恢复B的寄存器</li><li>转向用户模式</li></ol><h3 id="进程调度"><a href="#进程调度" class="headerlink" title="进程调度"></a>进程调度</h3><p><strong>调度指标：</strong></p><p>周转时间：$T_{完成时间}-T_{到达时间}$ </p><p>带权周转时间：$T_{周转时间}&#x2F;T_{占用CPU时间}$</p><p>响应时间：$T_{首次运行}-T_{到达时间}$</p><p><strong>FIFO</strong></p><p>先进先出，不多解释</p><p><strong>SJF</strong></p><p>短任务优先</p><p>当一个进程结束后，在已经到达的进程中挑选运行时间最短的执行</p><p>非抢占式</p><p><strong>STCF</strong></p><p>无需等一个进程结束，直接判断已经到达的中的最短的进程</p><p>抢占式</p><p><strong>RR</strong></p><p>轮转</p><p>切成一个个时间片，时间片必须为中断周期的倍数</p><p>每个进程运行一个时间片</p><p>需要设置合理的时间片长度，果断频繁上下文切换的开销过大，过长，失去轮转的意义</p><h3 id="多级反馈队列"><a href="#多级反馈队列" class="headerlink" title="多级反馈队列"></a>多级反馈队列</h3><p>MLFQ 中拥有许多个独立的队列，每个队列的优先级不同，先执行优先级较高的队列中的任务，对应优先级相同的任务采用轮转运行</p><p><strong>初步制定规则</strong>：</p><ul><li><p>1 A的优先级＞B的优先级，执行A</p></li><li><p>2 AB优先级相同，采用轮转的方式进行执行</p></li><li><p>3 工作进入系统时先放在最高优先级上</p></li><li><p>4a 执行完一个时间片，优先级降低，移入下一个队列</p></li><li><p>4b 如果进程主动放弃CPU，则不降低其优先级</p></li></ul><p>对应长进程，会慢慢降低其优先级，将CPU有限给其他进程使用，防止长期占用</p><p>对于IO密集型进程，IO交互时会主动放弃CPU，因此一直保持较高的优先级</p><p>缺点：</p><ul><li>太多交互型的进程会导致其他进程处于饥饿状态</li><li>如果程序中设计进程一直主动进行放弃CPU，那么则可以欺骗操作系统，一直占有CPU</li></ul><p><strong>改进1</strong></p><p>规则5：经过一段S时间，就把所有的进程全部加入最高优先级</p><p>S如果太高，则长进程饥饿，太短，对IO型进程不友好</p><p><strong>改进2</strong></p><p>重写规则4，如果一个进程完成了一定的时间配额，无论中途是否放弃过CPU，均降低其优先级</p><p>因此最后的规则为：</p><ul><li><p>1 A的优先级＞B的优先级，执行A</p></li><li><p>2 AB优先级相同，采用轮转的方式进行执行</p></li><li><p>3 工作进入系统时先放在最高优先级上</p></li><li><p>4 如果一个进程完成了一定的时间配额，无论中途是否放弃过CPU，均降低其优先级</p></li><li><p>5 经过一段S时间，就把所有的进程全部加入最高优先级</p></li></ul><p>同时，对于不同队列分配不同长度的时间片，高优先级拥有较短的时间片</p><h2 id="内存管理"><a href="#内存管理" class="headerlink" title="内存管理"></a>内存管理</h2><h3 id="抽象：地址空间"><a href="#抽象：地址空间" class="headerlink" title="抽象：地址空间"></a>抽象：地址空间</h3><p>操作系统为程序提供一个物理内存的抽象，叫做<strong>地址空间</strong>，运行的程序感受不到这个虚拟化的过程，作为程序员，我们所操作和感知到的内存也并非真实的物理内存，因此程序的地址总是从0开始</p><p>一个地址空间包含一个程序需要的所有的内存状态，主要包括以下三个区域：</p><ul><li><p>代码区：存储程序运行的代码</p></li><li><p>堆区：动态分配的内存，由用户手动管理，如malloc或者new分配的内存</p></li><li><p>栈区：保存函数的调用信息，分配空间给局部变量，函数返回值等，由操作系统自动管理</p></li></ul><p>在地址空间中，程序代码和堆位于地址的低位，并且分配时由低位向高位进行分配，栈位于地址的高位，分配时从高位向低位分配，中间的为未分配区域，留给栈和堆进行增长</p><p>在由地址空间向物理地址的映射过程当中，主要解决的就是如何合理分配空间，使中间的未分配的区域尽可能小的占用实际物理内存</p><h3 id="机制：地址转换"><a href="#机制：地址转换" class="headerlink" title="机制：地址转换"></a>机制：地址转换</h3><p>将指令中的虚拟地址转换到数据实际存储的地址，有点类似内存的寻址过程</p><p><strong>动态重定位(基于硬件)</strong> </p><p>实现重定位CPU需要两个寄存器，基址寄存器和限制寄存器</p><p>基址寄存器用于存放起始位置，虚拟地址的位置加上基址寄存器中的值即可得到真实的物理地址</p><p>界限寄存器确保这个地址在进程地址的空间范围内，在虚拟地址与基址寄存器求和前就进行检查，如果超过了这个界限，或者为负数时，则会引发异常</p><ol><li>创建进程时，操作系统在物理内存空间中找到一个能够容纳当前位置，并且将其标记为已用</li><li>进程终止时，对当前进程所占用的内存空间进行释放，归还为空闲列表，清除当前区域的相关数据结构，比如说一个记录着当前空间大小和下一个空间位置的结构体</li><li>上下文切换时，由于CPU只有一个基址寄存器和界限寄存器，因此对于不同进程之间的切换，应当保存当前寄存器中的内容到每个进程都有的结构当中，如PCB等，供上一个进程以后使用，并且将当前进程的值加载进寄存器当中。在移动地址空间时，操作系统会把新的地址空间重新更新到寄存器当中</li></ol><h3 id="分段"><a href="#分段" class="headerlink" title="分段"></a>分段</h3><p>分段主要用于解决的就是栈和堆之间的一大块空域区域的问题。</p><p>在代码块，栈，堆这种模型下，将其分为三个段，每个段设置一对基址寄存器和界限寄存器，在进行地址映射时，分别将这三个段映射到物理地址当中</p><p>在进行映射的过程当中，由于段在虚拟地址中的起始位置不再是0，因此在进行地址映射时，应当考虑虚拟地址中段本身的偏移量，比如：</p><p>一个数据在虚拟地址中的地址为4200，而段的起始地址为4096，因此考虑段在虚拟地址中的偏移量，真正进行映射的地址(转换为段起始从0开始的地址)即为4200-4096&#x3D;104，将104加上基址寄存器中的值得到的才为真正的物理地址，如果不考虑段的偏移量&#x2F;起始地址，那么则与为分段无异，失去分段的意义</p><p><strong>段的引用</strong></p><p>用前几位表示段号，后几位为段内的偏移量，类似于cache的块号和块内寻址，在界限检查时，只需要将段内偏移量与界限寄存器进行比较即可</p><p>如一个14位的地址，前两位的表示段号，那么则为00-03四个段，后12位为段内偏移量</p><p><strong>栈的反向增长</strong></p><p>为了解决栈的反向增长，在段寄存器中新加一位是否进行反向增长用于区分，反向增长则为反向偏移</p><p><strong>共享</strong></p><p>部分内存可以共享给不同进程使用，但是为了防止不同进程之间的相互影响，在段寄存器当中对段加上保护位，来标识读写权限，代码段的权限位可读可执行那么则可映射到多个虚拟空间，供多个进程使用，但是每个进程又认为自己独占了这个内存</p><p><strong>外部碎片</strong></p><p>不同的段的大小不同，对物理内存空间进行划分切割，导致一些小的空间无法放下任何的段，成为外部碎片</p><p>解决方法</p><ul><li>重新分配，紧凑内存，但是成本很高</li><li>内存分配算法，成本低，但是无法完全解决问题，总会留下碎片</li></ul><h3 id="空闲空间管理"><a href="#空闲空间管理" class="headerlink" title="空闲空间管理"></a>空闲空间管理</h3><p>主要解决分配过程中的外部碎片的问题</p><p><strong>底层机制</strong></p><p>堆上管理空闲空间的数据结构为空闲链表</p><p>分配空间时找到一个合适大小的空间进行分割，一部分分配，剩余的部分保持未分配的空闲状态，同时碎片也就是这么得来的</p><p>释放时一是对当前内存空间进行回收，然后对相邻的空间空间进行合并，恢复成大的空间</p><p><strong>实际于内存中</strong></p><p>当将地址分配给用户时，会同时分配一个头块，存储一定的信息，如当前段的大小和一个进行完整性检查的幻数，在对空余块进行分配时，同样会分配一个头块，其中包含着用于标识剩余空间大小的size和下一个段的位置next</p><p>分配和释放时，需要对next进行修改，指向新的下一个内存块</p><p><strong>算法策略</strong></p><ul><li>最优匹配：遍历空间链表，寻找大小最合适的区域，尽可能小，但是问题时遍历性能损耗高，并且会产生小的碎片</li><li>最差匹配：遍历，寻找尽可能大的，会产生过量的碎片，实际表现确实是差</li><li>首次匹配：找到第一个就进行分配，有速度优势</li><li>下次匹配：相比首次匹配多维护一个指针，每次从上一次分配的结果出继续寻找空间进行分配</li><li>伙伴系统：分配时对空间不断进行二分，一分为二得到的两个空间称为<strong>伙伴</strong>直至获得一个能够分配的最小空间，释放时检查该空间的伙伴是否为空闲，如果为空闲则将二者合并，获得一个更大的空间，这个过程一直不断向上回溯合并到不能合并为止</li></ul><h3 id="分页"><a href="#分页" class="headerlink" title="分页"></a>分页</h3><p>将空间分为大小相同的的区间，每一个区间称为一个页</p><p>与段相同，采用页号加页内偏移的方式</p><p>定义：</p><ul><li>页表：实现虚拟页和实际物理页之间进行地址转换的数据结构，每个进程一个</li><li>VPN：虚拟页面号</li><li>PFN：物理页号</li><li>PTE：页表格条目(页表项)，用于保存物理地址转换和任何其他有用的信息，存储在页表当中，每一对虚拟和物理地址之间的映射即每一页均需要一个页表项。PTE其中可以存储有效位、保护位、存在位、参考位（追踪页是否被访问）等信息</li><li>TLB：地址转换旁路缓冲存储器，教材上叫做快表，类似于CPU中cache的硬件缓存</li><li>页目录：管理页表中的页，内部的元素成为页目录项（与页表和页表项相对应）</li><li>PDE：页目录项，至少含有PFN和有效位</li></ul><p><strong>寻址过程：</strong></p><p>​已知虚拟地址，通过前几位获得VPN，通过VPN在进程中的页表中去提取适当的页表项，执行转换，得到PFN，PFN在进行左移和偏移量进行拼接得到真实的物理地址</p><h4 id="TLB"><a href="#TLB" class="headerlink" title="TLB"></a>TLB</h4><p><strong>TLB算法流程</strong></p><p>如果在虚拟地址中提取页号，检查TLB中是否有地址映射，命中那么则直接从TLB中提取PFN，与偏移量组合得到实际的物理地址，于是可以访问内存。如果未命中则将该页从页表中提取到TLB中，避免下次访问该页的其他地址时未命中（通常系统会访问连续的地址空间）</p><p><strong>TLB未命中时的处理</strong></p><p>复杂指令集时期的计算机由硬件全权负责，未命中时硬件遍历页表，寻找正确的页表项，更新TLB</p><p>较为现代的操作系统当中通过软件处理，未命中时，硬件系统抛出一个一场，此时操作系统提升至内核模式，执行陷阱处理程序（处理TLB未命中的程序），通过特权指令修改更新TLB，从陷阱返回，此时硬件再次访问则会命中</p><p><strong>TLB中的内容</strong></p><p>VPN | PFN | 其他位</p><p>其他位包括 有效位，保护位，脏位等</p><p><strong>上下文切换时的TLB</strong></p><p>一个TLB需要针对多个进程，因此在上下文切换时，上一个进程的相关信息则没有任何意义，一共有两种处理方式：</p><ul><li>上下文切换时清空TLB，但是频繁进行上下文切换时开销很高</li><li>跨上下文切换的TLB共享，添加地址空间标识符，用于区别不同的进程</li></ul><p><strong>替换策略</strong></p><p>与CPU的cache相似，采用（最近最少使用）LRU和随机替换</p><h4 id="解决页表项过多的问题"><a href="#解决页表项过多的问题" class="headerlink" title="解决页表项过多的问题"></a>解决页表项过多的问题</h4><p>正如上面所述，每一个页均需要一个页表项来进行地址映射，因此如果VPN为20位，那么则需要$2^{20}$个页表项，虽然这些页表项并不会全部有效可能为分配，但是仍存在于页表中，因此页表则会有$2^{20}$ 个项，对应的空间消耗极大。为了减少空间消耗，主要有以下几种方案：</p><ul><li>更大的页</li><li>段页混合方法</li><li>多级页表</li></ul><p><strong>更大的页</strong></p><p>使用更大的页可以减少PTE的数量，但随之每页会产生浪费，造成较大的内部碎片</p><p><strong>段页混合</strong></p><p>不在位整个进程空间提供页表，而是为每个逻辑段提供页表，如果按照代码区、堆、栈分段的话那么我们有三个页表，混合方式中我们依旧使用段中的基址寄存器和界限寄存器</p><p>在这个例子当中，段页混合之后，此时我们的虚拟地址前两位用于确定地址引用哪个段，后面剩余的分别为VPN和偏移量</p><p>分段之后，只针对段内的地址进行页表项的分配，堆和栈之间未使用的部分不再设置页表项，从而有效地节省了页表项，减少内存消耗</p><p>缺点：</p><p>分段并不像分页那么灵活，如果有一个大而稀疏的堆，我们需要对堆所在的整个段进行页表项的分配，依旧会产生较大的页表浪费</p><p><strong>多级页表</strong></p><p>基本思想：将页表分为多个页大小的单元，如果该页中的页表项全为无效，那么就完全不分配该页的页表。使用新的数据结构<strong>页目录</strong>来管理页表的页</p><p>页目录和页目录项对应着页表和页表项，页表和页目录用于管理具体的地址映射，页目录和页目录项用于管理页表，如果PDE有效，那么PDE指向中的页页表其中至少有一页是有效的，即该PDE指向的页中，至少有一个有效的PTE</p><p>不使用多级页表是，需要在内存找找到一个连续的空间，令页表连续的驻留在物理内存中，但是使用了多级页表之后，只需要保证能找到一段连续空间来存储页目录即可（比页表要小得多），页表的页通过页目录的指向则可以放在内存中的任意位置</p><p>缺点：</p><p>时间换空间，多页页表的查找显然要比线性页表查找复杂得多，同时TLB处理未命中时，也需要从内存中加载两次（一次页目录，一次PTE）</p><p>  TLB：如果TLB命中时则与之前线性页表一致，从TLB中获取即可，只有未命中时，才需要进行完整的多级页表查找</p><h3 id="虚拟内存"><a href="#虚拟内存" class="headerlink" title="虚拟内存"></a>虚拟内存</h3><h4 id="机制"><a href="#机制" class="headerlink" title="机制"></a>机制</h4><p>在磁盘上开辟一块空间用于物理内存的移入和移除，通常以页大小为单元</p><p><strong>存在位</strong>:表示该页是否存在于物理内存当中</p><p><strong>页错误：</strong>访问该页但是该页不存在于物理内存当中，通常由操作系统软件全权负责</p><p>页错误处理流程：</p><ul><li><p>硬件在地址转换中的工作</p><ul><li>TLB命中，直接从从TLB中获取</li><li>TLB未命中，但是该页存在且有效，那么直接从PTE中获取对应的PFN,换入TLB中，再次重试则TLB命中</li><li>TLB未命中，且该页不在物理内存当中，执行页错误处理程序，将该页换到物理内存当中去，按照在物理内存中的处理方法继续执行</li><li>TLB未命中，且该页无效，硬件捕获这个非法访问，操作系统调用陷阱程序，可能会结束这个非法进程</li></ul></li><li><p>操作系统在页错误时所作的工作：</p><p>首先操作系统为将要换入的页找到一个物理空间，如果没有则执行交换算法踢出一些页，获得物理帧之后重新请求从交换空间读取页，操作系统更新页表重试指令，此时TLB会未命中，交换到TLB当中去，再次重试则TLB命中</p></li></ul><h4 id="策略"><a href="#策略" class="headerlink" title="策略"></a>策略</h4><p>如果将物理内存视为cache，硬盘视为内存，那么则可以按照cache命中的那一套思路去思考</p><p>因此，我们目前最应当关系的为命中率和平均访问时间</p><p><strong>最优替换策略</strong></p><p>替换内存中在最远的将来才被访问的页，可以达到缓存未命中率最低</p><p>但是未来的访问无法得知，因此无法为操作系统实现最优策略，只能用于评估算法时作为其他算法的参考</p><p><strong>FIFO</strong></p><p>先进先出，无法确定页的重要性，导致命中率很低，甚至比随机替换的效果还差</p><p><strong>随机</strong></p><p>不解释</p><p><strong>LRU</strong></p><p>最少最近使用，由于访问的**近期性(时间局部性)**，利用历史数据，替换最近最少使用的页面</p><p>相比于FIFO和随机，能够更好的保持热门页，取得较好的效果</p><p>为了实现LRU，需要对每次内存引用进行记录，再删除时还需遍历所有页，寻找近期最少使用的页来进行替换，导致了巨大的时间开销，因此引出的近似LRU算法</p><p><strong>近似LRU</strong></p><p>周期性的清楚使用位，然后通过使用位的0和1来区分近期是否被使用过，来判断使用哪个页</p><p>比较有代表性的为时钟算法(CC)&#x2F;NRU：</p><p>所有页放在一个循环列表当中，开始时指针指向一个页，如果该页为1那么则为近期使用过，不进行替换，往后遍历直至寻找到第一个不是1的页，将其进行替换，遍历过的位置均设置为0</p><p><strong>考虑脏页</strong></p><p>脏页代表从进行过修改的页，如果此时再写回硬盘中，那么则会带来额外的硬盘开销，因此替换时不应当有限考虑脏页，通过增加一个修改位，达到区分脏页的目的</p><h2 id="并发"><a href="#并发" class="headerlink" title="并发"></a>并发</h2><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p><strong>进程&amp;线程</strong></p><p>进程：一个运行着的程序，非共享地址空间，之间的通信只能通过内核来实现</p><p>线程：类似于一个独立的进程，但是供养地址空间，上下文切换消耗小，地址空间保持不变</p><p>传统进程（单线程）只有一个栈，位于地址空间的底部，多线程中多个线程独立运行，均拥有自己的栈</p><p>不同线程之间对于共享资源的操作可能会处于竞争状态（互相中断），如同时进行++操作，最后得到的结果非我们所想的那样。</p><p>整个并发这一章都是为了解决如何高效且不存在错误的进行并发操作，解决竞争时的冲突问题。我们期望能原子性的执行某些操作</p><p><strong>概念陈述：</strong></p><ul><li>临界区：共享资源的一段代码，可以被多个线程操作，通常为一个变量或者数据结构</li><li>竟态条件： 多个线程大致同时进入临界区，试图更新数据结构</li><li>不确定性：由于并发进行操作或修改而导致的结果的不确定性</li><li>互斥：只有一个线程能进入临界区</li></ul><h3 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h3><p>在这一节对于锁的内部进行探究，实现一个真正有实用价值且高效的锁，在这一节之后，不在关注锁的内部，直接调用API进行锁的获取和归还</p><p>锁本质就是一个变量，用来保存这一时刻的信息，如是否有线程持有锁，可用还是已上锁，使用锁来保证在这一时刻，只能有一个线程对临界区进行操作</p><h4 id="实现一个锁"><a href="#实现一个锁" class="headerlink" title="实现一个锁"></a>实现一个锁</h4><p><strong>评价标准：</strong></p><ul><li>提供互斥，维护临界区</li><li>公平性：保证每个线程都能公平的抢到锁</li><li>性能：上锁与解锁的时间开销</li></ul><p><strong>初步尝试：控制中断</strong></p><p>在访问临界区是关闭中断，该线程不会被其他线程中断，则可以安全操作临界区</p><p>通过lock()进行关中断，unlock()进行开中断</p><p>缺点：</p><ul><li>必须信任一个程序，如果一个恶意程序上来就lock()，直接关中断，导致操作系统所依赖的时钟中断也无法生效，从而操作系统失去了对计算机的控制，只能重启</li><li>不支持多处理器，该处理器关中断仍可以在其他处理器上运行</li><li>关中断导致中断丢失</li></ul><p><strong>方案一：test-and-set</strong></p><p>对变量进行检测，如果不是1，则设置为1，表示持有锁，否则自旋等待</p><p>代码如下：</p><p><img src="https://user-images.githubusercontent.com/89784872/183352676-d887cd20-3f1b-439b-9e6b-becea9283700.png" alt="image-20220422220224937"></p><p>存在的问题：该指令本身并不是原子性的，依然可以被中断，下面这种情况导致了flag被设置了两次1</p><p><img src="https://user-images.githubusercontent.com/89784872/183352683-04830c1c-4720-4c32-ae5a-c7d821d5e517.png" alt="image-20220422220204428"></p><p>解决方案：借助硬件，将整个TestAndSet变为一个原子性的操作,测试旧值的同时设置新值</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-type">int</span> <span class="hljs-title function_">TestAndSet</span><span class="hljs-params">(<span class="hljs-type">int</span> * old_ptr,<span class="hljs-type">int</span> new)</span>&#123;<br>    <span class="hljs-type">int</span> old=*old_ptr;<br>    *old_ptr=new;<br>    <span class="hljs-keyword">return</span> old;<br>&#125;<br></code></pre></td></tr></table></figure><p>此时在对锁的进行修改，flag的判断变为调用TestAndSet</p><p><img src="https://user-images.githubusercontent.com/89784872/183352684-9ff1e57b-34f8-40a6-937e-c5cdc1575376.png" alt="image-20220422220144279"></p><p><strong>评价自旋锁：</strong></p><ul><li>功能：自旋锁可以保证互斥</li><li>公平性：自旋锁在自旋等待的过程中，锁被释放时可以有新的线程试图申请，从而导致原本的线程继续自旋，产生饥饿的情况</li><li>性能：在线程归还锁之前，其他的线程都在进行自旋，性能开销相当大，浪费CPU周期</li></ul><p><strong>方案二：比较与交换</strong></p><p>检测ptr指向的值是否与预期值相等，如果是，则更新为新值，获取锁，与TestAndSet相比只是多了一个比较的过程，思路基本一致</p><p><img src="https://user-images.githubusercontent.com/89784872/183352688-596338c1-19ab-4000-affe-5e5fa93958aa.png" alt="image-20220422220026687"></p><p><strong>方案三(改进1)：获取并增加</strong></p><p>原子地返回特定地址的旧值，并且让该值自增1，通过fetch-and-add可以实现一个ticket锁</p><p><img src="https://user-images.githubusercontent.com/89784872/183352735-3cc066ae-c963-4ad5-851e-9cef75e4d9d4.png" alt="image-20220422221511100"></p><p>使用ticket与turn进行构建锁</p><ul><li>ticket(全局)：相当于按照顺序对锁进行一个申请，没申请一次，无论成功与否，都进行一次++</li><li>turn(全局)：表示在对临界的操作当中，全局中进行了几次操作</li><li>myturn(线程)：表示该线程在对临界区的访问当中处于顺位</li></ul><p>操作流程：</p><ul><li>首先对一个ticket执行FetchAndAdd，令ticket++，并且给myturn赋值0</li><li>turn与myturn进行比较，判断是否获取锁</li><li>归还锁事，对turn执行FetchAndAdd，令turn++，与其他的myturn进行比较</li></ul><p>实例：</p><p>假设我们有三个线程，T1，T2，T3同时对一个临界区进行访问与操作，执行流程和各变量的如下</p><table><thead><tr><th align="center">时刻</th><th align="center">线程&amp;操作</th><th align="center">全局ticket</th><th align="center">全局turn</th><th align="center">线程myturn</th></tr></thead><tbody><tr><td align="center">0时刻</td><td align="center">T1申请访问</td><td align="center">1</td><td align="center">0</td><td align="center">0（T1）</td></tr><tr><td align="center">1时刻</td><td align="center">T2申请访问</td><td align="center">2</td><td align="center">0</td><td align="center">1（T2）</td></tr><tr><td align="center">2时刻</td><td align="center">T3申请访问</td><td align="center">3</td><td align="center">0</td><td align="center">2（T3）</td></tr><tr><td align="center">3时刻</td><td align="center">T1归还锁</td><td align="center">3</td><td align="center">1</td><td align="center">XXX</td></tr></tbody></table><p>分析：</p><ol><li><p>申请过程：随着线程的不断申请ticket不断自增，并且将自增前的旧值赋给对应线程的myturn，从而不同的线程获得到了他的顺位</p></li><li><p>归还过程：T1完成了对临界区的访问，如今归还锁，对全局turn执行FetchAndAdd，全局turn变为1</p></li><li><p>竞争过程：T1归还锁之后，T2，T3同时对锁进行申请，但是由于顺位的差异，T2的myturn与全局turn一致，因此T2，申请到了锁，实现了一个按序访问的状态</p></li></ol><p>评价：</p><ul><li>功能性：实现了互斥</li><li>公平性：通过turn与myturn的协作，实现了按序访问，防止了后来者居上，前者饿死的情况，保证了公平性</li><li>性能：其中涉及自旋，仍然有性能的问题</li></ul><p>至此我们通过了三种方案实现了一个锁，ticket在功能性和公平性都有了保证，剩下的则是对性能进行优化</p><h4 id="性能优化"><a href="#性能优化" class="headerlink" title="性能优化"></a>性能优化</h4><p><strong>痛点</strong></p><p>在单处理器上，线程0获取到锁，线程1在获取锁失败之后，线程1便进入了自旋等待的状态，他会一直自旋，直至时钟中断，再次期间，线程0也无法执行，只有时钟中断时，线程0重新运行，最后释放锁，线程1结束自旋，获取锁</p><p>过多的自旋导致了时间片被浪费，甚至获取锁的线程也无法继续执行，需要等待一个时钟中断，造成了巨大的时间开销</p><p><strong>改进方式</strong></p><ul><li>让出CPU</li></ul><p>在要自旋的时候，让出CPU，通过一个yeild原语，让运行态转变为就绪态，在线程少的时候，较为有效，但是在多线程的情况下，反复让出带来的上下文切换，也造成了很大的浪费，同时还存在一个线程反复让出，最后导致饿死的情况</p><ul><li>使用休眠代替自旋</li></ul><p>没抢到锁的进入休眠，通过一个队列保存等待锁的队列，park()进行休眠,unpark(thread)唤醒对应的线程</p><p><img src="https://user-images.githubusercontent.com/89784872/183352745-a0eeabd9-7c02-4695-853f-31d40aadd435.png" alt="image-20220422233247586"></p><p><img src="https://user-images.githubusercontent.com/89784872/183352741-22005bd1-84ab-4ac0-9c7c-6c3d9cf31697.png" alt="image-20220422233223623"></p><p>分析：</p><p>获取锁：初始时guard，flag为0，TestAndSet之后guard变为1，获取到锁之后guard重新设置为1，flag为1，此时其他进行再想尝试获取锁时不会在while进行自旋，而是执行else部分，将自己加入到队列当中，调用park进行休眠</p><p>归还锁：队伍为空只需要将flag设置为0，用于下一次申请锁，如果队列不为空，则唤醒其中的元素，guard设置为0，跳过申请锁时的循环</p><p>在正常操作时，开锁和上锁的while循环都会被跳过，不起任何作用，只有在申请或归还锁的时候被中断时，为了保证上锁的原子性，令其他线程进行自旋，等待上锁完成，虽然依旧存在自旋现象，不存在大量线程自旋等待的现象（均跳过循环加入到了队列当中去），只有在中断的时候才会用于保证一下上锁开锁的完整执行过程。</p><p>在唤醒队列中的线程时，并未将flag设置为0从而去走原本的锁申请过程。设置为0则会让新的线程也参与到竞争当中去，导致老线程的饥饿情况，当一个锁被唤醒时，相当于从park的调用返回，guard为0，flag为1，新的线程会被直接加入到队列当中并休眠，相当于把原本的线程直接转移给了刚被唤醒的线程</p><p>问题：</p><p>如果当前线程刚加入到队列当中还未park，此时被中断，旧的线程执行完成归还锁，当前线程被从队列中移除，此时在park()，则unpark时无法从队列中找到对应的线程，从而导致一直休眠下去</p><p>改进措施：</p><p>通过setpark，一个线程表面自己马上要park，此时另一个线程中断并调用unpark，后续的park会直接返回，不在继续睡眠</p><p>对lock进行修改：</p><p><img src="https://user-images.githubusercontent.com/89784872/183352747-2c48eb8f-252c-4389-be8f-d07117c1dd73.png" alt="image-20220423000108121"></p><p>至此，锁的功能性，公平性，性能均得到了实现和保障，之后涉及锁的部分只需要调用API实现即可，不再探究其内部</p><h3 id="基于锁的并发数据结构"><a href="#基于锁的并发数据结构" class="headerlink" title="基于锁的并发数据结构"></a>基于锁的并发数据结构</h3><p><strong>并发计数器</strong></p><p>在修改数的操作前后加上一把锁，解决了冲突的问题但是性能并不理想</p><p><strong>可拓展计数</strong></p><p>设计了懒惰计数器，分为多个局部计数器和一个全局计数器，每个局部计数器一把锁，全局计数器一把锁，这样保证了可以同时进行多次计数，不在全部都去竞争一把锁，一段时间后，局部计数器的数值再加到全局计数器之上</p><p><img src="https://user-images.githubusercontent.com/89784872/183352750-312dd606-4f3c-4f3c-ab04-ddefd2dcb4d4.png" alt="image-20220423001450990"></p><p><img src="https://user-images.githubusercontent.com/89784872/183352802-c9601408-e5d4-40b6-b8f1-6108e485f816.png" alt="image-20220423001508151"></p><p>阈值S的选取决定了计数器是更高效还是延迟更短</p><p><strong>并发链表</strong></p><p><img src="https://user-images.githubusercontent.com/89784872/183352809-e1ad1631-19ea-4167-b649-5c5e8cb1c2c8.png" alt="image-20220423001822387"></p><p>malloc线程安全，因此只对插入操作加上锁</p><p><strong>并发队列</strong></p><p>使用两个锁，一个负责队列头，一个负责队列尾，使插入和删除可以同时进行</p><p><strong>并发散列表</strong></p><p>散列表的每个桶通过并发链表进行组织，即为每个桶加上一个锁，各个桶之间可以进行并发操作</p><h3 id="条件变量"><a href="#条件变量" class="headerlink" title="条件变量"></a>条件变量</h3><p>线程通过条件变量去等待一个条件变为真，条件变量本身为一个队列，不满足将自身加入队列，进行wait，满足时再重新唤醒。</p><p>wait()休眠，同时释放锁，signal()唤醒</p><p>通过join()函数进行休眠，exit() 函数自身退出，唤醒别的线程</p><p><img src="https://user-images.githubusercontent.com/89784872/183352815-f400c9ae-9d11-4ad1-9a28-a86c7a97b6e9.png" alt="image-20220423005012646"></p><p><img src="https://user-images.githubusercontent.com/89784872/183352816-0d2eb231-c6d6-456a-8b39-0c94440500b4.png" alt="image-20220423005036271"></p><p><strong>特别注意：</strong></p><p>条件变量和锁必须配合使用，即在调用wait和signal的时候都必须持有锁</p><ul><li><p>wait的定义要求他在调用时必须持有锁，</p></li><li><p>signal在调用时如果未持有锁，可能出现问题，但也可能没有问题，为了避免，应在使用signal时持有锁，</p></li><li><p>以下的例子可以说明wait释放锁的设计的合理性</p><p><a href="https://blog.csdn.net/madbunny/article/details/51267984">(77条消息) 使用条件变量时为啥一定要指定一个锁？_madbunny的博客-CSDN博客</a></p></li></ul><p>条件变量必须配合锁来使用，来避免如下的竞争条件：一个线程准备在条件变量上等待，但是进入等待之前另一个线程唤醒了条件变量。</p><p>也就是说，没有锁进行同步的情况下，一个线程在wait操作中被挂起，并没有完成wait，这时另一个线程触发了signal操作进行唤醒，然后第一个线程才完成wait操作，那第一个线程就收不到第一个线程的signal操作。</p><p>所以，根据上边的解释，通过锁使wait和signal操作进行线程同步，通过锁使wait和signal都是原子操作。</p><p><strong>生产者和消费者的问题</strong></p><p>一个或多个生产者和一个或多个消费者，共用一个缓冲区，缓冲区非满时生产，缓冲区非空时消费</p><p><strong>首次尝试</strong></p><p><img src="https://user-images.githubusercontent.com/89784872/183352821-2c24778d-c1a4-46b1-8762-bdbb59d53652.png" alt="image-20220423005439216"></p><p>可以保证缓冲区非满时生产，缓冲区非空时消费，解决了生产者和消费者之间竞争缓冲区的问题，问题：</p><ul><li>由于只存在一个队列，唤醒时无法区分生产者和消费者，生产者可能在满的时候唤醒的是生产者</li><li>锁只能控制申请的过程，对于已经申请结束进入睡眠的并发无法解决，如果唤醒一个消费者的但是还未执行该消费者，此时另外一个消费者抢先执行，最后导致get两次</li></ul><p>改进：</p><p>将if改为while，唤醒之后再次进行判断，使用empty和fill两个条件变量来区分生产者与消费者，得到最终的解决方案</p><p><img src="https://user-images.githubusercontent.com/89784872/183352823-fa2d315b-6c0c-4d58-a841-fe5f8d64bf5d.png" alt="image-20220423012354760"></p><p><strong>Java中生产者消费者的具体实现</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.fischer;<br><br><span class="hljs-comment">/**</span><br><span class="hljs-comment"> * <span class="hljs-doctag">@description</span>: 参考了 https://www.jianshu.com/p/da312eee4ac4</span><br><span class="hljs-comment"> * <span class="hljs-doctag">@author</span>: fischer</span><br><span class="hljs-comment"> **/</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Object</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>();<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-variable">product</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-comment">//如果没有产品,在lock对象上等待唤醒,如果有产品,消费.</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">Runnable</span> <span class="hljs-variable">consumer</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br>        System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; prepare consume&quot;</span>);<br>        <span class="hljs-keyword">synchronized</span> (lock) &#123;<br>            i (product &lt;= <span class="hljs-number">0</span>) &#123;<span class="hljs-comment">//替换为while解决线程虚假唤醒问题</span><br>                <span class="hljs-keyword">try</span> &#123;<br>                    System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; wait&quot;</span>);<br>                    lock.wait();<br>                &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>                    e.printStackTrace();<br>                &#125;<br>                System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; wakeup&quot;</span>);<br>            &#125;<br>            product--;<br>            System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; consumed product:&quot;</span> + product);<br>            <span class="hljs-keyword">if</span> (product &lt; <span class="hljs-number">0</span>) &#123;<br>                System.err.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; spurious lock happend, product: &quot;</span> + product);<br>            &#125;<br>        &#125;<br>    &#125;;<br>    <span class="hljs-comment">//生产一个产品然后唤醒一个在lock对象上等待的consumer</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">Runnable</span> <span class="hljs-variable">producer</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br>        System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; prepare produce&quot;</span>);<br>        <span class="hljs-keyword">synchronized</span> (lock) &#123;<br>            product += <span class="hljs-number">1</span>;<br>            System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot;produced product: &quot;</span> + product);<br>            lock.notify();<br>        &#125;<br>    &#125;;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">producerAndConsumer</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-comment">// 启动2个consumer,1个producer</span><br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">c1</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(consumer);<br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">c2</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(consumer);<br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">p</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(producer);<br>        c1.start();<br>        c2.start();<br>        p.start();<br><br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-comment">//运行100次,以便触发异常现象</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">100</span>; i++) &#123;<br>            <span class="hljs-keyword">new</span> <span class="hljs-title class_">Test</span>().producerAndConsumer();<br>        &#125;<br><br>        <span class="hljs-keyword">try</span> &#123;<br>            Thread.sleep(<span class="hljs-number">5000</span>);<br>        &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>            e.printStackTrace();<br>        &#125;<br>        System.exit(<span class="hljs-number">0</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><img src="https://user-images.githubusercontent.com/89784872/183352824-922efa79-551a-40fd-8b4c-16e3b7715e9c.png" alt="image-20220423012409010"></p><p><strong>if与while的问题&#x2F;虚假唤醒：</strong></p><p>while可以保证问题的解决，if可能对，取决于发信号的语义？？？</p><p>while可以避免假唤醒的问题，如果一个signal唤醒多个线程，或者线程因为其他原因被莫名唤醒，第一个线程操作之后后面的线程可能不满足条件变量，从而导致错误操作，因此应当总使用while</p><p>由于唤醒-获取锁的过程非原子性，线程被唤醒，此时条件被改变，如果使用if，已经不满足运行要求，但是依旧获取到了锁，因此需要使用while在唤醒使再次进行条件判断</p><p>虚假唤醒为操作系统本身的问题，只能在写代码时使用while来进行避免</p><p><a href="https://juejin.cn/post/6903789756954443784">Java多线程中的虚假唤醒和如何避免 - 掘金 (juejin.cn)</a></p><h3 id="信号量"><a href="#信号量" class="headerlink" title="信号量"></a>信号量</h3><p><strong>定义</strong></p><p>一个整数值的对象，在POSIX中通过sem_wait()[自减]和sem_post()[自增]进行操作</p><p>sem_wait()在信号量&gt;&#x3D;1是直接返回，否则将该线程挂起，等待一个sem_post()将其恢复</p><p>信号量为负时，这个值就是等待线程的个数</p><p><strong>信号量作为锁</strong></p><p>用sem_post和sem_wait将临界区环绕起来，通过信号量来判断当前进程是等待还是运行，初始化为1，只允许一个线程进入临界区</p><p><strong>信号量解决生产者与消费者的问题</strong></p><p><img src="https://user-images.githubusercontent.com/89784872/183352830-9ed8725f-5601-4272-b2ff-93a4de2ad42c.png" alt="image-20220423013749909"></p><ul><li>mutex保证始终只有一个人在互斥操作缓冲区</li><li>empty&#x2F;fill用于互相控制对方的休眠与唤醒</li><li>mutex信号量在full&#x2F;empty信号量之内，避免了死锁，如果在外面，会导致缓冲区为空时，消费者申请到了mutex，但是被full卡住，无法退出，同时生产者又申请不到mutex，二者均被卡住，形成了死锁</li></ul><p><strong>读者写者问题</strong></p><p>要求：</p><ul><li><p>读时不可写，写时不可读</p></li><li><p>某一时刻只能有一个写者在写</p></li></ul><p>双方操作时均需要判断对方的状态</p><p>为了防止写者的饥饿问题，采用写者优先的策略，二者共同竞争一个信号量S，如果写者竞争到了S，那么读者则被封锁，直至写完</p><p><img src="https://user-images.githubusercontent.com/89784872/183352834-3c5d2de5-b5da-4bab-bde9-e421bd080398.png" alt="image-20220423015228999"></p><p><strong>哲学家就餐问题</strong></p><p>五个人五个餐叉，拿到两个才能就餐</p><p>解决方法：</p><ul><li>其中一个人的获取方式与其他人不同，其他人优先获取左手边的，他优先获取右手边的，那么他或者他左右的两个人一定会有一个人拿到两个餐叉</li><li>限制最多只能由四个人同时吃面</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183352835-46813084-89a1-4d13-b751-16b2e4756d9b.png" alt="image-20220423015521205"></p>]]></content>
    
    
    
    <tags>
      
      <tag>CS基础</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>计算机网络-自顶向下</title>
    <link href="/2022/08/08/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/"/>
    <url>/2022/08/08/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/</url>
    
    <content type="html"><![CDATA[<h1 id="计算机网络"><a href="#计算机网络" class="headerlink" title="计算机网络"></a>计算机网络</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>相关术语：</p><ul><li>主机&#x2F;端系统</li><li>ISP：网络提供商</li><li>RFC：标准文档，定义了一众协议</li><li>协议： 定义了通讯实体之间交换报文的格式和顺序，<strong>语法、语义、时序、动作</strong></li><li>因特网：分布式应用以及为其提供服务的基础设施</li></ul><p><strong>因特网</strong></p><ul><li>网络边缘：位于网络边缘的端系统以及应用程序</li><li>网络核心：端系统分组交换机和链路构成的网络</li><li>接入网：将端系统物理连接到边缘路由器的网络</li></ul><p><strong>交换方式：</strong></p><ul><li>电路交换<ul><li>对所需的资源进行预留，分配好则他人不可使用，独占性</li><li>对于同一条电路划分成不同的piece进行分配带宽</li><li>复用方式：频分复用、时分复用<ul><li>频分复用：分配固定的频率（类比电台）</li><li>时分复用：分配固定长度的帧和间隙</li></ul></li></ul></li><li>分组交换<ul><li>端系统之间相互交换的信息称为报文，报文划分成小的数据块则称为分组(包，packet)</li><li>越过通信链路使用全部的带宽</li><li>复用方式：按需划分成时间片，非固定长度</li><li>存储转发传输：在不同的节点之间，需要先将分组完整的存储起来，然后再转发到下一个检点，在此期间产生了延迟。总延迟为存储过程中产生的延迟和物理传输过程中的延迟(距离&#x2F;速度)</li><li>转发：通过目标来查询路由器，获取下一次转发的路径</li></ul></li></ul><p><strong>延时：</strong>处理延时，排队延时，传输延时(上载下载速度)，传播延时(物理传播)，</p><h3 id="协议层次以及模型"><a href="#协议层次以及模型" class="headerlink" title="协议层次以及模型"></a>协议层次以及模型</h3><p>通过协议分层，只需关注当前层如何功能，对于下面的层次，只需调用其API即可，无需关注实现方式，(类似 controller service dao三层架构)</p><p><strong>协议栈</strong>：</p><ul><li>应用层：网络应用程序以及相关协议存在的位置</li><li>传输层：通过传输协议(TCP&#x2F;UDP)进行应用层报文的传输</li><li>网络层：IP协议传输数据报(datagram)</li><li>链路层：将分组再不同的节点之间传输(MAC)，链路层分组为帧</li><li>物理层：一个个bit的将数据进行传输，电信号，光信号表示01</li></ul><p><strong>OSI七层模型</strong></p><p>多出了表示层和会话层</p><ul><li>表示层：使通信的应用程序能够解释交换数据的含义</li><li>会话层：提供数据交换的定界和同步功能</li></ul><p><strong>封装</strong></p><p>在将分组向下一层传输时，从上一次获取到有效负载(payload)，从在负载之前加上自己的首部字段</p><h2 id="应用层"><a href="#应用层" class="headerlink" title="应用层"></a>应用层</h2><h3 id="概述-1"><a href="#概述-1" class="headerlink" title="概述"></a>概述</h3><p>应用层所运行的为应用程序以及其所支持的协议，如web项目，邮箱服务等以及所涉及的相关协议均运行在应用层之上</p><p><strong>体系结构</strong></p><ul><li>客户端-服务器（C-S）</li><li>P2P</li></ul><p><strong>进程通信</strong></p><p>一个进程可认为是端系统上的一个程序，此处所说的进程通信为两个端系统之间的通信，通过交换报文的方式借助socket API，将报文下交给传输层，使用TCP&#x2F;UDP进行传输</p><p><strong>定位方式</strong></p><p>一个进程对应着一个端口，通过主机IP+端口即可，具体绑定方式在传输层中讨论，对于应用层只需调用socket API即可</p><h3 id="应用层协议"><a href="#应用层协议" class="headerlink" title="应用层协议"></a>应用层协议</h3><h4 id="HTTP"><a href="#HTTP" class="headerlink" title="HTTP"></a>HTTP</h4><ul><li><p>定义了浏览器向服务器的请求方式，以及服务器做出相应，传递页面的方式</p></li><li><p>以tcp作为支撑</p></li><li><p>无状态协议</p></li><li><p>默认为持续连接，即创建tcp连接(三次握手)后保持该连接</p></li></ul><p><strong>三次握手</strong></p><ol><li>客户端向服务器发送tcp报文段，</li><li>服务器用tcp报文段确认并作出响应</li><li>客户端向服务器返回确认</li></ol><p>通过该三次握手，第二次握手服务端确认了客户端的发送能力，客户端接收到服务端的响应后确认了服务端的接收和发送能力，第三次握手服务器确认了客户端的接收和响应能力，从而客户端和服务端的发送和接收能力都得到确认，建立起完备的连接</p><p><strong>HTTP报文</strong></p><ul><li><p>请求报文：</p><ul><li><p>请求行：方法字段 url字段 http版本字段</p></li><li><p>首部行：host，user-agent等，F12抓包查看即可</p><p><strong><img src="https://user-images.githubusercontent.com/89784872/183347405-b75bda96-ef07-48d5-a592-5ffcce9b25cf.png" alt="image-20220720233034070"></strong></p></li></ul></li><li><p>响应报文</p><ul><li>初始状态行</li><li>首部行</li><li>实体</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347432-fcb938ad-da75-4c30-b553-2d737951ed97.png" alt="image-20220720233107288"></p></li></ul><p> <strong>C-S交互</strong></p><ul><li>cookie&amp;session</li><li>JWT</li></ul><p><strong>Web缓存</strong></p><p>正向代理服务器，在客户端侧，客户端和代理服务器建立tcp连接后向代理服务器发送http请求，若代理服务器本地有，则直接返回给客户端即可，起到了缓存的效果，如果没有，再向目标服务器建立连接发送请求。</p><ul><li>代理服务器位于局域网内，客户端与代理服务器之间比和原目标服务器速度要快</li><li>源目标服务器被访问次数减少，压力减小，响应速度变快</li></ul><p><strong>HTTP 1.1</strong></p><p>HTTP1.1主要在长连接方面进行了优化，使得GET POST请求后连接不断开，无需再次重新建立连接,但是同时又引入了队头阻塞的问题，即某个HTTP请求的响应没有被即时的返回，后面的响应都会被阻塞</p><p>除此之外还有部分传输、HOST、缓存等方面</p><p><strong>HTTPS</strong></p><p>通过SSL进行加密，HTTPS解密出来仍是HTTP</p><p>非对称加密</p><p><img src="https://user-images.githubusercontent.com/89784872/183347691-902ffa78-c168-4ead-89ec-f8495a14bb2c.png" alt="image-20220806200424509"></p><p><strong>HTTP2.0</strong></p><p>并行进行请求，发送多个请求，提高访问速度。原本的HTTP1.0更像是个停等协议，2.0由停等转变为流水线方式</p><p><img src="https://user-images.githubusercontent.com/89784872/183347693-6ddd2c32-fca1-4993-8934-0c90c4a267b6.png" alt="image-20220806200956971"></p><p><strong>QUIC&#x2F;HTTP3.0</strong></p><ul><li>使用UDP</li><li>在用户态Kernel</li><li>0RTT</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347712-bc369b5d-caf8-4b5c-9b5a-e450fb73f9dd.png" alt="image-20220806202645652"></p><h4 id="电子邮件协议"><a href="#电子邮件协议" class="headerlink" title="电子邮件协议"></a>电子邮件协议</h4><p>电子邮件系统主要由三个部分组成：</p><ul><li>用户代理</li><li>邮件服务器</li><li>简单邮件传输(STMP)</li></ul><p>电子邮件协议主要分为发送协议和访问协议：</p><ul><li>发送协议：STMP</li><li>访问协议：常用 POP3和IMAP</li></ul><p><strong>STMP</strong></p><p>发送过程：</p><ol><li>Alice调用用户代理程序撰写报文，并提供Bob的邮箱，指示用户代理发送该报文</li><li>Alice用户代理把报文发送给邮件服务器，报文放在报文队列当中</li><li>Alice的邮件服务器上的STMP客户端发现报文，通过TCP与Bob的STMP建立连接，发送报文</li><li>Bob的邮件服务器上，STMP服务器接收报文，存入邮箱</li></ol><blockquote><p>邮件服务器为一个实体服务器，而STMP服务器邮件服务器上运行的应用程序，类似于http服务器</p></blockquote><p><strong>POP3</strong></p><p> 分为三个阶段：</p><ol><li>特许(Authorization)：对用户进行鉴权</li><li>事务处理：用户代理代理取回报文 （删除）</li><li>更新：客户端发出quit命令，结束会话</li></ol><blockquote><p>最新的POP3协议可以通过配置来选择“下载删除”或”下载保留”</p></blockquote><p><strong>IMAP</strong></p><p>将报文和文件夹相关联</p><p>IMAP协议中客户端和服务器上的邮件保持同步，如果不⼿动删除邮件，那么服务器上的邮件也不会被删除。IMAP这种做法可以让⽤户随时随地去访问服务器上的邮件。</p><p><strong>HTTP</strong></p><p>基于Web的电子邮件，用户代理即为Web浏览器，用户和远程邮件服务器之间的通信则使用HTTP协议，而非POP3和IMAP，邮件服务器之间的仍为STMP</p><h4 id="DNS"><a href="#DNS" class="headerlink" title="DNS"></a>DNS</h4><p>作用：IP地址与域名之间的解析</p><p>组成：</p><ul><li>分层的DNS服务器实现的分布式数据库</li><li>使主机能够查询分布式数据库的应用层协议</li></ul><p><strong>分布式，分层数据库</strong></p><p>DNS服务器主要分为三个层级：</p><ul><li>根DNS服务器</li><li>顶级域服务器(com,org,io,edu等)</li><li>权威服务器：组织机构保存对外公共访问的域名和IP的解析关系</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347437-1abf63e2-e8fc-449c-a9e9-ffc6b58d72a0.png" alt="image-20220731220418554"></p><p>查询方式：</p><ul><li>递归查询：首先向根目录发起查询请求，根目录在向顶级域发起查询，最终到提供的域名的权威服务器，权威服务器再将结果向上传达，最终由根将结果告知给用户</li><li>迭代查询：首先向根目录发起查询请求，根目录让用户向顶级域去发起请求，顶级域再让用户向权威服务器发起查询，最终由权威服务器将查询结果告知给用户</li></ul><blockquote><p>二者之间的区别为最终由谁将结果告知给查询的用户，递归为向下递归查询，再将查询的结果向上传递，由最初的根将结果返还给用户，而迭代更像是再对其进行重定向</p></blockquote><p><strong>DNS记录和报文</strong></p><p>数据库中存储的资源记录格式如下：<code>(Name,Value,Type,TTL)</code></p><p>其中，Name和Value的具体值由Type来决定：</p><ul><li>Type &#x3D; A Name为主机名 Value为IP</li><li>Type &#x3D; NS Name为域名 Value为DNS服务器的主机名</li><li>Type &#x3D; CNAME  Name为主机的别名，Value为规范主机名</li><li>Type &#x3D; MX Name为别名，Value为规范主机名（邮件服务器）</li></ul><blockquote><ul><li><p>提供<strong>主机别名</strong>（host aliasing）服务，有着复杂主机名的主机可以有一个或者多个别名，例如aaa.xxx.com的主机可能还会有aaa.com和<a href="http://www.aaa.com两个别名,在这种情况下,aaa.xxx.com叫做**规范主机名**(canonical/">www.aaa.com两个别名，在这种情况下，aaa.xxx.com叫做**规范主机名**（canonical</a> hostname）。主机别名的特征是比规范主机名更容易记忆，DNS可以提供根据主机别名获取规范主机名的服务</p></li><li><p>提供<strong>负载分配服务</strong>（load distribution） 一般来说，被繁忙访问的大型站点是分布在多台服务器上的， 这个时候，主机名和IP地址就不是一一对应的关系，而是一个主机名对应一个IP地址的集合。 在大量的，连续的多次访问中，DNS通过旋转IP地址达到负载均衡的目的：在向这个主机名发出DNS请求的时候，服务器会用包含全部这些IP地址的报文进行回答， 但在每个不同的回答中会旋转这些IP地址的摆放顺序，<strong>而客户机总向报文中排在最前的IP地址发出请求</strong></p></li></ul></blockquote><p>当查询时，提供一个域名，通过域名，在数据库中查询Type &#x3D; NS类型的资源记录，找到对应的DNS服务器，在向DNS服务器进行查询，在其中找到 Type &#x3D; CNAME的记录 查询出主机名，在通过主机名查询Type &#x3D; A的资源记录，最终查询出域名对应的IP</p><p><strong>插入记录</strong></p><p>向注册登记机构提供 ip和基本&#x2F;辅助DNS服务器的名字，将Type &#x3D; A，NS类型的插入到数据库当中们同事还要确保A类型和MX类型要存储在自己的权威DNS服务器当中</p><h4 id="P2P"><a href="#P2P" class="headerlink" title="P2P"></a>P2P</h4><p>P2P模式避免了传统C-S模式下服务器性能瓶颈的情况，每个节点既可以请求，也可以提供服务</p><p>速度分析：</p><ul><li>最初只有服务器有文件，因此需要通过服务器发送给对等方${F}&#x2F;{U_{s}}$F为文件大小，Us为服务器上载速度,</li><li>在对等方下载：$F&#x2F;d_{min}$ dmin为最低下载速度</li><li>当有N个一同请求时：总的传输为$NF$，总的上载能力为服务器的上载能力加上N个对等方的上载能力：$u_s+∑_{i&#x3D;1}^{N}u_i$</li></ul><p>因此，最小分发时间为$D_{P2P} &#x3D; max[{F}&#x2F;U_{s},F&#x2F;d_{min},{NF&#x2F;(u_s+∑_{i&#x3D;1}^{N}u_i)}]$</p><p><strong>BitTorrent</strong></p><p>洪流：参与一个文件分发的所有对等方的集合</p><p>一个对等方参与到了洪流当中，下载的同时也在进行上载，在他完成下载后，可以选择离开洪流，也可以选择继续在其中无私奉献</p><p>Alice周期性的询问邻居的具有的块列表</p><p>策略：</p><ul><li>请求：<strong>最稀缺优先</strong>，Alice首先请求在邻居中副本最少的块</li><li>响应：优先响应以最高速率给Alice提供服务的邻居，同时每过30s随机选出一个邻居来提供服务</li></ul><h4 id="CDN"><a href="#CDN" class="headerlink" title="CDN"></a>CDN</h4><p>内容分发网，通常用于进行流媒体访问加速</p><p>安置原则：</p><ul><li><strong>深入</strong>：靠近用户</li><li><strong>邀请做客</strong>：在少量关键位置设置大集群来邀请到ISP做客</li></ul><p>CDN通过拦截客户端的请求来确定适合该客户的CDN服务器集群，同时将请求重定向到该集群。</p><h2 id="运输层"><a href="#运输层" class="headerlink" title="运输层"></a>运输层</h2><p>要点：</p><ul><li>多路复用&#x2F;解复用</li><li>可靠数据传输</li><li>流量控制</li><li>拥塞控制</li><li>TCP&#x2F;UDP</li></ul><h3 id="概述-2"><a href="#概述-2" class="headerlink" title="概述"></a>概述</h3><p>运输层协议为不同主机上的应用进程之间提供了<strong>逻辑</strong>通信功能，在应用层的角度，直接通过socket编程的方式即可完成不同应用程序之间的通信，而不需要关注底层的网络层和链路层物理层等</p><p>运输层从应用层出接收到报文，划分成不同的段并添加上头部，形成运输层报文段，向下传递后发送给对方，对方再把报文段还原成报文上交给应用层，应用层收到报文后即为一次通信结束</p><h3 id="多路复用解复用"><a href="#多路复用解复用" class="headerlink" title="多路复用解复用"></a>多路复用解复用</h3><p>应用程序-进程-socket-端口之间一一对应(不算进程fork出来的子进程)</p><p>网络层提供的为端到端的服务，运输层则为进程到进程的服务，因此，为了通过网络层传输运输层的报文段，实现进程到主机的对应关系，便有了多路复用&#x2F;解复用</p><p><strong>多路复用</strong>：</p><ul><li>一个socket套接字对应着一个进程，为了让不同的进程的数据均通过网络层的IP协议传输，</li><li>因此在源主机从不同的套接字中手机数据块，并为每个块封装上首部信息，生成报文段，将报文段传递到网络层即为多路复用</li><li>生成报文段之后，在网络层看来，不断进程的数据在性质上已经相同</li></ul><p><strong>多路分解</strong>：将运输层报文段中的数据交付到正确套接字出，重新在进程层面上加以区分</p><p><strong>经典案例</strong>：Ann和Bill两家之间写信通信</p><p><strong>多路复用要求：</strong></p><ul><li>套接字有唯一标识符</li><li>每个报文段有特殊字段(源端口+目标端口)来指示该报文段要交付到的套接字</li></ul><p>TCP使用的为四元组（源IP，源端口，目标IP，目标端口）作为特殊字段来绑定和表示套接字，完成数据到套接字的正确指派</p><p>UDP只有二元组(目标IP,目标端口)</p><h3 id="UDP"><a href="#UDP" class="headerlink" title="UDP"></a>UDP</h3><p>UDP的主要特点为无连接，并且出了多路复用和解复用和少量误差检测之外，几乎没有在IP之上添加新的功能，没有像TCP的可靠性校验和阻塞控制等，因此保证了低延时等特点，被运用于流媒体，DNS等对延时敏感的场景上</p><ul><li><p>发送什么数据和什么时候发送的应用层更为精细</p></li><li><p>无须建立连接</p></li><li><p>无连接状态</p></li><li><p>分组首部开销小</p></li></ul><p><strong>UDP报文段结构</strong></p><img src="https://user-images.githubusercontent.com/89784872/183347441-81d838f2-6f94-4ae8-8a4f-c61e77a6740c.png" alt="image-20220801192825210" style="zoom: 67%;" /><p>其中，校验和为源端口号+目标端口号+长度的反码，因此源端口号+目标端口号+长度+校验和的结果各个位全为1，全为1则通过校验</p><p>提供检验但是对擦错无恢复能力</p><h3 id="可靠数据传输原理-RDT"><a href="#可靠数据传输原理-RDT" class="headerlink" title="可靠数据传输原理(RDT)"></a>可靠数据传输原理(RDT)</h3><h4 id="停等协议"><a href="#停等协议" class="headerlink" title="停等协议"></a>停等协议</h4><p><strong>RDT1.0</strong></p><p>假设底层通信完全可靠，因此在运输层不做任何可靠保障，但IP提供的为尽力而为的服务，并不提供任何可靠性保障，只是一个美好的念想</p><p><img src="https://user-images.githubusercontent.com/89784872/183347451-6866c608-0883-4645-b345-720501532471.png" alt="image-20220801205002106"></p><p><strong>RDT2.0</strong></p><ul><li>发送方等待接收方的ACK或NAK</li><li>停等协议</li><li>无超时重传且无法处理ACK和NAK受损的情况</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347458-3fa882cb-778d-4fe2-92a4-5e6e395d3fcf.png" alt="image-20220801205107581"></p><p><strong>RDT3.0</strong></p><ul><li>加入超时重传 发送方发送后启动定时器 在分组和ACK丢失时会超时重传</li><li>依然为停等协议</li><li>ACK通过 0 1表示受到了的分组的序号</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347464-db955cc9-68a8-4b27-bc37-cef3d63ff4cd.png" alt="image-20220801210514390"></p><h4 id="流水线协议"><a href="#流水线协议" class="headerlink" title="流水线协议"></a>流水线协议</h4><p>为解决停等协议的性能和信道利用率问题，提出了流水线协议，一次发送多个报文段，根据异常处理的方式不同又可分为GBN和SR</p><ul><li>发送缓冲区 &gt;1 接收缓冲区 &#x3D; 1：GBN</li><li>发送缓冲区 &gt;1 接收缓冲区 &gt; 1：SR</li></ul><p>而发送缓冲区和接收缓冲区均为1时即为停等协议</p><p><strong>Go Back N：</strong></p><p>发送方可发送多个分组，但是接收方只能确认一个，如果已经接收到了第N个，但是第一个丢失，那么从第一个开始重发</p><p>可将分组分为四组：</p><ul><li>发送且已经确认</li><li>发送了但还未确认</li><li>还未发送但是处于发送的缓冲区内</li><li>还未进入发送缓冲区，不可用</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347466-49565f16-b0f8-4d76-ad0a-70ab786f0f3b.png" alt="image-20220801212451783"></p><p><strong>Selective Repeat</strong></p><p>相比GBN多了接收缓冲区，在接收缓冲区内任一一个分组丢失，只需要对该分组进行重发</p><p><img src="https://user-images.githubusercontent.com/89784872/183347468-f4e23adc-f615-49c5-8988-63a0b4b9739e.png" alt="image-20220801212633072"></p><h3 id="TCP"><a href="#TCP" class="headerlink" title="TCP"></a>TCP</h3><h4 id="连接管理"><a href="#连接管理" class="headerlink" title="连接管理"></a>连接管理</h4><ul><li>逻辑连接</li><li>全双工服务</li><li>报文段的数据量受限于最大报文段长度(MSS),最大传输单元(MTU)[最大链路层帧长度]</li></ul><p><strong>三次握手：</strong></p><ol><li>客户端现象服务端发送一个TCP报文段，不包含应用层数据，SYN位置为1</li><li>服务端提取SYN报文段，为该TCP连接分配资源，像客户端发送允许连接的报文段</li><li>客户端分配完资源后，服务端发送一个确认连接的报文段，至此三次握手结束</li></ol><blockquote><p>第一次握手服务端确认了客户端具有发送报文的能力，第二次握手客户端确认了服务端的接收和发送能力，第三次握手服务端确认了客户端的接收能力，双方均确认对方的发送和接收能力   –来自《码出高效》</p></blockquote><p><img src="https://user-images.githubusercontent.com/89784872/183347472-833ab5f4-9764-4bf0-bb06-8a7f0e3efe26.png" alt="image-20220801220057235"></p><p><strong>四次挥手：</strong></p><p>双向关闭：</p><ul><li>一二次挥手：客户端向服务端发送FIN并接收到ACK后，客户端到服务端的通道已经关闭，但此时服务端还可向客户端发送</li><li>三四次挥手：服务端向客户端发送FIN并接收到ACK后，服务端到客户端的通道也进行关闭，服务端接收到这个请求后，服务端就能成功变成关闭（CLOSE）状态。客户端则会等一段时间再进入关闭状态，因为第四次挥手不一定能成功发给服务端，所以要等一下，看看服务端会不会因为没收到第四次挥手，而重发第三次挥手。</li></ul><h4 id="报文段结构"><a href="#报文段结构" class="headerlink" title="报文段结构"></a>报文段结构</h4><p><img src="https://user-images.githubusercontent.com/89784872/183347549-abca5844-bb24-452d-8f83-8837535dcc45.png" alt="image-20220801221608776"></p><ul><li>序号：该报文段的首字节的字节流编号(应用层向运输层提供的为字节流)</li><li>确认号：期望收到的下一字节的序号</li><li>接收窗口字段：用于流量控制</li><li>可选字段：用于发送方和接受方协商MSS</li></ul><h4 id="RTT"><a href="#RTT" class="headerlink" title="RTT"></a>RTT</h4><ul><li><p>往返时间，可用于在超时重传时作为超时时间的参考</p></li><li><p>动态计算：</p><p>$EstimatedRTT &#x3D; (1-a)* EstimatedRTT +a*SampleRTT$</p><p>对于当前测量的RTT给予较小的权重，通常设置a&#x3D;0.125</p></li><li><p>DevRTT：用于估量SimpleRTT相对EstimatedRTT的偏移程度</p><p>$DevRTT &#x3D; (1-β)<em>DevRTT+β</em>|SampleRTT-EstematedRTT|$</p></li><li><p>超时时间通常设置为：$TimeoutInterval &#x3D; EstimatedRTT+4 * DevRTT$</p></li></ul><blockquote><p>EstimatedRTT和DevRTT类似于正态分布中的u和σ的关系，$EstimatedRTT+4 * DevRTT$意为在概率为XX的情况下，该分组已经丢失</p></blockquote><h4 id="可靠数据传输"><a href="#可靠数据传输" class="headerlink" title="可靠数据传输"></a>可靠数据传输</h4><p>大致与之前讨论的流水线方案类似，但有针对调整设计</p><ul><li><p>超时间隔加倍：TCP每次重传后都将超时时间设为原来的两倍，而不是通过RTT估算的来的</p></li><li><p>快速重传：收到三个冗余ACK，则快速重传，不再等待超时定时器超时</p></li><li><p>丢失控制：</p><ul><li><p>发送方仅维持发送了但未确认和下一个要发送的</p></li><li><p>接收方发送ACK时采用一个辅助定时器，在定时器到时前不发ACK，将时间段内多个ACK一同发送给发送方</p></li><li><p>接收方仍设置缓存：在中间存在丢失分组，形成Gap时，返回ACK X，要求接收方来填补Gap</p><p><img src="https://user-images.githubusercontent.com/89784872/183347551-83ec36e4-6841-4c6f-93e6-7e86fdf5827a.png" alt="image-20220801235237745"></p></li></ul></li></ul><h4 id="流量控制"><a href="#流量控制" class="headerlink" title="流量控制"></a>流量控制</h4><p>接收方维护两个变量：</p><ul><li>LastByteRead：从缓冲区中读出的最后一个字节编号</li><li>LastByteRcvd：缓冲区中的最后一个字节编号</li></ul><p>二者相减即可得到缓冲区中剩余的字节数，要求剩余的字节数小于缓冲区大小</p><p>$LastByteRcvd-LastByteRead &lt;&#x3D; RcvBuffer$</p><p>接收窗口rwnd:还能继续接收的字节数</p><p>$rwnd &#x3D; RcvBuffer -[LastByteRcvd-LastByteRead]$</p><p>接收窗口这个变量交给发送方来维护，从而控制进行流量控制</p><p>同时发送方维护：</p><ul><li>LastByteSent：发送的最后一个</li><li>LastByteAcked：确认的收到的最后一个</li></ul><p>二者相减即可得到发送出但还未收到的字节数，要求这个字节数小于接收窗口的大小，即可保证接收方能正确处理</p><p>$LastByteSent-LastByteAcked &lt;&#x3D; rwnd$</p><h4 id="拥塞控制"><a href="#拥塞控制" class="headerlink" title="拥塞控制"></a>拥塞控制</h4><p>发送方维护者一个拥塞窗口(cwnd)的变量，对TCP发送方的速率进行限制，有：</p><p>$LastByteSent-LastByteAcked &lt;&#x3D; min(rwnd,cwnd)$</p><p>若接收方缓存足够大,则有：</p><p>$LastByteSent-LastByteAcked &lt;&#x3D; cwnd$</p><p>TCP将丢包事件(超时或三个冗余ACK)视为阻塞的证明</p><p><strong>TCP拥塞控制算法：</strong></p><p>参数：</p><ul><li>cwnd</li><li>ssthresh：慢启动阈值，用于结束慢启动模式</li></ul><ol><li>慢启动：cwnd最初设置为一个MSS的大小，只要发送被确认就×2，以指数增长，检测到拥塞时将ssthresh设置为cwnd的一半，cwnd再重新从1开始继续慢启动，只要在cwnd和ssthresh相等时，慢启动模式才结束，转入拥塞避免阶段</li><li>拥塞避免：每个RTT将cwnd+1,出现拥塞时同样将ssthresh设置为cwnd的一半，而对于由三个冗余ACK引发的拥塞，转入快速恢复阶段</li><li>快速恢复：收到一个冗余ACK，cwnd增加一个MSS</li></ol><p><img src="https://user-images.githubusercontent.com/89784872/183347556-64c8034d-a4be-4cbc-b513-ba2373f2809b.png" alt="image-20220802003022350"></p><p>最终呈现的结果为线性增(cwnd每次增加一个MSS)，乘性减(ssthresh变为cwnd的一半):</p><p><img src="https://user-images.githubusercontent.com/89784872/183347553-6be713fa-e312-4cbf-bb95-a3af014a819a.png" alt="image-20220802003001021"></p><p><strong>公平性：</strong></p><p>对于两个TCP连接公用一条链路的情况，如果假设跳过了慢启动阶段，则拥塞窗口按照线性增，乘性减的方式，到达阈值之后二者一同÷2后+1，即便最开始二者的吞吐量相差悬殊，多次÷2+1后最终区域相同，达到公平的平衡状态</p><p><img src="https://user-images.githubusercontent.com/89784872/183347560-07ff0c45-298b-4e9a-ac6f-cc61004b5bde.png" alt="image-20220802003803686"></p><p>A为起始状态，A-&gt;B-&gt;C-&gt;D,最终趋于相同</p><p>而对于UDP，UDP不存在拥塞控制，因此可能出现UDP压制TCP流量的情况</p><h2 id="网络层：数据平面"><a href="#网络层：数据平面" class="headerlink" title="网络层：数据平面"></a>网络层：数据平面</h2><h3 id="网络层概述"><a href="#网络层概述" class="headerlink" title="网络层概述"></a>网络层概述</h3><p>网络层主要分为数据平面和控制平面两个平面，分别对应着转发和路由选择的功能</p><p>控制平面通过路由选择算法为每台路由器计算出转发表(传统或SDN的方式)，路由器再根据网络层分组的首部字段，作为索引在路由表当中查询出对应的value，即为输出链路接口，即可交给下一个路由器</p><h3 id="路由器"><a href="#路由器" class="headerlink" title="路由器"></a>路由器</h3><p>主分为四个部分：</p><ul><li>输入端口：查找转发表确定输出端口，同时还执行一定的物理层和链路层的功能</li><li>交换结构：将输入结构和输出结构相连，一个”网络结构”</li><li>输出端口：存储从交换结构而来的分组，并执行物理层和链路层的功能进行传输</li><li>路由选择处理器：传统路由器中负责执行路由选择协议，SDN控制方式中负责与远程通信，接收传来的流表</li></ul><blockquote><p>在输入端口出进行查找，确定了输出端口之后再进入交换结构，向输出端口传输</p><p>此处的端口并非在应用层所说的软件端口，而是路由器的物理接入和输出接口</p></blockquote><p><img src="https://user-images.githubusercontent.com/89784872/183348776-29e6dbac-0003-4b5c-bfc9-b8abb5c2a1c6.png" alt="image-20220802213038495"></p><p><strong>输入端口处理</strong></p><p>在输入端口出执行路由表的查询，来寻找输出端口，转发决策在本地做出，无需调用集中式路由选择处理器，避免了集中式处理的瓶颈</p><p>通过最长前缀匹配原则，将一定范围内的IP地址和路由器的输出接口做映射，为保证速度，采用硬件逻辑</p><p>其他工作：</p><ul><li>进行物理层和链路层的处理</li><li>检查分组的版本号，TTL，校验</li><li>更新用于网络管理的计时器</li></ul><p><strong>交换</strong></p><ul><li>经内存交换：在CPU的控制下直接完成</li><li>经总线交换</li><li>经互联网络交换：通过控制线路的导通和闭合来确定线路</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347581-8bb52128-3e32-4b15-a75e-766e97cf81c7.png" alt="image-20220802214701760"></p><p><strong>输出端口处理</strong></p><p><img src="https://user-images.githubusercontent.com/89784872/183347585-ffecc9fc-5c97-42d0-8e24-c62bdab7ae4d.png" alt="image-20220802214708708"></p><p><strong>分组调度</strong></p><p>当传输速度超过处理速度时，便会出现排队与阻塞，即分组位与输出和输入接口的缓存当中，而当缓冲区满时，则会出现丢包，网络层为尽力而为的服务，不会对丢包进行任何处理，可靠性交给运输层的TCP来进行保障</p><p>此时便需要相应的调度算法来确认分组的处理顺序</p><ul><li>FIFO</li><li>优先权排队：缓冲区分为多个队列，队列内FIFO，队列之间优先处理高优先级的</li><li>循环排队：分为多个类别的队列，队列内FIFO，队列之间循环选择</li><li>加权公平排队：分为多个权重不同的队列，分组按照权重分配给不同的队列，导致各队列内分组数量不同，队列之间循环选择。</li></ul><h3 id="网络协议"><a href="#网络协议" class="headerlink" title="网络协议"></a>网络协议</h3><h4 id="IPV4"><a href="#IPV4" class="headerlink" title="IPV4"></a>IPV4</h4><p><strong>数据报格式</strong></p><ul><li>版本号：IP协议版本，标识是IPV4还是IPV6</li><li>寿命：TTL，还能存活几跳</li><li>上层协议：标识是TCP还是UDP</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347588-7eb5db65-157b-48af-be1b-4b7b74c0e5ce.png" alt="image-20220802220343431"></p><p><strong>数据报分片</strong></p><p>最大传送单元(MTU)：链路层帧能承载的最大数据量</p><p>IP数据报封装在链路层帧中进行传输，因此受MTU的限制，需要对IP数据报进行分片，在传输到对方的网络层之后再进行重组拼接</p><p>通过数据报中的标识，标志，偏移量来确定该段分片在整个数据报中的位置</p><p><strong>IPV4编址</strong></p><p>IP长度32比特，点分十进制记法</p><p>IPV4总共有40多亿个地址，因此直接通过路由寻址到精确完整的IPV4地址开销较大，在公网中，以子网为单位进行寻址，便有了子网掩码</p><blockquote><p>为了确定子网，分开主机和路由器的每个接口，产生的几个隔离的网络岛，使用接口段接这些隔离的网络的端点，这些隔离的网络中的每一个都叫一个子网</p></blockquote><p>在同一个子网内的IP，左侧的几位是相同的，称为子网地址，并通过子网掩码来进行区分</p><p>例 IP地址均为231.1.1.XXX,左侧24位为子网地址，子网掩码可以表示为&#x2F;24，也可表示为255.255.255.0</p><p><img src="https://user-images.githubusercontent.com/89784872/183347589-6fb678e8-10aa-4071-ba51-e600adcfac48.png" alt="image-20220802222717520"></p><p>图中共六个子网：</p><ul><li>223.1.1.0&#x2F;24  223.1.2.0&#x2F;24  223.1.3.0&#x2F;24</li><li>223.1.9.0&#x2F;24  223.1.8.0&#x2F;24  223.1.7.0&#x2F;24</li></ul><p><em>路由聚合</em>：</p><p>ISP对子网进行聚合，向外界通告，将XXX.XXX.XXX.XXX&#x2F;XX的IP均发给我</p><p><img src="https://user-images.githubusercontent.com/89784872/183347591-00f186da-909d-46f7-936b-3aa326d8d503.png" alt="image-20220802223150307"></p><p>在CIDR被采用之前，IP地址采用的为分类编址，即分为A，B，C类，网络部分长度分别为8,16,25比特，B类子网中可以有65534个地址，而C类只有255个地址，因此导致B类被很快抢完，并且子网内的地址却用不完，C类又不够用</p><p>因此为解决这种情况，提出了五类别域间路由选择(CIDR)，将子网寻址的概念一般化，均使用IP+子网掩码的方式，众生平等</p><p><strong>地址分配</strong></p><ol><li><p>从ISP获取一块地址，或通过ICANN</p></li><li><p>获取主机地址：DHCP</p><ol><li>DHCP客户生成包含DHCP发现报文的数据包，通过链路层广播到子网的所有节点，让DHCP服务器发现客户</li><li>DHCP提供服务，向其提供IP地址，子网掩码和地址租用期</li><li>DHCP请求，客户从一个或多个服务器中选择一个，做出响应</li><li>DHCP ACK，交互完成</li></ol></li></ol><h4 id="地址转换"><a href="#地址转换" class="headerlink" title="地址转换"></a>地址转换</h4><p>公网中以子网为路由单位，子网内部可以使用如10.0.0.0&#x2F;24这类的内网IP进行通讯，但是在对外时内网IP显然无法使用</p><p>因此每个内网通过一个NAT路由器对外进行访问，在外部看来，NAT路由器像是个单一IP的单一设备，对外隐藏了内网的细节</p><p>而由外向内时，通过NAT转换表来将NAT的IP+端口转换到子网内一台设备上的子网IP+端口</p><h4 id="IPV6"><a href="#IPV6" class="headerlink" title="IPV6"></a>IPV6</h4><p><img src="https://user-images.githubusercontent.com/89784872/183347592-a866b5a7-5ffc-475d-9388-4b53b257f44f.png" alt="image-20220802231627788"></p><ul><li>地址扩容 32bit-&gt;128bit</li><li>流标签，标识一条数据包中的流，给出一条流中的某些数据报给出优先权</li></ul><p>通过隧道的方式实现IPV4到IPV6的迁移，即在两个IPV6路由器之间设置一个IPV4路由器，中间的IPV4路由器集合称为隧道，将整个IPV6数据报放置在IPV4数据报的载荷内，隧道中间的IPV4路由器不知该数据报中含有IPV6的数据报，只是当作正常的IPV4数据报处理，隧道另一边的IPV6路由器确定含有IPV6数据报，取出之后为IPV6数据报提供路由</p><h2 id="网络层：控制平面"><a href="#网络层：控制平面" class="headerlink" title="网络层：控制平面"></a>网络层：控制平面</h2><p>控制方式：</p><ul><li>每路由器控制：每台路由器允许路由选择算法</li><li>逻辑集中式控制：集中式控制器计算并分发转发表</li></ul><h3 id="路由选择算法"><a href="#路由选择算法" class="headerlink" title="路由选择算法"></a>路由选择算法</h3><p>本质上就是解决图的最短路径问题，可通过集中式或分布式来划分：</p><ul><li>集中式路由选择算法：在真正开始计算前，需要对全局的cost已知，也叫做链路状态算法，代表为Dijkstra算法</li><li>分布式路由选择算法：计算开始时各个路由器只知道自己邻居的状态，没人清楚全局的状态。路由器通过迭代的方式，不断告诉邻居相应的状态，从而最终获取全局的状态，确认路径</li></ul><h4 id="链路状态路由选择算法-Dijkstra算法"><a href="#链路状态路由选择算法-Dijkstra算法" class="headerlink" title="链路状态路由选择算法-Dijkstra算法"></a>链路状态路由选择算法-Dijkstra算法</h4><p>本质为迭代算法，经过N次迭代可以知道从起始节点到N个节点的最短开销路径，核心思想为贪心算法，通过局部最优解来推算出全局最优解</p><p><strong>例：</strong></p><p><img src="https://user-images.githubusercontent.com/89784872/183347576-e65dc916-ab57-4cbc-bf3f-a4bbdada95b2.png" alt="image-20220803211544939"></p><p><img src="https://user-images.githubusercontent.com/89784872/183347670-27e439ba-eda0-4477-afb0-8d7a29598308.png" alt="image-20220803211557526"></p><p>求以u为起点，到各个点的最小开销路径</p><p>具体步骤：</p><ol><li>初始化：与u相连的个点的距离设置为图上标注的cost，与u无法直达的点初始化为∞，u放置在已知点的队列之中，其他点放置在未知点队列之中</li><li>第一次迭代：选取一个距离最短的点，加入到已知节点当中，在第一次选择的便是·x,cost为1，此时的已知队列变为ux</li><li>第二次迭代：由于x已经为已知节点，便可计算通过x出发的消耗，再加上已知的ux之间距离，即可计算出该节点到u的距离，例w通过x到达比u直达更短，对其进行更新，y通过x变为可达节点，也对其进行更新</li><li>重复迭代直至所有的节点均变为已知节点</li></ol><p>代码实现：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">package</span> main<br><br><span class="hljs-keyword">import</span> (<br>    <span class="hljs-string">&quot;fmt&quot;</span><br>    <span class="hljs-string">&quot;math&quot;</span><br>)<br><br><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span> &#123;<br><br>    <span class="hljs-comment">// 初始化图 -1代表不可达的无穷远</span><br>    graph :=[...][<span class="hljs-number">6</span>]<span class="hljs-type">int</span>&#123;&#123;<span class="hljs-number">0</span>,<span class="hljs-number">2</span>,<span class="hljs-number">5</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">1</span>&#125;,<br>                        &#123;<span class="hljs-number">2</span>,<span class="hljs-number">0</span>,<span class="hljs-number">3</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">2</span>&#125;,<br>                        &#123;<span class="hljs-number">5</span>,<span class="hljs-number">3</span>,<span class="hljs-number">0</span>,<span class="hljs-number">5</span>,<span class="hljs-number">1</span>,<span class="hljs-number">3</span>&#125;,<br>                        &#123;<span class="hljs-number">-1</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">5</span>,<span class="hljs-number">0</span>,<span class="hljs-number">2</span>,<span class="hljs-number">-1</span>&#125;,<br>                        &#123;<span class="hljs-number">-1</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">0</span>,<span class="hljs-number">1</span>&#125;,<br>                        &#123;<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">1</span>,<span class="hljs-number">0</span>&#125;&#125;<br><br>    <span class="hljs-keyword">var</span> length = <span class="hljs-built_in">len</span>(graph)<br>    <span class="hljs-comment">// 起始点</span><br>    startVertex := <span class="hljs-number">0</span><br>    <span class="hljs-comment">// result保存最终的路径长度，初始化为全为不可达</span><br>    result := [<span class="hljs-number">6</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">-1</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">-1</span>,<span class="hljs-number">-1</span>&#125;<br>    <span class="hljs-comment">// 自己到自己的距离为0</span><br>    result[startVertex] = <span class="hljs-number">0</span><br>    <span class="hljs-comment">// 使用起始点的距离到初始化所有的不确定节点</span><br>    notFound := [<span class="hljs-number">6</span>]<span class="hljs-type">int</span>&#123;<span class="hljs-number">0</span>&#125;<br>    <span class="hljs-keyword">for</span> i:=<span class="hljs-number">0</span>;i&lt;length;i++ &#123;<br>        notFound[i] = graph[startVertex][i]<br>    &#125;<br><br>    <span class="hljs-keyword">for</span> i:=<span class="hljs-number">1</span>;i&lt;length;i++ &#123;<br>        min := math.MaxInt<br>        minIndex := <span class="hljs-number">0</span><br>        <span class="hljs-comment">// 贪心思想，在所有不确定的节点中选择一个最小的，将其加入到已知节点当中</span><br>        <span class="hljs-keyword">for</span> j:=<span class="hljs-number">0</span>;j &lt; length;j++ &#123;<br>            <span class="hljs-keyword">if</span> notFound[j] &gt; <span class="hljs-number">0</span> &amp;&amp; notFound[j] &lt; min &#123;<br>                min = notFound[j]<br>                minIndex = j<br>            &#125;<br>        &#125;<br>        <span class="hljs-comment">// 加入到已知节点，notFound中设置为不可达，从未知节点中删除</span><br>        result[minIndex] = min<br>        notFound[minIndex] = <span class="hljs-number">-1</span><br>        <span class="hljs-comment">// 添加了新的已知节点，因此从新的已知节点出发，对最小距离进行更新</span><br>        <span class="hljs-keyword">for</span> j := <span class="hljs-number">0</span>;j &lt; length; j++ &#123;<br>            <span class="hljs-keyword">if</span> graph[minIndex][j] &gt; <span class="hljs-number">0</span> &amp;&amp; result[j] == <span class="hljs-number">-1</span> &#123;<br>                <span class="hljs-comment">// result[minIndex]为起始点到新节点的距离 graph[minIndex][j]为新节点到其他所有节点的距离</span><br>                newDistance := result[minIndex] +graph[minIndex][j]<br><br>                <span class="hljs-keyword">if</span> newDistance &lt; notFound[j] || notFound[j] == <span class="hljs-number">-1</span> &#123;<br>                    notFound[j] = newDistance<br>                &#125;<br>            &#125;<br>        &#125;<br>    &#125;<br>    fmt.Println(result)<br>    <span class="hljs-comment">// 结果打印：[0 2 3 4 2 1]</span><br><br>&#125;<br></code></pre></td></tr></table></figure><p>最差情况下的复杂度为O(n^2)</p><h4 id="距离向量路由选择算法-Bellman-Ford方程"><a href="#距离向量路由选择算法-Bellman-Ford方程" class="headerlink" title="距离向量路由选择算法-Bellman-Ford方程"></a>距离向量路由选择算法-Bellman-Ford方程</h4><p>迭代、异步、分布式的算法：每个节点都从邻居那获取信息，执行计算，然后将计算结果发送给邻居，一直持续到再无更多信息要交换为止</p><p><strong>Bellman-Ford方程</strong></p><p>$d_x(y) &#x3D; min_v[c(x,v)+d_v(y)]$</p><blockquote><p>$d_x(y)$:从节点x到节点y的最小开销</p><p>c(x,v) :节点v到x的开销</p><p>$d_v(y)$:节点v到节点y的最小开销</p><p>$min_v$所针对的为x的所有邻居</p></blockquote><p><img src="https://user-images.githubusercontent.com/89784872/183347675-11193057-dc3c-45d6-942a-2fda0c84fe7b.png" alt="image-20220803230722626"></p><p>具体步骤：</p><ol><li>最初xyz仅知道到自己邻居的直接距离</li><li>y将自己的信息传递给x之后，x计算以y为中间节点到达z的距离，并进行更新，y z同理进行计算更新</li><li>再将新计算出信息继续进行传递，(x通过y的信息计算出新的信息传递给y，z)</li><li>直至最后没有新的信息产生</li></ol><p><strong>好消息传得快，坏消息传得慢</strong></p><p><img src="https://user-images.githubusercontent.com/89784872/183347680-359c07b7-3459-452a-8137-0e4d0051bc5a.png" alt="image-20220803233202177"></p><ul><li><p>对于cost由4变为1的好消息：</p><ol><li>y检测到链路发生变化，对其进行更新</li><li>y通知z y至x的距离进行了缩减，z进行重新计算</li><li>z将重新计算后的结果发给y，y无需进行更新，至此算法进入静止状态，结束迭代</li></ol></li><li><p>对于cost由4变为60的坏消息：</p><ol><li>y计算到x的开销：min(60,6) &#x3D; 6</li><li>y将消息传递给z，z无需进行更新，进入静止状态</li></ol><p>但如果x z之间的开销为50：</p><img src="https://user-images.githubusercontent.com/89784872/183347682-583751d9-85af-44f6-baaa-a5175ed88a83.png" alt="image-20220803233933903" style="zoom:67%;" /><ol><li>y计算到x的开销：min(60,6) &#x3D; 6</li><li>y将消息传递给z，z到x的开销为min(50,1+)</li></ol></li></ul><p>&#x3D;&#x3D;存疑&#x3D;&#x3D;</p><p>LS DV二者之间比较：</p><ul><li>DV仅与他的邻居进行交谈，LS需要全局信息</li><li>收敛速度：LS要求O(NE)个报文复杂度为O(N^2) 的算法，DV需通过迭代，收敛速度较慢，还会遇到无穷计数的问题</li><li>健壮性：DV算法情况下，如果一个节点发生问题，则会导致迭代异常，从而影响全局，不正确的结果会扩散至整个网络，LS使用的Dijkstra算法，处理的为单源问题，故计算的只是自己路由器到其他节点的开销，每个路由器之间为独立计算，健壮性较好</li></ul><h3 id="路由协议"><a href="#路由协议" class="headerlink" title="路由协议"></a>路由协议</h3><p>在进行路由时，基本以子网为单位，全网几百万的子网来执行距离矢量算法或者链路状态算法开销依旧是承担不起的，因此，提出了自治区的概念：</p><ul><li>在自治区内部按照自治区内部的路由选择算法来进行路由，使用RIP、OSPF或者私有的算法</li><li>在自治区之间，通过BGP算法，将各个自治区(ISP)粘合起来</li></ul><p>这样，即便是新加入了一个一个新的子网，配合上路由聚合，子网内的多个IP对于全网来说也就是多了一个点，大大减轻了经路由选择算法的负担</p><h3 id="自治系统内部的路由选择"><a href="#自治系统内部的路由选择" class="headerlink" title="自治系统内部的路由选择"></a>自治系统内部的路由选择</h3><h4 id="RIP"><a href="#RIP" class="headerlink" title="RIP"></a>RIP</h4><ul><li>基于距离矢量算法</li><li>代价为跳数</li><li>跳数最大为15跳，16跳视为不可达</li><li>最多25个目标子网，适用于小网</li><li>每隔30秒交换报文，或者请求时交换报文</li></ul><h4 id="OSPF"><a href="#OSPF" class="headerlink" title="OSPF"></a>OSPF</h4><ul><li>使用泛洪链路状态信息和Dijkstra算法</li><li>链路的代价由管理员配置</li><li>链路状态发生变化时，广播链路状态信息，未变化则每隔30min进行一次广播</li><li>优点：<ul><li>安全</li><li>当存在多条开销相同的路径，允许使用多条路径，减小单条路径的压力</li><li>可层次化配置多个区域，每个区域运行自己的链路选择算法</li></ul></li></ul><h3 id="ISP之间路由选择：BGP"><a href="#ISP之间路由选择：BGP" class="headerlink" title="ISP之间路由选择：BGP"></a>ISP之间路由选择：BGP</h3><p>RIP和OSPF完成了自治系统内部的，BGP进行自治系统之间的路由，将多个ISP黏合起来</p><p>可以理解成距离矢量算法的升级，通知的信息更加全面，在BGP协议当中，不仅向邻居通告开销，并且向邻居进行通告路径</p><p><img src="https://user-images.githubusercontent.com/89784872/183347684-3fffa6bf-2013-4163-9d1d-55a867e66a98.png" alt="image-20220804193600933"></p><p>例如此时3b上线，3a路由器向1c路由器通告 AS3 3b，1b路由器向2a路由器通告AS3 AS1 3b,此时 AS1 AS2自治系统均知道了AS3中的3b节点上线，并且可以通过AS1 AS3抵达</p><ul><li>跨越两个AS的连接称为外部BGP(external BGP eBGP)</li><li>AS之内的称为内部BGP(internal BGP iBGP)</li></ul><p>例如3a路由器，既可以作为网关BGP之间进行通告，又可以作为内部路由器参与内部路由算法</p><ul><li>NEXT-HOP:下一个AS的网关路由器的接口，在上图中，对于AS3向2b发送，AS1中的1c即为NEXT-HOP</li><li>AS-PATH:包含通告已经经过了的AS的列表，在上述例子中，AS2到子网3b，AS-PATH为 AS1 AS3</li></ul><p><strong>热土豆算法</strong></p><p>字面意思，尽快将手上的热土豆尽快抛出，尽快将分组送出AS因此选择通往NEXT-HOP路由器的最小开销的路径 </p><p>热土豆算法只考虑了在自己的AS内的最小代价，而并未考虑AS之间传输的代价，因此，最终取得的路由选择结果并非最优结果，只是一条开销较小的路径</p><p><strong>实际应用</strong></p><ol><li>路由器被指派了一个本地偏好作为其属性之一，该属性完全由管理员负责，具有最高本地偏好值的路由将被选择</li><li>当本地偏好相同时，选择具有最短AS-PATH的路由</li><li>本地偏好和AS-PATH均相同时，使用热土豆算法</li></ol><h3 id="SDN控制平面"><a href="#SDN控制平面" class="headerlink" title="SDN控制平面"></a>SDN控制平面</h3><p>体系结构特点：</p><ul><li>基于流的转发</li><li>数据平面和控制平面相分离，其中控制平面由一个控制器和若干个网络控制应用程序组成</li><li>网络控制功能</li><li>可编程网络 ：网络控制应用程序为可编程1的</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347828-196603c6-3801-4b10-9126-b44dd3aad33b.png" alt="img"></p><p>相比于传统的网络层 SDN最大的特点即为分层，原本交换机&#x2F;路由器为一个整体，只能由一家厂商生产，而对于SDN的方式，不同层次的服务可以由不同厂商来提供，在生态系统上更加开放</p><p>控制器组成：</p><ul><li>通信层：用户在控制器和SDN控制器控制的交换机、主机等之间传送信息，之间通过南向接口</li><li>网络范围状态管理层：SDN控制平面所做出的最终控制决定，SDN控制设备的最新状态信息等</li><li>对于网络控制应用程序层的接口：控制器和网络控制应用程序之间通过北向接口进行交互(REST风格)</li></ul><p>SDN控制器在逻辑上集中，但是具体实现上为分布式的，如服务或数据库采用分布式结构</p><h2 id="链路层"><a href="#链路层" class="headerlink" title="链路层"></a>链路层</h2><h3 id="概述-3"><a href="#概述-3" class="headerlink" title="概述"></a>概述</h3><p>概念：</p><ul><li>节点：运行链路层协议的任何设备</li><li>链路：通信路径连接相邻节点的通信信道</li><li>网络适配器：俗称网卡，链路层的主体实现部分</li></ul><p>链路层提供的服务：</p><ul><li>成帧：将网络层的IP数据报作为Payload加上头部封装成帧</li><li>链路接入</li><li>可靠交付：对于某些出错率高的传输方式，如无线网等，对其进行可靠性保证，而对于如以太网协议等出错概率极低的，不添加可靠性保证</li><li>差错检测和纠正</li></ul><h3 id="差错检测和纠正技术"><a href="#差错检测和纠正技术" class="headerlink" title="差错检测和纠正技术"></a>差错检测和纠正技术</h3><h4 id="奇偶校验"><a href="#奇偶校验" class="headerlink" title="奇偶校验"></a>奇偶校验</h4><p>增加一位校验比特，数据比特中1的数量为奇数校验比特则为1，偶数则为0</p><p>在接收方重新计算1的数量，和校验位进行比较</p><p><img src="https://user-images.githubusercontent.com/89784872/183347716-b397879a-c9ef-4581-bc96-26b6dafed579.png" alt="image-20220806211942726"></p><p>问题：但出现偶数位的错误时则校验不出</p><p><strong>二维奇偶校验</strong></p><p>同样也存在校验不出的情况，如下：</p><img src="https://user-images.githubusercontent.com/89784872/183347720-8810b18b-04a2-4f63-aa44-b71174ee5ca0.png" alt="image-20220806213011351" style="zoom:50%;" /><p>图中的四位一同发生错误，校验位的结果仍保持不变</p><h4 id="校验和"><a href="#校验和" class="headerlink" title="校验和"></a>校验和</h4><p>和TCP&#x2F;UDP中的校验和checksum思路相同，将K位相加，将得到的和作为差错检测比特，在接收方与反码进行相加，检验结果是否为0</p><h4 id="循环冗余检测"><a href="#循环冗余检测" class="headerlink" title="循环冗余检测"></a>循环冗余检测</h4><p>规则：</p><ul><li><p>将数据为左移r位，并添加r比特的附加比特，形成最终要传输的值</p></li><li><p>双方约定一个生成多项式，称为G，G的最高位必须为1</p></li><li><p>在接收方，如果收到的值进行模2运算(异或)恰好能被G整除，则<strong>大概率</strong>不存在错误</p></li></ul><p>因此，现在的问题变成了如何求出R来保证合成的数据能被G整除</p><p><img src="https://user-images.githubusercontent.com/89784872/183347724-8a763edc-0635-4dba-9765-9489093fcc5d.png" alt="image-20220806214134532"></p><p><strong>公式推导：</strong></p><p>如果能被G整除(模2运算)，那么则有：$D*2^{r} ⊕ R &#x3D; nG$（D左移后空出的位均为0，0与R异或结果为R，相当于把R补在了后面）</p><p>对公式进行变化，两边同时异或R：$D*2^{r} ⊕ R ⊕ R &#x3D; nG ⊕ R$</p><p>又因为：$R ⊕ R &#x3D; 0$，则有：$ D * 2^{r} ⊕ 0 &#x3D; nG ⊕ R$</p><p>$D * 2^{r} &#x3D; nG ⊕ R$,该式子表明：$D * 2^{r}$除nG得到的余数为R</p><p>$R &#x3D; remainder D * (2^{r}&#x2F;G)$</p><p><img src="https://user-images.githubusercontent.com/89784872/183347725-4965ef59-6773-466c-b2f2-35ee1a19eb8a.png" alt="image-20220806215526703"></p><h3 id="多路访问链路和协议"><a href="#多路访问链路和协议" class="headerlink" title="多路访问链路和协议"></a>多路访问链路和协议</h3><p>在广域网之上，链路层采用的是点到点的传输，像太平洋之下的海底电缆，纽约直通上海，二者之间直发</p><p>但是对于局域网来说，采用两两点对点连接的形成的网状过于复杂，因此在局域网之上采用的方案为多个设备连接到同一个共享设备之上，一个设备进行发送，采用的为广播的方式，所有的其他节点都能收到，但这也引入了两个问题：</p><ul><li>多个节点同时发送时会产生碰撞，好比多个人同时说话而互相听不清</li><li>如何辨别广播中的信号是发送给自己的</li></ul><p>辨别广播信号是否是发送给自己的，则引入了MAC地址，通过比较帧中的目标MAC，来判断自己是否应该接收</p><p>为解决碰撞问题，引路了多种多路访问协议，主要有：</p><ul><li>信道划分协议</li><li>随机接入协议</li><li>轮流协议</li></ul><h4 id="信道划分协议"><a href="#信道划分协议" class="headerlink" title="信道划分协议"></a>信道划分协议</h4><ul><li>时分 TDM，每个节点只有在在允许自己发送的时隙才能发送</li><li>频分 FDM，每个节点使用自己的频率，类似收音机</li><li>码分 CDMA 每个节点使用自己的编码</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347728-4da14888-7d86-46a6-9202-37519ab3ace0.png" alt="image-20220806221839268"></p><p>时隙为真正的时间划分单位，每个节点发送一个时隙后切换为下一个节点</p><p>帧为统计周期，信道的传输速率为R，N个节点时，在一个帧内，一个节点的发送速率为$R&#x2F;N$</p><p>无论是时分，频分，还是码分，存在的问题都是一个节点的发送速率被限制在了$R&#x2F;N$,无法有效利用信道容量来全速发送，随机接入协议即可解决该问题</p><h4 id="随机接入协议"><a href="#随机接入协议" class="headerlink" title="随机接入协议"></a>随机接入协议</h4><p>一个节点总是按照全速来进行发送，当有碰撞时，涉及碰撞的节点反复重新发送，直至无碰撞的通过为止。但是如果多个节点都同时重新发送，那么碰撞的问题则会一直存在，形成阻塞，因此引入一个随机时延，每个节点在经过一个随机时延后再重新发送，将各个节点的发送错开</p><p>而对于随机接入协议，也可继续细分</p><p><strong>时隙ALOHA</strong></p><p>做出如下假设：</p><ul><li>所有帧由L比特组成</li><li>时间被划分成L&#x2F;R的时隙(一个时隙等于传输一帧的时间)</li><li>节点在时隙开始时传输帧</li><li>节点时同步的，每个均知道时隙什么时候开始</li><li>一个时隙中有两个或者更多个帧碰撞，所有节点在时隙结束前能检测到</li></ul><p>设定一个概率p，操作逻辑如下：</p><ul><li>当节点有新帧需要发送时，在下一个时隙开始时传输整个帧</li><li>没有碰撞则成功传输</li><li>如果存在碰撞，该节点在时隙结束之前检测到，在后续的时隙按照p进行重传</li></ul><p>通过按照概率重传，将碰撞错开，避免阻塞，并且在传输时可以按照满速进行传输</p><p><strong>性能分析：</strong></p><p>现有N个节点，根据N重伯努利实验，一个节点成功发送的概率为$p(1-p)^{N-1}$,</p><p>N个节点中有一个能成功发送的概率为$N*p(1-p)^{N-1}$</p><p>最大效率为37%,即37%的时隙在做有用的工作</p><p><strong>ALOHA</strong></p><p>除无时隙之外其他和时隙ALOHA思想一致</p><p><img src="https://user-images.githubusercontent.com/89784872/183347705-d1e43d29-8e67-4aac-8a0b-1e0621954b23.png" alt="image-20220806232207506"></p><p>由于无时隙，任意时间都可进行传输，如果不存在碰撞，则在$[t_0-1,t_0+1]$时间段内均不能有其他节点进行传输，对应的概率为$p(1-p)^{2(N-1)}$</p><p>最大效率为时隙ALOHA的一半</p><p><strong>载波侦听多路访问</strong></p><ul><li>先对信道进行侦听，如果检测一小段时间没有其他节点进行传输，该节点再进行传输</li><li>检测到碰撞，自己则停止传输</li></ul><p>即便事先已经进行了侦听，但是由于传输时延的问题，依旧会产生碰撞：</p><p><img src="https://user-images.githubusercontent.com/89784872/183347791-0dc9ff46-8130-48db-917f-aca4ef90cd8a.png" alt="image-20220806233427375"></p><p><strong>具有碰撞检测的载波侦听多路访问</strong></p><p>在发生碰撞后，中止传输，等待一个随机量，再重新侦听链路，准备发送，之所以使用随机量，同样是为了将多个碰撞的节点相互错开，和使用概率p的目的一致</p><p>在用于以太网时，做出了细微调整，当该节点发生了n次碰撞，则从$[0,1,2,….2^{n-1}]$中选择一个作为等待时间，碰撞的次数越多，等待时间长的可能性就越大，从而更大程度的保证错开</p><h4 id="轮流协议"><a href="#轮流协议" class="headerlink" title="轮流协议"></a>轮流协议</h4><p><strong>轮询协议：</strong></p><p>选择一个节点为主节点，主节点按照循环的方向轮询每个节点，主节点观察信道上是否缺乏信号，来决定一个节点何时能发送帧</p><p>缺陷：</p><ul><li>主节点寄则协议寄</li><li>引入了轮询时延</li></ul><p><strong>令牌传递协议：</strong></p><p>使用token在节点之间按照次序进行传递，只有持有令牌时才能有资格发送，发送后将token传递给下一个节点</p><h3 id="交换局域网"><a href="#交换局域网" class="headerlink" title="交换局域网"></a>交换局域网</h3><h4 id="寻址"><a href="#寻址" class="headerlink" title="寻址"></a>寻址</h4><p><strong>MAC地址</strong></p><p>拥有链路层地址实际上为网卡，而非主机或路由器</p><p>链路层的交换机不具有和接口相对应的链路层地址，(而对于路由器而言，每个接口均有对应的IP地址，转发即是寻找正确的接口),因此交换机透明的执行承载数据报的任务，主机和路游戏寻址也不会寻址到交换机处</p><p>并且，MAC地址解决了之前提及的如何在一个广播的链路中确定该帧是否是发给自己的问题，通过将帧中的目标和自己的MAC地址进行匹配，如果不匹配，则丢弃该帧</p><p><strong>地址解析协议ARP</strong></p><p>每台主机和路由器在内存中包含一个ARP表，ARP表包含IP和MAC地址之间的映射关系</p><p>通过ARP协议，以下一跳的IP作为输入，返回相应的MAC地址</p><p>在子网内，通过广播的方式，向各个主机和路由器发起查询，以求获取对应的MAC地址</p><p><strong>跨域子网发送</strong></p><p><img src="https://user-images.githubusercontent.com/89784872/183347793-b03cbe23-6e93-46c9-8b1d-a8ca4de2e9ef.png" alt="image-20220807001349467"></p><p>以111.111.111.111向222.222.222.222发送为例：</p><ol><li>首先111.111.111.111想向外发送，首先通过IP的路由选择算法确定了下一跳的IP为网关路由111.111.111.110</li><li>再通过ARP协议，查询到该路由器接口的MAC地址，此时便可以将帧进行发送</li><li>通过路由器的转发表确定通过接口222.222.222.220向外进行转发</li><li>该接口再通过ARP通过目标IP来查询对应的MAC地址：49-BD-D2-C7-56-2A</li><li>该接口对帧重新进行封装，源MAC为1A-23-F9-00-11-BB-4B,目标MAC为49-BD-D2-C7-56-2A</li><li>最终，222.222.222.222收到了该帧，解析出IP数据报向网络层进行传递</li></ol><h4 id="以太网"><a href="#以太网" class="headerlink" title="以太网"></a>以太网</h4><p>有线局域网</p><p>帧结构：<img src="https://user-images.githubusercontent.com/89784872/183347800-4e7a47bc-b408-435e-a79f-3028bb66dcf4.png" alt="image-20220807003112072"></p><ul><li>数据字段承载IP数据报</li><li>类型：网络层协议名</li><li>CRC：循环冗余校验</li></ul><p>由于有线网本身较为可靠出错率极低，故以太网提供的为不可靠的服务，CRC通过时不确认，未通过也不否定，依靠网络层来提供可靠服务(TCP)</p><h4 id="链路层交换机"><a href="#链路层交换机" class="headerlink" title="链路层交换机"></a>链路层交换机</h4><p>接收入链路层帧并转发到出链路</p><p>交换机本身无MAC地址也不会被寻址，透明进行工作</p><p><strong>转发和过滤</strong></p><p>借助交换机表：</p><ul><li>MAC地址</li><li>接口</li><li>表项放置的时间</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347807-156578a1-8dcb-4404-9e39-bc9ee45a7d36.png" alt="image-20220807004750337"></p><p>如果在交换机表中找不到对应的匹配项，或者匹配到的为输入接口，进行过滤</p><p><strong>自学习</strong></p><p>交换机表为自动、动态。自治的建立起来的，无需管理员进行干预</p><ul><li>交换机表初始为空</li><li>对于每个接口接收到的如真，交换机表将其存储在其中</li><li>在一段时间后(老化期)，如果没有接收到以该地址作为源地址的帧，将其从中删除</li></ul><p><strong>交换器和路由器</strong></p><ul><li>路由器使用网络层地址转发分组，交换机使用MAC地址转发分组</li><li>交换机为第二层分组交换机，路由器为第三层</li><li>交换机只是根据交换机表进行MAC地址的匹配和转发，自身并无地址，路由器的接口存在IP地址和MAC地址，参与路由选择算法</li></ul><p><img src="https://user-images.githubusercontent.com/89784872/183347811-eb20519e-28d9-46dc-bb3e-742b25578062.png" alt="image-20220807010228212"></p><h4 id="虚拟局域网"><a href="#虚拟局域网" class="headerlink" title="虚拟局域网"></a>虚拟局域网</h4><p>支持VLAN的交换机允许在一个物理局域网中定义多个虚拟局域网</p><p>对于交换机的端口进行分组，一个组分为一个虚拟局域网，彼此之间相互通信，不同组之间的帧彼此隔离</p><p>而不同的VLAN之间想要进行通信则需要借助一台外部路由器，逻辑上时一个LAN跨域了路由器抵达了另一个LAN，在硬件上，通过将交换机和路由器进行集成，合成单一设备，省去了外部路由器</p><p><img src="https://user-images.githubusercontent.com/89784872/183347812-1b0cf1a4-fe94-43ca-be9a-fdc52ad8907d.png" alt="image-20220807010929081"></p><h3 id="数据中心网络"><a href="#数据中心网络" class="headerlink" title="数据中心网络"></a>数据中心网络</h3><p><img src="https://user-images.githubusercontent.com/89784872/183347816-7e07865a-e8a2-4bc8-a606-34d263b35d0f.png" alt="image-20220807011222411"></p><ul><li>主机分布在不同的机架上，每个机架上设置一台交换机，连接主机和其他交换机</li><li>设置边界路由器，和外部因特网相连</li><li>等级体系结构</li><li>负载均衡：外部请求首先被定义到负载均衡器，负载均衡器再将请求分发到对应的主机上，类似于NAT或反向代理服务器</li></ul><p>当前的等级体系结构在内部流量的转发存在一定的限制，如机架1 2 3 4 分别向 5 6 7 8发送请求，则A-&gt;B,B-&gt;C则成为了瓶颈，故向全连接的方向演变</p><p><img src="https://user-images.githubusercontent.com/89784872/183347821-a53b2015-d13a-495d-ac64-6c2c1ab2972a.png" alt="image-20220807011728637"></p><h2 id="Web请求历程"><a href="#Web请求历程" class="headerlink" title="Web请求历程"></a>Web请求历程</h2><h3 id="1-DHCP、UDP、IP和以太网"><a href="#1-DHCP、UDP、IP和以太网" class="headerlink" title="1.DHCP、UDP、IP和以太网"></a>1.DHCP、UDP、IP和以太网</h3><p>当主机和网络连接时，首先需要做的便是获取IP，因此采取的第一个动作便是运行DHCP协议，将自己接入到网络当中</p><ol><li>生成一个DHCP请求报文 放入UDP报文段，该UDP报文段在放入到目标IP为255.255.255.255(广播),源IP为0.0.0.0的IP数据报</li><li>将该IP数据报封装到目标MAC为FF:FF:FF:FF:FF:FF的以太网帧中，进行广播，由于MAC地址和网卡绑定，故源MAC即为用户主机的MAC地址</li><li>由于目标MAC为广播，因此交换机向所有的出端口广播，最终达到路由器</li><li>路由器从以太网帧中抽出IP数据报，目标IP为255.255.255.255指示了这个IP数据报应当由该节点高层进行处理，上至UDP，在传输层抽取出DHCP请求报文，此时DHCP服务器获取到了DHCP请求报文</li><li>DHCP选取一个IP，生成包含这个IP和DNS服务器的IP、默认网关路由器的IP、子网掩码的DHCP ACK 报文，再依次放入到UDP数据报 IP数据报 以太网帧当中，目标MAC即为用户主机的MAC</li><li>由于交换机为自学习的，之前从用户主机发送的已经被记录，因此按照原本的记录即可转发到用户主机</li><li>用户主机收到后，依次抽取 IP数据报 UDP数据报 DHCP ACK 报文，DHCP客户端记录下IP和子网掩码和DNS服务器的IP，在转发表中安装默认网关</li></ol><h3 id="2-DNS和ARP"><a href="#2-DNS和ARP" class="headerlink" title="2.DNS和ARP"></a>2.DNS和ARP</h3><p>此时，便可以进行互联网的访问了，输入<a href="http://www.google.com的url,此时,只知道域名,故目前应当做的便是通过dns解析出对应的ip地址和使用arp解析出mac地址,最终将请求定位到谷歌的服务器之上/">www.google.com的URL,此时，只知道域名，故目前应当做的便是通过DNS解析出对应的IP地址和使用ARP解析出MAC地址，最终将请求定位到谷歌的服务器之上</a></p><ol start="8"><li>用户主机生成一个DNS查询报文，将<a href="http://www.google.com/">www.google.com</a> 放入到问题段当中，封装成UDP报文，该UDP报文封装到目标IP为DNS服务器的IP数据包当中</li><li>将该IP数据报放入到以太网帧当中，在上一步知道了网关路由器的IP，为获取MAC地址，则需要使用到ARP</li><li>生成一个目标IP为网关路由器的ARP查询报文，将该报文放入到目标MAC为FF:FF:FF:FF:FF:FF(广播)的以太网帧当中，向交换机发送该以太网帧，交换机将其广播给所有的设备</li><li>网关路由器连向用户主机内网的接口收到了该以太网帧，在ARP表当中查询到了对应的MAC地址，准备一个ARP响应报文，放入到以太网帧当中，目标MAC即为用户主机网卡的MAC地址。通过交换机转发至用户主机处</li><li>用户主机从以太网帧当中抽取出网关路由器的MAC地址，用户的主机此时便定位到了网关路由器，准备将跨越子网向DNS服务器发送请求</li><li>用户主机将包含DNS查询的以太网帧寻址到网关路由器的MAC地址，通过交换机发送至网关路由器处</li></ol><h3 id="3-域内路由选择到DNS服务器"><a href="#3-域内路由选择到DNS服务器" class="headerlink" title="3.域内路由选择到DNS服务器"></a>3.域内路由选择到DNS服务器</h3><ol start="14"><li>网关路由器的连接用户子网的接口抽取处IP数据报，检查目标IP，通过路由表确定出接口，将其通过链路层转发到Comcast网络的路由器上(转发表通过BGP协议+OSPF&#x2F;RIP协议事先确定)</li><li>此时以太网帧到达Comcast网络的网关服务器的接口，该接口检查目标服务器，通过转发表确认出接口向DNS服务器发送数据报(转发表通过OSPF&#x2F;RIP协议事先确定)</li><li>最终，包含DNS查询的数据报到达了DNS服务器，依次从中解析接IP数据报，UDP数据报，DNF查询报文，通过缓存&#x2F;迭代查询&#x2F;递归查询的方式查询出该域名对应的IP,生成一个DNS应答报文。放入UDP数据报当中，再放入目标IP为用户主机的IP数据报当中，通过Comcast反向转发到学校路由器，并经过以太网交换机抵达用户主机</li><li>用户主机依次抽取IP数据报、UDP数据报、获取到DNS响应报文，获取<a href="http://www.google.com的ip地址,准备向谷歌的服务器建立连接/">www.google.com的IP地址，准备向谷歌的服务器建立连接</a></li></ol><h3 id="4-Web客户-服务器交互"><a href="#4-Web客户-服务器交互" class="headerlink" title="4.Web客户-服务器交互"></a>4.Web客户-服务器交互</h3><ol start="18"><li>用户主机已经获取到了服务器的IP，但由于HTTP协议是基于TCP协议的，因此首先需要进行TCP三次握手建立稳定的连接：用户主机生成一个TCP SYN报文段(HTTP 目的端口为80 HTTPS目标端口为443) 放入目标IP为谷歌IP的IP数据报当中，再放入目标MAC为网关路由器的以太网帧中，发送到网关路由器</li><li>学校、Comcast、谷歌网络中的路由器通过转发表，将该数据报进行转发</li><li>最终该数据报抵达谷歌的服务器，从中抽取出 TCP SYN报文，并分解到与80&#x2F;443端口相联系的欢迎套接字，并生成一个连接套接字。产生一个SYNACK报文段，放入目标IP为用户主机IP的IP数据报，再放入以太网帧当中</li><li>该数据报依次通过谷歌网络、Comcast网络、学校网络。抵达用户主机的网卡，从中解析出SYNACK报文，数据报分解到18中生成的TCP套接字，客户端进入连接状态，再按照同样的步骤发送一个报文段，完成TCP三次握手</li><li>浏览器产生一个 HTTP GET请求报文，写入到套接字当中，GET报文成为TCP报文的Payload，TCP报文放置到IP数据报当中，向<a href="http://www.google.com按照上述步骤进行发送,/">www.google.com按照上述步骤进行发送，</a></li><li>谷歌HTTP服务器从TCP套接字中读取HTTP GET报文，将请求的web放入到HTTP响应体当中，把报文发送到套接字</li><li>包含HTTP响应的报文经过一系列转发，到达用户主机，浏览器从套接字中读取HTTP响应，抽取出HTML，进行渲染，展示。</li></ol><p><img src="https://user-images.githubusercontent.com/89784872/183347825-0a399836-083a-4b09-abaa-e916a1449d03.png" alt="image-20220807104543124"></p>]]></content>
    
    
    
    <tags>
      
      <tag>CS基础</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2022/08/08/hello-world/"/>
    <url>/2022/08/08/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
